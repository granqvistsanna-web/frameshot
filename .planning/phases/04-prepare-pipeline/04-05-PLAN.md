---
phase: 04-prepare-pipeline
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - src/cli.js
  - samples/serve-smoke.js
  - samples/smoke.yaml
autonomous: true
requirements:
  - PREP-01
  - PREP-02
  - PREP-03
  - PREP-04
  - PREP-05

# Wave 2 — depends on all four wave-1 plans (04-01 animations.js,
# 04-02 hide.js, 04-03 scroll.js, 04-04 index.js). This is the
# integration plan: it wires installAnimationGuards + runPreparePipeline
# into src/cli.js (touched also by 03-02, hence the wave-2 placement),
# extends samples/serve-smoke.js with fixture elements that exercise
# PREP-01/02/03/04, updates samples/smoke.yaml to drive the new fixture
# elements (PREP-03 hide, PREP-05 extraDelay), and runs the end-to-end
# smoke that proves ALL FIVE PREP-* requirements observably.
#
# After this plan, the full chain from CLI invocation through Phase 4
# prep into Phase 3's --smoke screenshot is live, and the smoke fixture
# enforces every requirement via post-screenshot inline assertions in
# the test runner (the Task 4 E2E node script). The roadmap's Phase 4
# success criteria #1, #2, #5 are observable hermetically via this
# fixture; SC #3 (lazy images load) is observable via the lazy-img
# element; SC #4 (extraDelay) is observable via console.time wall clock.

must_haves:
  truths:
    - "src/cli.js imports { installAnimationGuards, runPreparePipeline } from './prepare/index.js' — ONE import line covering both lifecycle halves"
    - "src/cli.js calls installAnimationGuards(context, config.prepare) BEFORE navigateToPage, inside the existing try block (between launchBrowser and navigateToPage — RESEARCH §Pattern 5 lines 555-563, 04-PATTERNS §cli.js Modification 2)"
    - "src/cli.js calls runPreparePipeline(navigatedPage, config.prepare) AFTER navigateToPage returns, capturing `{ hideSummary }` (intentionally unused in Phase 4 — Phase 6 will surface it)"
    - "The smoke screenshot call gains `animations: 'disabled'` (Playwright's WAAPI fast-forward — RESEARCH §Standard Stack, §Pattern 5 line 573, 04-PATTERNS §cli.js Modification 4). This is belt-and-braces for the few cases the CSS-injection animation guard misses (e.g. Web Animations API initiated via element.animate())."
    - "The smoke branch wraps runPreparePipeline in `console.time('prepare')` / `console.timeEnd('prepare')` — the SC #4 evidence point. The timing call lives in cli.js (the smoke branch ONLY), NEVER in src/prepare/* (RESEARCH §Anti-Patterns line 597-598, 04-PATTERNS §Risk 13, §cli.js Modification 5)"
    - "samples/serve-smoke.js grows the HTML constant with four new fixture elements: a `.anim-target` div with a `@keyframes spin` animation (PREP-01 evidence), an `.io-target` div + inline-script that uses IntersectionObserver to swap opacity 0→1 (PREP-02 evidence), a `.hidden-by-test` div (PREP-03 evidence — to be matched by smoke.yaml's hide:), and a `<img loading=lazy>` placed ~2000px down a tall spacer (PREP-04 evidence)"
    - "samples/smoke.yaml is renamed `name: phase-04-smoke`, sets `scrollPrime: true`, sets `extraDelay: 1000` (the PREP-05 timing evidence), and adds `hide: ['.hidden-by-test']`. The hermetic baseUrl/output/viewport/page block is otherwise unchanged from Phase 3."
    - "The lifecycle invariant from Phase 3 is preserved: `await context.close()` BEFORE `await browser.close()` in finally (cli.js:56-61 — RESEARCH §Pitfall 5, 04-PATTERNS §cli.js Lifecycle invariant)"
    - "The end-to-end verify proves ALL FIVE PREP-* requirements observably: (1) PREP-01 via post-prep getComputedStyle on .anim-target showing animation-duration: 0s; (2) PREP-02 via post-prep getComputedStyle on .io-target showing opacity: 1 (the inline script's IO fired via the shim, swapping 0→1); (3) PREP-03 via post-prep getComputedStyle on .hidden-by-test showing visibility: hidden; (4) PREP-04 via post-prep query on the lazy <img> showing img.complete && img.naturalWidth > 0; (5) PREP-05 via the console.time('prepare') wall clock being >=1000ms (the configured extraDelay floor)."
    - "Non-smoke runs of `capture` retain Phase 3's pass-through (prints JSON with `_resolvedOutput` AND `_navigated`). No new top-level CLI flags."
  artifacts:
    - path: "src/cli.js"
      provides: "Modified capture action: launchBrowser → installAnimationGuards → navigateToPage → runPreparePipeline → (smoke: page.screenshot({ animations: 'disabled' }) OR non-smoke: pass-through) → close. Adds one import line; adds two function call lines; adds animations: 'disabled' to the smoke screenshot; adds console.time/timeEnd around runPreparePipeline INSIDE the smoke branch only."
      contains: "installAnimationGuards|runPreparePipeline"
    - path: "samples/serve-smoke.js"
      provides: "Extended HTML constant with four PREP-evidence fixtures (.anim-target, .io-target, .hidden-by-test, <img loading=lazy>) plus the spacer to force a scrollable tall page. Same dep-free Node http server, same 127.0.0.1:7357 port, same SIGTERM/SIGINT/SIGHUP cleanup — extension only, no architecture change."
      contains: "anim-target|io-target|hidden-by-test|loading=\"lazy\"|loading='lazy'"
    - path: "samples/smoke.yaml"
      provides: "Updated phase-04-smoke fixture: scrollPrime: true, extraDelay: 1000, hide: ['.hidden-by-test']. Same baseUrl http://127.0.0.1:7357, same viewport 800×600, same DSR=2 — extension only."
      contains: "hide:|extraDelay: 1000|scrollPrime: true"
  key_links:
    - from: "src/cli.js"
      to: "src/prepare/index.js"
      via: "import { installAnimationGuards, runPreparePipeline } from './prepare/index.js'"
      pattern: "from\\s+['\"]\\./prepare/index\\.js['\"]"
    - from: "src/cli.js (pre-nav call)"
      to: "installAnimationGuards(context, config.prepare)"
      via: "between launchBrowser and navigateToPage"
      pattern: "installAnimationGuards\\s*\\(\\s*context\\s*,\\s*config\\.prepare"
    - from: "src/cli.js (post-nav call)"
      to: "runPreparePipeline(navigatedPage, config.prepare)"
      via: "between navigateToPage and the smoke branch"
      pattern: "runPreparePipeline\\s*\\("
    - from: "src/cli.js (smoke screenshot)"
      to: "page.screenshot({ ..., animations: 'disabled' })"
      via: "added option in the screenshot call"
      pattern: "animations:\\s*['\"]disabled['\"]"
    - from: "samples/serve-smoke.js HTML"
      to: "PREP-01/02/03/04 fixture elements"
      via: "literal HTML template additions"
      pattern: "anim-target|io-target|hidden-by-test|loading=[\"']lazy"
    - from: "samples/smoke.yaml"
      to: "prepare.hide + prepare.extraDelay"
      via: "YAML configuration"
      pattern: "hide:[\\s\\S]*hidden-by-test"
---

<objective>
Wire Phase 4 into the existing capture action and prove all five PREP-* requirements with one hermetic smoke run. This plan modifies THREE files:

1. **`src/cli.js`** — adds ONE import line from `./prepare/index.js`, calls `installAnimationGuards(context, config.prepare)` between launchBrowser and navigateToPage, calls `runPreparePipeline(navigatedPage, config.prepare)` after navigateToPage (capturing `{ hideSummary }` — intentionally unused in Phase 4), adds `animations: 'disabled'` to the smoke screenshot (Playwright's WAAPI fast-forward), and wraps `runPreparePipeline` in `console.time('prepare')` / `console.timeEnd('prepare')` INSIDE the smoke branch only (SC #4 timing evidence — library code stays silent, the timing instrument lives at the CLI integration site).

2. **`samples/serve-smoke.js`** — extends the HTML constant with four fixture elements engineered to exercise each PREP-* requirement: a `.anim-target` div with `@keyframes spin` (PREP-01 — `animation-duration: 0s` after prep), a `.io-target` div whose inline script uses IntersectionObserver to swap `opacity: 0 → 1` (PREP-02 — the shim makes IO fire so `opacity: 1` after prep), a `.hidden-by-test` div (PREP-03 — `visibility: hidden` after prep), and a `<img loading=lazy>` placed at offset ~2000px after a tall spacer (PREP-04 — `img.complete && img.naturalWidth > 0` after prep). The dep-free http server, port, and shutdown handlers from Phase 3 are unchanged.

3. **`samples/smoke.yaml`** — updates `name: phase-04-smoke`, adds `hide: ['.hidden-by-test']` (PREP-03 driver), sets `scrollPrime: true` (PREP-04 driver), sets `extraDelay: 1000` (PREP-05 driver — the SC #4 wall-clock floor).

Then runs the end-to-end smoke and verifies all five PREP-* requirements via post-prep inline assertions in a Node E2E script that opens the same page in the same lifecycle, runs prep, and reads computed styles before the context closes.

Purpose: This plan is the only one that touches `src/cli.js`, the only one that extends the smoke fixture, and the only one that validates the whole prep pipeline end-to-end. The wave-1 plans (04-01..04-04) each created an independent module with a static-source verify; the runtime proof — "do the four modules compose correctly when wired by the CLI against a real page?" — lives here. The smoke fixture extension is owned by exactly one plan (this one) per the quality gate; if multiple plans touched serve-smoke.js or smoke.yaml, the wave-1 parallelism would break.

The five PREP-* requirements all appear in this plan's `requirements:` frontmatter because the end-to-end verify exercises all five at once — the smoke run IS the multi-requirement integration test. Implementation traceability is in 04-01..04-04; integration traceability is here.

Output: `src/cli.js` (modified — 1 import, 2 call sites, 1 screenshot option, 2 console.time lines), `samples/serve-smoke.js` (modified — HTML constant grown with 4 fixtures + spacer), `samples/smoke.yaml` (modified — name + hide + scrollPrime + extraDelay), and end-to-end verification proving all 5 PREP-* requirements.

## Cross-Plan Decisions Re-Stated

- **No new CLI flag** — `--smoke` (added in Phase 3) is the verification seam. RESEARCH §Locked Decisions, 04-PATTERNS §Risk 4.
- **Lifecycle preserved** — Phase 3's try/finally with `context.close()` BEFORE `browser.close()` is untouched (RESEARCH §Pitfall 5, 04-PATTERNS §cli.js Lifecycle invariant).
- **Library posture preserved** — chalk/ora are NOT added to cli.js (Phase 6's job). The `console.time` is allowed as the documented exception in the CLI layer (RESEARCH §Code Examples, 04-PATTERNS §cli.js Modification 5).
- **No new dependencies**.
- **Smoke fixture lives at 127.0.0.1:7357** — port unchanged from Phase 3 (smoke.yaml references it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-prepare-pipeline/04-RESEARCH.md
@.planning/phases/04-prepare-pipeline/04-PATTERNS.md
@.planning/phases/04-prepare-pipeline/04-01-PLAN.md
@.planning/phases/04-prepare-pipeline/04-02-PLAN.md
@.planning/phases/04-prepare-pipeline/04-03-PLAN.md
@.planning/phases/04-prepare-pipeline/04-04-PLAN.md
@.planning/phases/03-browser-navigation/03-02-PLAN.md
@.planning/phases/03-browser-navigation/VERIFICATION.md
@src/cli.js
@src/browser/launcher.js
@src/browser/navigator.js
@src/config/schema.js
@samples/smoke.yaml
@samples/serve-smoke.js
@package.json

<interfaces>
<!-- Inherited interfaces this plan consumes:

     From src/prepare/index.js (04-04 — the barrel):
       export { installAnimationGuards } from './animations.js';
       export async function runPreparePipeline(
         page: import('playwright-chromium').Page,
         prepareConfig: { animations, hide, scrollPrime, extraDelay }
       ): Promise<{ hideSummary: { matched: number, missed: string[] } }>

     From src/browser/launcher.js (Phase 3):
       export async function launchBrowser(config):
         Promise<{ browser, context }>

     From src/browser/navigator.js (Phase 3):
       export async function navigateToPage(context, pageConfig): Promise<Page>

     CURRENT STATE of src/cli.js (Phase 3 final — to be modified):
       Lines 1-5 are the imports (Command, loadConfig, resolveTemplate,
       launchBrowser, navigateToPage). Line 18 is the --smoke option. Lines
       19-62 are the capture action body — currently launchBrowser → try →
       navigateToPage → opts.smoke branch (mkdir + screenshot + log) ELSE
       JSON pass-through → finally context.close + browser.close.

       This plan inserts:
       1. A new import line after line 5: `import { installAnimationGuards, runPreparePipeline } from './prepare/index.js';`
       2. A call to installAnimationGuards BEFORE navigateToPage (was line 29; new code goes between current lines 27 and 29).
       3. A call to runPreparePipeline AFTER navigateToPage (between current lines 29 and 30).
       4. The `animations: 'disabled'` option on the screenshot call (current line 39).
       5. `console.time('prepare')` BEFORE the runPreparePipeline call AND `console.timeEnd('prepare')` AFTER it — BUT ONLY inside the `if (opts.smoke)` branch.

     Smoke fixture current state (samples/serve-smoke.js HTML at lines 20-32):
       Single H1 "framershot phase 3 smoke" on a red background. NO @font-face
       (intentional — Phase 3's CAP-04 gate moved to live site). This plan adds
       fixture elements; it does NOT add @font-face rules.

     Smoke yaml current state (samples/smoke.yaml lines 25-39):
       name: phase-03-smoke
       baseUrl: http://127.0.0.1:7357
       output: /tmp/framershot-smoke/{page}.png
       deviceScaleFactor: 2
       viewport: 800×600 'smoke'
       page: / 'home'
       prepare: { animations: true, scrollPrime: false, extraDelay: 0 }
       // hide: key absent (defaults to [] via schema)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend samples/serve-smoke.js — add four PREP-evidence fixture elements + tall spacer</name>
  <files>samples/serve-smoke.js</files>
  <read_first>
    - /Users/sannagranqvist/Documents/App/screenshotter/samples/serve-smoke.js (current state — 49 lines; the HTML constant is at lines 20-32)
    - /Users/sannagranqvist/Documents/App/screenshotter/.planning/phases/04-prepare-pipeline/04-RESEARCH.md (§Validation Architecture — the four PREP-evidence elements; §Pattern 2 line 391 — IntersectionObserver-using inline script proves PREP-02)
    - /Users/sannagranqvist/Documents/App/screenshotter/.planning/phases/04-prepare-pipeline/04-PATTERNS.md (§serve-smoke.js section lines 232-263 — the four fixture additions and the existing conventions to preserve: port 7357, no deps, clean shutdown)
  </read_first>
  <action>
    Edit `/Users/sannagranqvist/Documents/App/screenshotter/samples/serve-smoke.js` — extend ONLY the `HTML` template literal constant (lines 20-32). Do NOT touch the port (7357), the `http.createServer` block, or the signal-handler block. Do NOT change to a different module pattern.

    The new `HTML` constant must contain (in this order, inside `<body>`):

    1. **`<h1>framershot phase 4 smoke</h1>`** — keeps a visible header so a human eyeballing the screenshot sees it's the Phase 4 fixture. Rename from "phase 3 smoke" to "phase 4 smoke".

    2. **PREP-01 fixture: `.anim-target` div with a CSS keyframe animation that would visibly play if unkilled.** This element MUST have a CSS `animation` property declared with a non-zero duration so post-prep `getComputedStyle(el).animationDuration === '0s'` proves the injected guards beat the inline declaration. Use:

       ```html
       <style>
         @keyframes spin {
           from { transform: rotate(0deg); }
           to { transform: rotate(360deg); }
         }
         .anim-target {
           width: 50px;
           height: 50px;
           background: #ff0;
           animation: spin 2s linear infinite;
         }
       </style>
       <div class="anim-target" data-test="anim-target"></div>
       ```

       Notes:
       - The `data-test="anim-target"` attribute is for the E2E verify's `document.querySelector('[data-test="anim-target"]')` selector.
       - 50×50 is small (doesn't visually dominate the smoke screenshot).
       - Inline-style declaration `animation: spin 2s linear infinite` is what the universal `*` `animation: none !important` rule must beat.

    3. **PREP-02 fixture: `.io-target` div + inline script using IntersectionObserver.** The element starts at `opacity: 0` (set inline). An inline `<script>` constructs a `new IntersectionObserver` and observes the element; when the callback fires with `isIntersecting: true`, the script sets `opacity: 1`. Post-prep, the E2E verify reads `getComputedStyle(el).opacity` — if the IO shim fired (PREP-02 works), opacity is `1`; if the shim is broken, opacity stays `0`. Use:

       ```html
       <div class="io-target" data-test="io-target" style="width: 40px; height: 40px; background: #0ff; opacity: 0;"></div>
       <script>
         (() => {
           const el = document.querySelector('[data-test="io-target"]');
           const io = new IntersectionObserver((entries) => {
             for (const e of entries) {
               if (e.isIntersecting) {
                 e.target.style.opacity = '1';
               }
             }
           });
           io.observe(el);
         })();
       </script>
       ```

       Notes:
       - Wrapped in an IIFE so the temporary `io` and `el` don't pollute window.
       - The IO is constructed AFTER the page's bundle has executed (we're inline-script-at-the-bottom). Since the IO shim is installed via `context.addInitScript` BEFORE any page script runs (04-01), this `new IntersectionObserver(...)` resolves to the shim — its `observe()` schedules a microtask, the microtask fires the callback with `isIntersecting: true`, and `opacity` flips to `1`. The E2E verify then reads computed opacity.
       - Setting `e.target.style.opacity = '1'` (a string, the CSSOM standard form) — NOT `1` (number; would coerce to '1' but the string form is the idiom).
       - The `for (const e of entries)` loop handles the case where the shim batches multiple targets if a later test extends to multi-element observation — defensive, costs nothing.

    4. **PREP-03 fixture: `.hidden-by-test` div** — a simple element that smoke.yaml's `hide:` array will match. Use:

       ```html
       <div class="hidden-by-test" data-test="hidden-by-test" style="padding: 1rem; background: #888; color: #fff;">
         this element SHOULD be hidden by Phase 4 prepare
       </div>
       ```

       Notes:
       - Has visible text and a background so if PREP-03 fails, the failure is obvious in a screenshot.
       - The E2E verify reads `getComputedStyle(el).visibility` — expected `'hidden'`.

    5. **PREP-04 fixture: tall spacer + `<img loading=lazy>` placed ~2000px down.** The spacer ensures the page is taller than the 600px viewport, so scrollPrime has work to do. The lazy image starts as a 1×1 transparent placeholder and would normally fetch its source only when scrolled into view — scroll prime should trigger that fetch. Use:

       ```html
       <div style="height: 2000px; background: linear-gradient(#c00, #00c);"></div>
       <img
         data-test="lazy-img"
         loading="lazy"
         src="data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='40'><rect width='100' height='40' fill='%2300ff00'/><text x='4' y='24' fill='black' font-size='14'>PREP-04</text></svg>"
         style="display: block; width: 100px; height: 40px; margin-top: 100px;"
       />
       ```

       Notes:
       - The 2000px linear-gradient spacer makes the page ~2700px tall total (h1 + .anim-target + .io-target + .hidden-by-test + spacer + img + footer margin), so a 600px viewport requires ~5 scroll steps. The exact step count doesn't matter for the verify (E2E reads `img.complete`); the requirement is "page tall enough that scrollPrime has steps to take."
       - `loading="lazy"` is the canonical attribute; Chromium implements native lazy loading via IntersectionObserver. Without scrollPrime, the image would still be at the placeholder phase when the screenshot fires. With scrollPrime, the inter-step waits give the lazy-load IO time to fire and the image's data URI to fetch (data URIs resolve synchronously, so once the IO fires, `img.complete === true` and `img.naturalWidth === 100` shortly after).
       - The data: URI is an inline SVG (no network dependency — hermetic). 100×40 pixels with green background.
       - The verify reads `document.querySelector('[data-test="lazy-img"]')` then asserts `img.complete && img.naturalWidth > 0`.

    6. **A final small `<footer>` element** for visual closure (optional, but a clean "end of fixture" marker):

       ```html
       <footer style="padding: 1rem; color: #aaa;">end of phase-04 fixture</footer>
       ```

    The complete updated HTML constant should be ONE template literal assigned to `const HTML = ...`. Preserve the existing `<!doctype html><html><head><meta charset>...` skeleton; the page-level reset (`html, body { margin: 0; ... font-family: system-ui; }` from Phase 3) stays. Combine the new `<style>` block (containing the keyframe + class rules) with whatever inline style declarations are needed; you may put all CSS into a single `<style>` element in `<head>`.

    **Anti-patterns the executor MUST avoid:**

      - Do NOT add `@font-face` rules — Phase 3 explicitly excluded these (the live-site CAP-04 gate covers fonts; the hermetic fixture must NOT exercise fonts.ready BLOCKING behavior — 04-PATTERNS §serve-smoke.js Existing conventions to honor).
      - Do NOT change the port (7357 is hardcoded in smoke.yaml — 04-PATTERNS §serve-smoke.js).
      - Do NOT add any dependencies (the file uses only `node:http`).
      - Do NOT touch the signal-handler block at the bottom (lines 44-48 in current state).
      - Do NOT introduce a route parameter or multiple endpoints — single `/` responds with the same HTML (RESEARCH §Validation Architecture: hermetic, one page).
      - Do NOT use `<script src="...">` external script tags — inline `<script>` only (the file ships no external assets).
      - Do NOT animate via `transition` for the .anim-target element — use `animation` keyword + keyframes. Why: PREP-01's universal selector kills both, but the keyframe-based animation is the canonical "scroll-driven appear effect" mental model that Framer Motion uses, so it's the right thing to prove the freeze against. (Transitions are equally killed by the guard; we just exercise the animation property because it's the more common shape.)
  </action>
  <verify>
    <automated>cd /Users/sannagranqvist/Documents/App/screenshotter && test -f samples/serve-smoke.js && grep -q "framershot phase 4 smoke" samples/serve-smoke.js && grep -q "anim-target" samples/serve-smoke.js && grep -q "@keyframes spin" samples/serve-smoke.js && grep -q "animation: spin" samples/serve-smoke.js && grep -q "io-target" samples/serve-smoke.js && grep -q "new IntersectionObserver" samples/serve-smoke.js && grep -q "isIntersecting" samples/serve-smoke.js && grep -q "hidden-by-test" samples/serve-smoke.js && grep -qE "loading=[\"']lazy[\"']" samples/serve-smoke.js && grep -q "data-test=" samples/serve-smoke.js && ! grep -q "@font-face" samples/serve-smoke.js && grep -q "127.0.0.1" samples/serve-smoke.js && grep -q "PORT = 7357" samples/serve-smoke.js && (timeout 5 node samples/serve-smoke.js & SERVER_PID=$!; sleep 1; curl -s -o /tmp/smoke-html.html http://127.0.0.1:7357/ && CURL_EXIT=0 || CURL_EXIT=1; kill $SERVER_PID 2>/dev/null; wait $SERVER_PID 2>/dev/null; test "$CURL_EXIT" = "0" && grep -q "anim-target" /tmp/smoke-html.html && grep -q "io-target" /tmp/smoke-html.html && grep -q "hidden-by-test" /tmp/smoke-html.html && grep -qE "loading=[\"']lazy[\"']" /tmp/smoke-html.html && rm -f /tmp/smoke-html.html) && echo fixture-ok</automated>
  </verify>
  <done>`samples/serve-smoke.js` continues to be a dep-free Node http server on 127.0.0.1:7357, but its `HTML` template literal now contains: the h1 renamed to "framershot phase 4 smoke"; a `.anim-target` div with `@keyframes spin` + `animation: spin 2s linear infinite` declaration; an `.io-target` div at `opacity: 0` plus an inline IIFE that uses `new IntersectionObserver` to swap opacity to '1' on intersection; a `.hidden-by-test` div with visible text; a 2000px-tall linear-gradient spacer; and an `<img loading=lazy>` with an inline SVG `data:` URI as source. Each fixture element has a `data-test="..."` attribute for E2E selection. No `@font-face` rules added (hermetic boundary preserved). Server-side test: spawn the server, curl `/`, the response body contains all four fixture markers; server cleans up via SIGTERM. The signal-handler block (lines 44-48) is unchanged.</done>
  <acceptance_criteria>
    1. File continues to exist at `/Users/sannagranqvist/Documents/App/screenshotter/samples/serve-smoke.js`.
    2. Page header text renamed — `grep -q "framershot phase 4 smoke" samples/serve-smoke.js` passes.
    3. PREP-01 fixture present — `grep -q "anim-target"` AND `grep -q "@keyframes spin"` AND `grep -q "animation: spin"` all pass.
    4. PREP-02 fixture present — `grep -q "io-target"` AND `grep -q "new IntersectionObserver"` AND `grep -q "isIntersecting"` all pass.
    5. PREP-03 fixture present — `grep -q "hidden-by-test"` passes.
    6. PREP-04 fixture present — `grep -qE "loading=[\"']lazy[\"']"` passes.
    7. All fixtures have data-test markers — `grep -q "data-test="` passes (count >= 4).
    8. NO @font-face rules — `! grep -q "@font-face"` passes (Phase 3 boundary preserved).
    9. Port + bind address unchanged — `grep -q "127.0.0.1"` AND `grep -q "PORT = 7357"` pass.
    10. Server starts, serves the new HTML, and shuts down cleanly: spawn `node samples/serve-smoke.js` in the background, sleep 1s, `curl -s http://127.0.0.1:7357/` returns the new HTML body containing all four fixture markers, then SIGTERM the server.
    11. Existing signal-handler block is preserved (the SIGTERM at the end of the verify exits the server cleanly).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update samples/smoke.yaml — drive PREP-03 (hide), PREP-04 (scrollPrime), PREP-05 (extraDelay)</name>
  <files>samples/smoke.yaml</files>
  <read_first>
    - /Users/sannagranqvist/Documents/App/screenshotter/samples/smoke.yaml (current state — 39 lines; the prepare block is at lines 36-39)
    - /Users/sannagranqvist/Documents/App/screenshotter/.planning/phases/04-prepare-pipeline/04-PATTERNS.md (§smoke.yaml section lines 265-300 — the four modifications and the existing conventions to preserve)
    - /Users/sannagranqvist/Documents/App/screenshotter/src/config/schema.js (lines 22-34 — the prepareSchema this yaml must conform to)
  </read_first>
  <action>
    Edit `/Users/sannagranqvist/Documents/App/screenshotter/samples/smoke.yaml`. Preserve the top-of-file comment block (Phase 3's hermetic-fixture rationale and the `node samples/serve-smoke.js &` instructions) AND preserve everything except the `name:` line and the `prepare:` block.

    Apply these four modifications:

    1. **Rename** `name: phase-03-smoke` → `name: phase-04-smoke`.

    2. **Add `hide:`** to the prepare block — a list containing `'.hidden-by-test'`:

       ```yaml
       prepare:
         animations: true
         hide:
           - '.hidden-by-test'
         scrollPrime: true
         extraDelay: 1000
       ```

    3. **Change `scrollPrime: false` → `scrollPrime: true`** so PREP-04 is exercised.

    4. **Change `extraDelay: 0` → `extraDelay: 1000`** so the smoke-branch `console.time('prepare')` reports a wall clock ≥1000ms — the SC #4 evidence point.

    5. **Add a 2-3 line addendum** at the top of the YAML's existing comment block explaining the Phase 4 additions. Append to (don't replace) the existing comment block — e.g.:

       ```
       # Phase 4 additions:
       #   - hide: ['.hidden-by-test'] — exercises PREP-03 against the .hidden-by-test
       #     element added to samples/serve-smoke.js
       #   - scrollPrime: true — exercises PREP-04 against the tall spacer + lazy <img>
       #   - extraDelay: 1000 — exercises PREP-05; the console.time('prepare') wall clock
       #     printed by --smoke runs is the SC #4 evidence
       ```

    Preserve verbatim:
    - The Phase 3 hermetic-rationale comment block at the top (the explanation of why baseUrl is http://127.0.0.1 not data:, and the `node samples/serve-smoke.js &` run instructions).
    - `baseUrl: http://127.0.0.1:7357` (do NOT change — matches the server in Task 1).
    - `output: /tmp/framershot-smoke/{page}.png` (outside the repo; no .gitignore work).
    - `deviceScaleFactor: 2`.
    - `viewport: { width: 800, height: 600, name: smoke }`.
    - `page: { path: "/", name: home }`.

    Do NOT introduce new schema keys (04-PATTERNS §Risk 5). The four keys under `prepare:` are exhaustively `animations`, `hide`, `scrollPrime`, `extraDelay` per schema.js:22-34.

    **Anti-patterns the executor MUST avoid:**

      - Do NOT change the port or baseUrl scheme. 04-PATTERNS §serve-smoke.js Existing conventions.
      - Do NOT add a `prepare.extraDelay: 100` "fast iteration" alternative — the 1000ms is the SC #4 evidence floor.
      - Do NOT add unrelated test fixtures (additional pages, viewports, hidden selectors that don't match anything). The yaml is the canonical smoke; one fixture page, one hide entry, one PREP-05 evidence value.
      - Do NOT set `animations: false` — the fixture exists to PROVE PREP-01/02 work; turning them off defeats the purpose.
  </action>
  <verify>
    <automated>cd /Users/sannagranqvist/Documents/App/screenshotter && test -f samples/smoke.yaml && grep -q '^name: phase-04-smoke' samples/smoke.yaml && grep -q "scrollPrime: true" samples/smoke.yaml && grep -q "extraDelay: 1000" samples/smoke.yaml && grep -q "hide:" samples/smoke.yaml && grep -q "hidden-by-test" samples/smoke.yaml && grep -q "animations: true" samples/smoke.yaml && grep -q "baseUrl: http://127.0.0.1:7357" samples/smoke.yaml && grep -q "deviceScaleFactor: 2" samples/smoke.yaml && grep -q "width: 800" samples/smoke.yaml && grep -q "height: 600" samples/smoke.yaml && grep -q 'path: "/"' samples/smoke.yaml && node --input-type=module -e "import('./src/config/load.js').then(async m => { const c = await m.loadConfig('samples/smoke.yaml'); if (c.name !== 'phase-04-smoke') process.exit(10); if (c.prepare.animations !== true) process.exit(11); if (!Array.isArray(c.prepare.hide) || c.prepare.hide.length !== 1 || c.prepare.hide[0] !== '.hidden-by-test') process.exit(12); if (c.prepare.scrollPrime !== true) process.exit(13); if (c.prepare.extraDelay !== 1000) process.exit(14); if (c.viewport.width !== 800 || c.viewport.height !== 600) process.exit(15); if (c.deviceScaleFactor !== 2) process.exit(16); if (c.baseUrl !== 'http://127.0.0.1:7357') process.exit(17); if (c.page.path !== '/') process.exit(18); console.log('ok'); }).catch(e => { console.error(e); process.exit(99); })"</automated>
  </verify>
  <done>`samples/smoke.yaml` continues to validate cleanly through Phase 2's `loadConfig`. Renamed to `phase-04-smoke`. `prepare.animations: true`, `prepare.hide: ['.hidden-by-test']`, `prepare.scrollPrime: true`, `prepare.extraDelay: 1000`. Everything else (baseUrl, output, deviceScaleFactor, viewport, page) is unchanged. Top-of-file comment block extended with a Phase 4 additions paragraph. No new schema keys introduced.</done>
  <acceptance_criteria>
    1. File continues to exist at `/Users/sannagranqvist/Documents/App/screenshotter/samples/smoke.yaml`.
    2. `name: phase-04-smoke` — grep-verified.
    3. `prepare.hide` contains `.hidden-by-test` — yaml parses with `c.prepare.hide === ['.hidden-by-test']`.
    4. `prepare.scrollPrime: true` — yaml parses with `c.prepare.scrollPrime === true`.
    5. `prepare.extraDelay: 1000` — yaml parses with `c.prepare.extraDelay === 1000`.
    6. `prepare.animations: true` — yaml parses with `c.prepare.animations === true`.
    7. Unchanged fields: `baseUrl: http://127.0.0.1:7357`, `output: /tmp/framershot-smoke/{page}.png`, `deviceScaleFactor: 2`, `viewport: 800×600 'smoke'`, `page: '/'·'home'`.
    8. File validates through Phase 2's loadConfig with no zod errors (the E2E node script in `<verify>` exits 0).
    9. No new schema keys (the yaml conforms exactly to schema.js:22-34).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Modify src/cli.js — wire Phase 4 (install guards, run pipeline, animations:disabled screenshot, console.time around prepare)</name>
  <files>src/cli.js</files>
  <read_first>
    - /Users/sannagranqvist/Documents/App/screenshotter/src/cli.js (current state — 66 lines; the imports are at lines 1-5, the --smoke option at line 18, the capture action at lines 19-62)
    - /Users/sannagranqvist/Documents/App/screenshotter/.planning/phases/04-prepare-pipeline/04-RESEARCH.md (§Pattern 5 lines 545-584 — canonical CLI diff; §Anti-Patterns line 597-598 — no instrumentation in library; §Don't Hand-Roll — animations:'disabled' is Playwright's WAAPI fast-forward)
    - /Users/sannagranqvist/Documents/App/screenshotter/.planning/phases/04-prepare-pipeline/04-PATTERNS.md (§cli.js section lines 187-228 — the five modifications, the lifecycle invariant; §Risk 13 — console.time location)
    - /Users/sannagranqvist/Documents/App/screenshotter/.planning/phases/03-browser-navigation/03-02-PLAN.md (the canonical lifecycle pattern this plan preserves)
    - /Users/sannagranqvist/Documents/App/screenshotter/.planning/phases/04-prepare-pipeline/04-04-PLAN.md (the runPreparePipeline signature this consumes)
  </read_first>
  <action>
    Edit `/Users/sannagranqvist/Documents/App/screenshotter/src/cli.js`. The modifications are surgical insertions/changes only — Phase 3's lifecycle ordering, the `--smoke` option, the non-smoke JSON pass-through, and the try/finally close-order MUST be preserved.

    **Modification 1 — Add ONE import after the existing `navigateToPage` import (after line 5):**

      ```
      import { installAnimationGuards, runPreparePipeline } from './prepare/index.js';
      ```

      Single import covers both lifecycle halves (the index.js barrel from 04-04). Place this AFTER the `./browser/navigator.js` import — keeps the import block grouped by subsystem (browser layer, then prepare layer, mirroring the order of execution).

    **Modification 2 — Insert the pre-navigation guards installation BEFORE the existing `navigateToPage` call:**

      The existing line 29 currently reads:
      ```
      const navigatedPage = await navigateToPage(context, config.page);
      ```

      Insert this line IMMEDIATELY BEFORE that (i.e., between the `try {` on line 28 and the `const navigatedPage` line):
      ```
      await installAnimationGuards(context, config.prepare);
      ```

      Critical: this must run BETWEEN `launchBrowser` and `navigateToPage` (RESEARCH §Pitfall 3, 04-PATTERNS §Risk 7). If placed after navigateToPage, the page's bundle has already captured the original IntersectionObserver into its module closure and the shim is invisible to Framer's code.

    **Modification 3 — Insert the post-navigation pipeline call AFTER the existing `navigateToPage` line and BEFORE the `if (opts.smoke)` branch:**

      Insert this line immediately after `const navigatedPage = await navigateToPage(...)`:
      ```
      const { hideSummary } = await runPreparePipeline(navigatedPage, config.prepare);
      // hideSummary is intentionally unused in Phase 4 — Phase 6 (CLI-03) will
      // surface hideSummary.missed as warnings if any hide: selector matched 0 elements.
      ```

      The variable `hideSummary` is captured but not consumed in Phase 4. The destructure makes the intent explicit; the comment documents the Phase 6 owner. (Linters may complain about an unused variable; the comment is the contract — Phase 4 does NOT add `// eslint-disable-next-line no-unused-vars` because this project has no lint config yet, and adding one would be Phase 6 territory.)

    **Modification 4 — Add `animations: 'disabled'` to the smoke-branch screenshot call.**

      The existing line 39 currently reads:
      ```
      await navigatedPage.screenshot({ path: resolvedOutput, fullPage: false });
      ```

      Update to:
      ```
      await navigatedPage.screenshot({ path: resolvedOutput, fullPage: false, animations: 'disabled' });
      ```

      Why: Playwright's `animations: 'disabled'` option fast-forwards CSS animations, CSS transitions, AND Web Animations API tweens AT THE SCREENSHOT MOMENT (verbatim from types.d.ts:12150-12153 per RESEARCH §Standard Stack table). This is BELT-AND-BRACES on top of the CSS-injection animation guard: the injected CSS handles the "before screenshot" steady state (and is observable by getComputedStyle for SC #5 — RESEARCH §Pattern 1 line 322), and Playwright's option handles any WAAPI animation that might have been kicked off via `element.animate()` (not coverable by CSS rules). RESEARCH §Pattern 5 line 573, 04-PATTERNS §cli.js Modification 4.

    **Modification 5 — Wrap the runPreparePipeline call in `console.time('prepare')` / `console.timeEnd('prepare')` INSIDE THE `if (opts.smoke)` BRANCH ONLY.**

      The console.time pair is the SC #4 timing evidence (per ROADMAP Phase 4 SC #4: "verifiable by adding a console.time around the prepare step"). It MUST live in the CLI, NOT in src/prepare/* (04-PATTERNS §Risk 13, RESEARCH §Anti-Patterns line 597-598).

      Restructure the smoke branch so it looks like:
      ```
      if (opts.smoke) {
        // ... existing mkdir + screenshot ...
        await navigatedPage.screenshot({ path: resolvedOutput, fullPage: false, animations: 'disabled' });
        console.log(`smoke screenshot written: ${resolvedOutput}`);
      } else {
        // ... existing JSON pass-through ...
      }
      ```

      Wait — the console.time should be around `runPreparePipeline`, but runPreparePipeline is OUTSIDE the smoke branch (it runs unconditionally — both smoke and non-smoke paths benefit from prepare). So restructure: move the `console.time('prepare') / console.timeEnd('prepare')` calls AROUND the `runPreparePipeline` call ONLY IF `opts.smoke` is true. The cleanest shape:

      ```
      const navigatedPage = await navigateToPage(context, config.page);
      if (opts.smoke) console.time('prepare');
      const { hideSummary } = await runPreparePipeline(navigatedPage, config.prepare);
      if (opts.smoke) console.timeEnd('prepare');
      ```

      This guarantees:
      - Production (non-smoke) runs do NOT pay the `console.time` cost — library code stays silent in the production path.
      - Smoke runs print `prepare: <ms>ms` to stderr, which is the SC #4 evidence (the wall clock includes the 1000ms extraDelay configured in samples/smoke.yaml, plus the scroll prime time, plus the hide time — typical total ~1300-1500ms for the fixture).
      - The runPreparePipeline call itself is invariant — both branches call it; only the instrumentation is gated.

      Note: re-state the comment about the unused hideSummary right after the destructure line — it's the same comment in both branches. (The hideSummary destructure is outside the if/else, so the comment only needs to appear once.)

    **Mod 6 (Lifecycle preservation — no change, but verify):**

      The try/finally at the bottom of the action body (current cli.js:56-61) MUST remain `await context.close(); await browser.close();` in that order. RESEARCH §Pitfall 5, 04-PATTERNS §cli.js Lifecycle invariant. Do NOT reorder these.

    **Resulting capture action shape** (paraphrased — DO NOT inline this as a code block in the executor's instructions; the executor should construct it from the modifications above):
      ```
      .action(async (configArg, opts) => {
        const config = await loadConfig(configArg);
        const date = new Date().toISOString().slice(0, 10);
        const viewport = config.viewport.name ?? 'default';
        const page = config.page.name;
        const resolvedOutput = resolveTemplate(config.output, { date, viewport, page });

        const { browser, context } = await launchBrowser(config);
        try {
          await installAnimationGuards(context, config.prepare);   // ★ NEW
          const navigatedPage = await navigateToPage(context, config.page);
          if (opts.smoke) console.time('prepare');                  // ★ NEW (smoke-only)
          const { hideSummary } = await runPreparePipeline(navigatedPage, config.prepare); // ★ NEW
          // hideSummary is intentionally unused in Phase 4 — Phase 6 (CLI-03)
          // will surface hideSummary.missed as warnings.
          if (opts.smoke) console.timeEnd('prepare');               // ★ NEW (smoke-only)
          if (opts.smoke) {
            const { mkdir } = await import('node:fs/promises');
            const { dirname } = await import('node:path');
            await mkdir(dirname(resolvedOutput), { recursive: true });
            await navigatedPage.screenshot({ path: resolvedOutput, fullPage: false, animations: 'disabled' }); // ★ animations added
            console.log(`smoke screenshot written: ${resolvedOutput}`);
          } else {
            console.log(JSON.stringify({ ...config, _resolvedOutput: resolvedOutput, _navigated: navigatedPage.url() }, null, 2));
          }
        } finally {
          await context.close();
          await browser.close();
        }
      });
      ```

    **Anti-patterns the executor MUST avoid:**

      - Do NOT add a new CLI flag (`--prepare`, `--no-prepare`, `--prepare-debug`) — RESEARCH §Locked Decisions, 04-PATTERNS §Risk 4. Users opt out by writing `prepare: { animations: false, scrollPrime: false, hide: [], extraDelay: 0 }` in YAML.
      - Do NOT remove the `_navigated` field from non-smoke JSON output — Phase 3 added it; Phase 4 preserves it.
      - Do NOT remove the `fullPage: false` explicit option — it's anti-regression armor against a future Phase 5 author copy-pasting and accidentally setting fullPage:true.
      - Do NOT remove the smoke screenshot's `mkdir(dirname(resolvedOutput), { recursive: true })` call — `/tmp/framershot-smoke/` may not exist on fresh systems.
      - Do NOT add chalk/ora here (Phase 6's job).
      - Do NOT add try/catch around `installAnimationGuards` or `runPreparePipeline` — failures bubble to the top-level handler in index.js (Phase 3's contract).
      - Do NOT reorder context.close + browser.close in finally (Phase 3 lifecycle invariant — 04-PATTERNS §cli.js Lifecycle invariant).
      - Do NOT move `console.time('prepare')` inside `runPreparePipeline` (04-PATTERNS §Risk 13 — library stays silent).
      - Do NOT pass `config.prepare` to anything except `installAnimationGuards` and `runPreparePipeline` — these two consume the whole sub-object; nothing else should touch it.
      - Do NOT add a debug-mode environment variable check (`if (process.env.FRAMERSHOT_DEBUG) ...`) — out of scope.
  </action>
  <verify>
    <automated>cd /Users/sannagranqvist/Documents/App/screenshotter && test -f src/cli.js && grep -qE "from\s+['\"]\\./prepare/index\\.js['\"]" src/cli.js && grep -qE "installAnimationGuards" src/cli.js && grep -qE "runPreparePipeline" src/cli.js && grep -qE "installAnimationGuards\s*\(\s*context\s*,\s*config\.prepare" src/cli.js && grep -qE "runPreparePipeline\s*\(\s*navigatedPage\s*,\s*config\.prepare" src/cli.js && grep -q "animations: 'disabled'" src/cli.js && grep -q "console.time('prepare')" src/cli.js && grep -q "console.timeEnd('prepare')" src/cli.js && grep -q "hideSummary" src/cli.js && grep -Pzo "(?s)launchBrowser[\s\S]*?try\s*\{[\s\S]*?installAnimationGuards[\s\S]*?navigateToPage[\s\S]*?runPreparePipeline[\s\S]*?finally[\s\S]*?context\.close\(\)[\s\S]*?browser\.close\(\)" src/cli.js > /dev/null && ! grep -q "fullPage: true" src/cli.js && grep -q "fullPage: false" src/cli.js && grep -q "_navigated" src/cli.js && grep -q "_resolvedOutput" src/cli.js && ! grep -qE "process\.env\.FRAMERSHOT" src/cli.js && (node index.js capture samples/smoke.yaml --help 2>&1 | grep -q -- '--smoke') && (node -e "import('./src/cli.js').then(m => { if (typeof m.buildProgram !== 'function') process.exit(1); console.log('buildProgram ok'); }).catch(e => { console.error(e); process.exit(99); })") && echo cli-wired-ok</automated>
  </verify>
  <done>`src/cli.js` imports `{ installAnimationGuards, runPreparePipeline }` from `./prepare/index.js` as one line; calls `installAnimationGuards(context, config.prepare)` between `launchBrowser` and `navigateToPage` inside the existing try block; calls `runPreparePipeline(navigatedPage, config.prepare)` after `navigateToPage` and captures `{ hideSummary }` (with a comment documenting that Phase 6 will use it); wraps the runPreparePipeline call in `console.time('prepare')` / `console.timeEnd('prepare')` ONLY inside the `if (opts.smoke)` branch (gated, not unconditional); adds `animations: 'disabled'` to the smoke-branch screenshot call. The non-smoke JSON pass-through still emits `_resolvedOutput` AND `_navigated`. The try/finally lifecycle (context.close BEFORE browser.close) is unchanged. NO new CLI flags. The full `grep -Pzo` ordering gate confirms the source contains, in order: launchBrowser → try { → installAnimationGuards → navigateToPage → runPreparePipeline → finally { → context.close → browser.close. `buildProgram` still imports cleanly. `--help` lists `--smoke`. NO `fullPage: true` anywhere; NO `process.env.FRAMERSHOT_*` debug knobs.</done>
  <acceptance_criteria>
    1. File continues to exist at `/Users/sannagranqvist/Documents/App/screenshotter/src/cli.js`.
    2. ONE new import line — `grep -qE "from\s+['\"]\\./prepare/index\\.js['\"]"` passes; both `installAnimationGuards` AND `runPreparePipeline` appear as imported names.
    3. `installAnimationGuards(context, config.prepare)` called between launchBrowser and navigateToPage — `grep -qE "installAnimationGuards\s*\(\s*context\s*,\s*config\.prepare"` passes; the full `grep -Pzo` ordering gate confirms it appears BEFORE navigateToPage in source.
    4. `runPreparePipeline(navigatedPage, config.prepare)` called after navigateToPage — `grep -qE "runPreparePipeline\s*\(\s*navigatedPage\s*,\s*config\.prepare"` passes; the ordering gate confirms it appears AFTER navigateToPage.
    5. Return is destructured to `hideSummary` — `grep -q "hideSummary"` passes.
    6. Smoke screenshot uses `animations: 'disabled'` — `grep -q "animations: 'disabled'"` passes.
    7. `console.time('prepare')` and `console.timeEnd('prepare')` both present — `grep -q "console.time('prepare')" && grep -q "console.timeEnd('prepare')"` pass. Both calls are inside an `if (opts.smoke)` gate.
    8. Phase 3 lifecycle preserved — full ordering gate `launchBrowser → try { → installAnimationGuards → navigateToPage → runPreparePipeline → finally { → context.close → browser.close` matches via `grep -Pzo "(?s)..."`.
    9. `fullPage: true` does NOT appear anywhere — anti-regression gate.
    10. Phase 3's non-smoke JSON pass-through preserved — both `_resolvedOutput` AND `_navigated` still appear in the source.
    11. NO new CLI options — `node index.js capture samples/smoke.yaml --help` lists `--smoke` and only `--smoke` (no additions).
    12. The module imports cleanly: `node -e "import('./src/cli.js')"` exits 0 and `buildProgram` is exported.
    13. NO debug env-var knobs — `! grep -qE "process\.env\.FRAMERSHOT"`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: End-to-end smoke verify — prove all 5 PREP-* requirements via one hermetic run</name>
  <files></files>
  <read_first>
    - /Users/sannagranqvist/Documents/App/screenshotter/src/prepare/index.js (the orchestrator — confirm the exports match what cli.js imports)
    - /Users/sannagranqvist/Documents/App/screenshotter/src/cli.js (the wired capture action — confirms the pipeline shape)
    - /Users/sannagranqvist/Documents/App/screenshotter/samples/smoke.yaml (the fixture config — confirms hide/scrollPrime/extraDelay)
    - /Users/sannagranqvist/Documents/App/screenshotter/samples/serve-smoke.js (the fixture server — confirms the four PREP-evidence elements)
    - /Users/sannagranqvist/Documents/App/screenshotter/.planning/phases/04-prepare-pipeline/04-RESEARCH.md (§Validation Architecture — the end-to-end approach)
  </read_first>
  <action>
    This task produces NO file modifications. It is the integration verification step that proves ALL FIVE PREP-* requirements via the hermetic smoke fixture. The work is to run the verify and document the observable outcomes.

    **The end-to-end verify orchestration:**

    1. Start the fixture server in the background:
       ```
       cd /Users/sannagranqvist/Documents/App/screenshotter && node samples/serve-smoke.js &
       SERVER_PID=$!
       sleep 1
       ```

    2. Clean previous smoke output and run the capture:
       ```
       rm -rf /tmp/framershot-smoke
       OUT=$(node index.js capture samples/smoke.yaml --smoke 2>&1)
       echo "$OUT"
       ```

       Expected stdout/stderr:
       - `prepare: NNNNms` (where NNNN >= 1000 — the SC #4 evidence; the wall clock includes the configured extraDelay floor + scroll prime + hide)
       - `smoke screenshot written: /tmp/framershot-smoke/home.png`

       Expected exit code: 0.

    3. Verify the PNG was written with the expected dimensions (Phase 3 hermetic geometry proof, preserved):
       ```
       test -f /tmp/framershot-smoke/home.png
       DIMS=$(node -e "import('fs/promises').then(async fs => { const buf = await fs.readFile('/tmp/framershot-smoke/home.png'); console.log(buf.readUInt32BE(16) + 'x' + buf.readUInt32BE(20)); })")
       test "$DIMS" = "1600x1200"   # viewport 800×600 × DSR 2
       ```

    4. **The five PREP-* assertions.** These are proven inline via a single Node E2E script that opens the SAME page in the SAME lifecycle (launchBrowser → installAnimationGuards → navigateToPage → runPreparePipeline) and then reads computed styles + image-load state via page.evaluate BEFORE closing the context. Use this script (run from the project root with the server still running):

       ```
       node --input-type=module -e "
         import { loadConfig } from './src/config/load.js';
         import { launchBrowser } from './src/browser/launcher.js';
         import { navigateToPage } from './src/browser/navigator.js';
         import { installAnimationGuards, runPreparePipeline } from './src/prepare/index.js';
         const config = await loadConfig('samples/smoke.yaml');
         const { browser, context } = await launchBrowser(config);
         try {
           await installAnimationGuards(context, config.prepare);
           const page = await navigateToPage(context, config.page);
           const t0 = Date.now();
           const { hideSummary } = await runPreparePipeline(page, config.prepare);
           const elapsed = Date.now() - t0;

           const evidence = await page.evaluate(() => {
             const anim = document.querySelector('[data-test=\"anim-target\"]');
             const io = document.querySelector('[data-test=\"io-target\"]');
             const hidden = document.querySelector('[data-test=\"hidden-by-test\"]');
             const lazy = document.querySelector('[data-test=\"lazy-img\"]');
             return {
               // PREP-01: animation-duration should be 0s (universal !important rule)
               animDuration: anim ? getComputedStyle(anim).animationDuration : 'NO-ELEMENT',
               // PREP-02: opacity should be 1 (the IO shim fired, inline script set it)
               ioOpacity: io ? getComputedStyle(io).opacity : 'NO-ELEMENT',
               // PREP-03: visibility should be 'hidden' (the hide: array matched)
               hiddenVisibility: hidden ? getComputedStyle(hidden).visibility : 'NO-ELEMENT',
               // PREP-04: lazy image should have loaded after scroll prime
               lazyComplete: lazy ? (lazy.complete && lazy.naturalWidth > 0) : false,
               lazyNaturalWidth: lazy ? lazy.naturalWidth : -1,
               // Also confirm the animation-guards style element is in the DOM
               hasGuards: !!document.querySelector('style[data-framershot=\"animation-guards\"]'),
             };
           });

           // Assertions:
           if (evidence.animDuration !== '0s') {
             console.error('FAIL PREP-01: animationDuration is', evidence.animDuration, '(expected 0s)');
             process.exit(101);
           }
           if (evidence.ioOpacity !== '1') {
             console.error('FAIL PREP-02: ioOpacity is', evidence.ioOpacity, '(expected 1)');
             process.exit(102);
           }
           if (evidence.hiddenVisibility !== 'hidden') {
             console.error('FAIL PREP-03: hiddenVisibility is', evidence.hiddenVisibility, '(expected hidden)');
             process.exit(103);
           }
           if (!evidence.lazyComplete) {
             console.error('FAIL PREP-04: lazy img not complete after scroll prime. naturalWidth =', evidence.lazyNaturalWidth);
             process.exit(104);
           }
           if (elapsed < 1000) {
             console.error('FAIL PREP-05: prepare took', elapsed, 'ms (expected >=1000ms from extraDelay)');
             process.exit(105);
           }
           if (!evidence.hasGuards) {
             console.error('FAIL: animation-guards style element not present in DOM');
             process.exit(110);
           }
           if (hideSummary.matched !== 1) {
             console.error('FAIL: hideSummary.matched =', hideSummary.matched, '(expected 1)');
             process.exit(111);
           }

           console.log('PREP-01..05 all pass. anim:' + evidence.animDuration + ' io:' + evidence.ioOpacity + ' hide:' + evidence.hiddenVisibility + ' lazy:' + evidence.lazyComplete + '(' + evidence.lazyNaturalWidth + ') extraDelay+:' + elapsed + 'ms hide.matched:' + hideSummary.matched);
         } finally {
           await context.close();
           await browser.close();
         }
       "
       ```

    5. Stop the fixture server:
       ```
       kill $SERVER_PID 2>/dev/null
       wait $SERVER_PID 2>/dev/null
       ```

    **What each assertion proves:**

    - **PREP-01** (CSS animation freeze): `.anim-target` is declared with `animation: spin 2s linear infinite` inline; after prep, `getComputedStyle(elem).animationDuration === '0s'` proves the universal `* { animation-duration: 0s !important }` rule beat the inline declaration. The animation-name is also forced to `none` by `* { animation: none !important }`.
    - **PREP-02** (Framer Motion in-view trigger via IO shim): `.io-target` starts at `opacity: 0` (inline style). The inline script constructs a `new IntersectionObserver` (which resolves to our shim because `addInitScript` ran before any page script), calls `observe(el)`, the shim's microtask fires with `isIntersecting: true`, the script sets `style.opacity = '1'`. After prep, `getComputedStyle(elem).opacity === '1'` proves the shim fired.
    - **PREP-03** (selector-based hide): `.hidden-by-test` is in the YAML's `prepare.hide` array. hideSelectors matched it; `setProperty('visibility', 'hidden', 'important')` was applied. After prep, `getComputedStyle(elem).visibility === 'hidden'`. Additionally, `hideSummary.matched === 1` proves the matched count is correct.
    - **PREP-04** (scroll prime triggers lazy loads): The lazy `<img>` is placed ~2000px down a 2000px spacer; on initial page load, it's well below the 600px viewport and Chromium's native lazy-loading withholds the source. scrollPrime walks the page in 600px steps with 200ms inter-step waits — when the step crosses the image's position, the browser's lazy-load IO fires and starts the data: URI fetch (synchronous for data: URIs). After prep, `img.complete === true && img.naturalWidth === 100` proves the load completed.
    - **PREP-05** (extraDelay honored): The YAML configures `extraDelay: 1000`. The wall clock from `runPreparePipeline` start to its return must be ≥1000ms — the SC #4 evidence point. The `console.time('prepare')` in cli.js also produces a stderr line `prepare: NNNNms` independently, which the Step 2 stdout capture observes.

    **What if a PREP-* fails:**
    - PREP-01 fail (`animationDuration !== '0s'`): the CSS-injection guard didn't install OR didn't beat the inline declaration. Check 04-01 CSS rule's specificity and !important flags.
    - PREP-02 fail (`ioOpacity !== '1'`): the IO shim either didn't install OR didn't fire. Check 04-01's addInitScript registration AND the shim's microtask scheduling.
    - PREP-03 fail (`hiddenVisibility !== 'hidden'`): hideSelectors didn't match the .hidden-by-test class. Check 04-02's selector matching OR the smoke.yaml `hide:` value.
    - PREP-04 fail (`!lazyComplete`): scrollPrime didn't reach the image's offset OR the wait between steps was too short to let the lazy-load IO fire. Check 04-03's INTER_STEP_WAIT_MS and the step-count math against the fixture's geometry.
    - PREP-05 fail (`elapsed < 1000`): extraDelay short-circuited unexpectedly OR the YAML's extraDelay value is wrong. Check 04-03 extraDelay's `ms <= 0` gate AND the YAML's value parses to 1000.

    All assertions must pass in ONE run. Partial-pass is not acceptable — the gap closure protocol would apply.
  </action>
  <verify>
    <automated>cd /Users/sannagranqvist/Documents/App/screenshotter && (node samples/serve-smoke.js & SERVER_PID=$!; sleep 1; rm -rf /tmp/framershot-smoke; OUT=$(node index.js capture samples/smoke.yaml --smoke 2>&1); EXIT=$?; echo "=== capture output ==="; echo "$OUT"; echo "=== end capture output ==="; if [ "$EXIT" != "0" ]; then kill $SERVER_PID 2>/dev/null; wait $SERVER_PID 2>/dev/null; echo "capture exited $EXIT"; exit $EXIT; fi; echo "$OUT" | grep -q "smoke screenshot written" || { kill $SERVER_PID 2>/dev/null; echo "no screenshot-written log"; exit 50; }; echo "$OUT" | grep -qE "prepare: [0-9]+(\.[0-9]+)?ms" || { kill $SERVER_PID 2>/dev/null; echo "no console.time prepare line"; exit 51; }; test -f /tmp/framershot-smoke/home.png || { kill $SERVER_PID 2>/dev/null; echo "no PNG"; exit 52; }; DIMS=$(node -e "import('fs/promises').then(async fs=>{const b=await fs.readFile('/tmp/framershot-smoke/home.png');console.log(b.readUInt32BE(16)+'x'+b.readUInt32BE(20));})"); test "$DIMS" = "1600x1200" || { kill $SERVER_PID 2>/dev/null; echo "wrong PNG dims: $DIMS"; exit 53; }; node --input-type=module -e "import { loadConfig } from './src/config/load.js'; import { launchBrowser } from './src/browser/launcher.js'; import { navigateToPage } from './src/browser/navigator.js'; import { installAnimationGuards, runPreparePipeline } from './src/prepare/index.js'; const config = await loadConfig('samples/smoke.yaml'); const { browser, context } = await launchBrowser(config); try { await installAnimationGuards(context, config.prepare); const page = await navigateToPage(context, config.page); const t0 = Date.now(); const { hideSummary } = await runPreparePipeline(page, config.prepare); const elapsed = Date.now() - t0; const e = await page.evaluate(() => { const a = document.querySelector('[data-test=\"anim-target\"]'); const i = document.querySelector('[data-test=\"io-target\"]'); const h = document.querySelector('[data-test=\"hidden-by-test\"]'); const l = document.querySelector('[data-test=\"lazy-img\"]'); return { animDur: a ? getComputedStyle(a).animationDuration : 'X', ioOp: i ? getComputedStyle(i).opacity : 'X', hideVis: h ? getComputedStyle(h).visibility : 'X', lazyOk: l ? (l.complete && l.naturalWidth > 0) : false, lazyNW: l ? l.naturalWidth : -1, guards: !!document.querySelector('style[data-framershot=\"animation-guards\"]') }; }); if (e.animDur !== '0s') { console.error('FAIL PREP-01 animDur=' + e.animDur); process.exit(101); } if (e.ioOp !== '1') { console.error('FAIL PREP-02 ioOp=' + e.ioOp); process.exit(102); } if (e.hideVis !== 'hidden') { console.error('FAIL PREP-03 hideVis=' + e.hideVis); process.exit(103); } if (!e.lazyOk) { console.error('FAIL PREP-04 lazyOk=' + e.lazyOk + ' nw=' + e.lazyNW); process.exit(104); } if (elapsed < 1000) { console.error('FAIL PREP-05 elapsed=' + elapsed); process.exit(105); } if (!e.guards) { console.error('FAIL no animation-guards style'); process.exit(110); } if (hideSummary.matched !== 1) { console.error('FAIL hideSummary.matched=' + hideSummary.matched); process.exit(111); } console.log('PREP-01..05 PASS anim:' + e.animDur + ' io:' + e.ioOp + ' hide:' + e.hideVis + ' lazy:' + e.lazyOk + '(' + e.lazyNW + ') elapsed:' + elapsed + 'ms hideMatched:' + hideSummary.matched); } finally { await context.close(); await browser.close(); }"; PASS=$?; kill $SERVER_PID 2>/dev/null; wait $SERVER_PID 2>/dev/null; exit $PASS)</automated>
  </verify>
  <done>The fixture server starts on 127.0.0.1:7357; `framershot capture samples/smoke.yaml --smoke` exits 0; stdout contains both `prepare: NNNNms` (with NNNN >= 1000) AND `smoke screenshot written: /tmp/framershot-smoke/home.png`; the PNG exists at exactly 1600×1200 (viewport 800×600 × DSR 2 — preserves Phase 3's geometry proof); the inline Node E2E script opens the same fixture page through the same lifecycle, runs prep, and reads via page.evaluate: PREP-01 — `.anim-target` reports `animationDuration === '0s'` (the universal CSS rule beat the inline declaration); PREP-02 — `.io-target` reports `opacity === '1'` (the IO shim fired and the inline script swapped opacity); PREP-03 — `.hidden-by-test` reports `visibility === 'hidden'` (hideSelectors matched and applied inline-important); PREP-04 — the lazy `<img>` reports `complete && naturalWidth === 100` (scrollPrime walked past the image position, the inter-step wait gave the lazy-load IO time to fire, and the data: URI fetch completed); PREP-05 — runPreparePipeline wall-clock elapsed >=1000ms (the configured extraDelay floor); also confirmed: `style[data-framershot="animation-guards"]` element is in the DOM, `hideSummary.matched === 1`. Server cleans up on SIGTERM.</done>
  <acceptance_criteria>
    1. Capture command exits 0 with the fixture server running and prepare wired.
    2. Capture stdout contains `smoke screenshot written: /tmp/framershot-smoke/home.png`.
    3. Capture stderr (or stdout, depending on Node's mapping) contains `prepare: NNNNms` where NNNN is >= 1000 (proves console.time was around runPreparePipeline AND extraDelay floor was honored).
    4. PNG written at `/tmp/framershot-smoke/home.png` with IHDR dimensions exactly `1600x1200` (preserves Phase 3 hermetic geometry).
    5. PREP-01 assertion passes: `getComputedStyle(.anim-target).animationDuration === '0s'` post-prep.
    6. PREP-02 assertion passes: `getComputedStyle(.io-target).opacity === '1'` post-prep.
    7. PREP-03 assertion passes: `getComputedStyle(.hidden-by-test).visibility === 'hidden'` post-prep.
    8. PREP-04 assertion passes: lazy `<img>.complete === true && naturalWidth > 0` post-prep.
    9. PREP-05 assertion passes: runPreparePipeline wall-clock elapsed >=1000ms (from inside the E2E node script that measures it independently of the cli.js console.time).
    10. Additional integration assertion: `style[data-framershot="animation-guards"]` element exists in DOM (proves 04-01's CSS injection ran).
    11. Additional integration assertion: `hideSummary.matched === 1` (proves 04-04's orchestrator correctly captured the return from 04-02's hideSelectors).
    12. Fixture server is cleanly killed at the end of the verify (no orphan processes left bound to port 7357).
    13. ALL FIVE PREP-* requirements pass in ONE run. Partial-pass is not acceptable.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.prepare` (validated by Phase 2) → CLI insertion points | Schema-enforced shape; both new call sites read this directly. |
| `installAnimationGuards(context, ...)` → BrowserContext | Same trust as launchBrowser's context creation. Init scripts register before any page exists. |
| `runPreparePipeline(navigatedPage, ...)` → loaded page | Same trust as navigateToPage's page return — already past networkidle + fonts.ready. |
| `animations: 'disabled'` screenshot option → Playwright internal | Documented option in types.d.ts:12153 — Playwright fast-forwards WAAPI at screenshot time. No new attack surface. |
| `console.time('prepare')` → stderr | The wall-clock value goes to stderr; bounded, contains no user data. |
| Fixture server (127.0.0.1:7357) → outside network | Loopback only; no traffic leaves the machine. Phase 3 lifecycle invariant preserved. |
| HTML fixture's inline IntersectionObserver script → page context | Self-contained — observes only one element it just selected; no exfiltration vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-SC | Tampering | npm supply chain | mitigate | Zero new dependencies in this plan. Same playwright-chromium@1.60.0 consumed via the same import chain. |
| T-04-05-LK | Resource Exhaustion | Fixture server left running after test | mitigate | The verify script captures `SERVER_PID=$!` and runs `kill $SERVER_PID` in cleanup. The server's existing SIGTERM handler exits cleanly. If verify is killed mid-run, the server is orphaned — operator manually kills via `lsof -i :7357`. Documented as a known limitation. |
| T-04-05-RC | Resource Exhaustion | Browser process leak on prepare failure | mitigate | The try/finally in cli.js owns context.close + browser.close in that order; preserved unchanged from Phase 3. Both new function calls (installAnimationGuards, runPreparePipeline) are INSIDE the try block, so a throw from either still triggers cleanup. RESEARCH §Pitfall 5. |
| T-04-05-XL | Information Disclosure | hideSummary leaks via stderr | accept | The `hideSummary` variable is captured but NEVER logged in Phase 4 (the comment in cli.js documents it explicitly). Phase 6 will decide presentation. |
| T-04-05-TR | Tampering | console.time mis-instrumentation | mitigate | Both `console.time('prepare')` and `console.timeEnd('prepare')` are inside the SAME `if (opts.smoke)` gate. Mismatched timer labels would produce silent no-ops (Node's console.time is lenient on missing pairs) — the grep verify confirms both calls are present with matching labels. |
| T-04-05-FF | Spoofing | Fixture HTML modified to fake PREP-* | accept | The fixture is in the same repo; a malicious modification would also have to fool the verify script's selectors and post-prep assertions. Personal tool; the verify is the documented gate. |
| T-04-05-PF | Tampering | animations: 'disabled' Playwright bug | accept | Playwright's `animations: 'disabled'` option is documented and stable across 1.x. If a bug appears, the CSS-injection guard (04-01) still provides the steady-state freeze; the WAAPI fast-forward is belt-and-braces. RESEARCH §Standard Stack. |
| T-04-05-PT-FUT | Tampering | Output path template traversal | accept | Flagged in 03-02's threat model; Phase 5 will harden. Phase 4 inherits the same `/tmp/framershot-smoke/` prefix outside the repo. |
</threat_model>

<verification>
After all four tasks complete, run these checks from the project root (Task 4's verify IS the consolidated check; tasks 1-3 each have their own static verifies):

1. **All four files modified:**
   ```
   cd /Users/sannagranqvist/Documents/App/screenshotter && test -f src/cli.js && test -f samples/serve-smoke.js && test -f samples/smoke.yaml && grep -q "phase-04-smoke" samples/smoke.yaml && grep -q "framershot phase 4 smoke" samples/serve-smoke.js && grep -q "installAnimationGuards" src/cli.js && grep -q "runPreparePipeline" src/cli.js && echo all-files-modified
   ```
   prints `all-files-modified`.

2. **Phase 3 lifecycle invariant preserved:**
   ```
   cd /Users/sannagranqvist/Documents/App/screenshotter && grep -Pzo "(?s)context\.close\(\)[\s\S]*?browser\.close\(\)" src/cli.js > /dev/null && echo lifecycle-ok
   ```
   prints `lifecycle-ok`.

3. **Full integration ordering:**
   ```
   cd /Users/sannagranqvist/Documents/App/screenshotter && grep -Pzo "(?s)launchBrowser[\s\S]*?try\s*\{[\s\S]*?installAnimationGuards[\s\S]*?navigateToPage[\s\S]*?runPreparePipeline[\s\S]*?finally[\s\S]*?context\.close[\s\S]*?browser\.close" src/cli.js > /dev/null && echo full-order-ok
   ```
   prints `full-order-ok`.

4. **End-to-end PREP-01..05 (Task 4's verify):** spawn the fixture server, run capture --smoke against samples/smoke.yaml, verify PNG dimensions = 1600×1200, then run the inline Node E2E script that asserts all five PREP-* requirements. The script exits 0 only when all five assertions pass plus the two integration assertions (animation-guards style present, hideSummary.matched === 1). Server cleaned up via SIGTERM.

5. **No regressions to Phase 3:**
   ```
   cd /Users/sannagranqvist/Documents/App/screenshotter && node samples/serve-smoke.js & SERVER_PID=$!; sleep 1; OUT=$(node index.js capture samples/smoke.yaml 2>&1); kill $SERVER_PID 2>/dev/null; wait $SERVER_PID 2>/dev/null; echo "$OUT" | grep -q '_resolvedOutput' && echo "$OUT" | grep -q '_navigated' && echo non-smoke-preserved
   ```
   prints `non-smoke-preserved`.

6. **No new CLI flags:**
   ```
   cd /Users/sannagranqvist/Documents/App/screenshotter && (node index.js capture --help 2>&1 | grep -E '^\s+--' | wc -l) | xargs -I{} test {} -le 2 && echo no-new-flags-ok
   ```
   prints `no-new-flags-ok` (the count is ≤2: --smoke + --help; commander's auto-help is the second).

7. **No new dependencies:**
   ```
   cd /Users/sannagranqvist/Documents/App/screenshotter && node -e "const pkg=require('./package.json'); const d=Object.keys(pkg.dependencies||{}).length + Object.keys(pkg.devDependencies||{}).length; console.log(d)"
   ```
   This count should match what Phase 3 left (the executor records this count in the SUMMARY).
</verification>

<success_criteria>
The plan is complete when:

1. `src/cli.js` is modified with the five specified changes (import line, pre-nav call, post-nav call + hideSummary capture, animations:'disabled' screenshot option, gated console.time/timeEnd around runPreparePipeline). All other lines are preserved verbatim (lifecycle, --smoke option, non-smoke pass-through with `_resolvedOutput` + `_navigated`).
2. `samples/serve-smoke.js` is extended with the four PREP-evidence fixture elements (.anim-target with @keyframes spin, .io-target with inline IntersectionObserver script, .hidden-by-test, lazy <img> on a 2000px spacer) — all with `data-test="..."` attributes. The dep-free Node http server, port 7357, signal handlers, and rendering of HTML are otherwise unchanged. NO @font-face rules.
3. `samples/smoke.yaml` is updated: `name: phase-04-smoke`, `prepare.hide: ['.hidden-by-test']`, `prepare.scrollPrime: true`, `prepare.extraDelay: 1000`. All other fields unchanged. Validates through Phase 2's `loadConfig`.
4. Phase 3's lifecycle invariant preserved: `context.close() → browser.close()` in finally; `launchBrowser` outside try; `installAnimationGuards → navigateToPage → runPreparePipeline` inside try in that order.
5. Phase 3's hermetic geometry proof preserved: `framershot capture samples/smoke.yaml --smoke` writes a PNG at exactly 1600×1200.
6. END-TO-END PREP-01 evidence: `getComputedStyle(.anim-target).animationDuration === '0s'` after prep.
7. END-TO-END PREP-02 evidence: `getComputedStyle(.io-target).opacity === '1'` after prep.
8. END-TO-END PREP-03 evidence: `getComputedStyle(.hidden-by-test).visibility === 'hidden'` after prep.
9. END-TO-END PREP-04 evidence: lazy `<img>.complete && naturalWidth > 0` after prep.
10. END-TO-END PREP-05 evidence: `console.time('prepare')` wall clock >= 1000ms AND the standalone E2E script's measured elapsed >= 1000ms.
11. Integration evidence: `style[data-framershot="animation-guards"]` element present in DOM (proves 04-01's CSS injection ran).
12. Integration evidence: `hideSummary.matched === 1` (proves 04-04's orchestrator correctly passes the 04-02 return through).
13. Non-smoke runs still emit JSON with `_resolvedOutput` AND `_navigated` (Phase 3 contract preserved).
14. No new CLI flags added (RESEARCH §Locked Decisions, 04-PATTERNS §Risk 4).
15. No new dependencies in `package.json`.
16. The fixture server cleans up via SIGTERM at the end of verify (no orphan processes bound to 127.0.0.1:7357).
</success_criteria>

<output>
Create `.planning/phases/04-prepare-pipeline/04-05-SUMMARY.md` when done, documenting:
- The diff to `src/cli.js`: the new import line, the two new call sites (with exact line context — before/after which Phase 3 line), the new screenshot option, the two new console.time lines.
- The diff to `samples/serve-smoke.js`: the four new fixture elements (with data-test attribute names quoted verbatim) and the 2000px spacer. Confirmation that NO @font-face was added.
- The diff to `samples/smoke.yaml`: the four changed values (`name`, `hide`, `scrollPrime`, `extraDelay`).
- The verbatim stdout of the smoke run (showing `prepare: NNNNms` and `smoke screenshot written: ...`).
- The verbatim stdout of the inline Node E2E script (the `PREP-01..05 PASS anim:0s io:1 hide:hidden lazy:true(100) elapsed:NNNNms hideMatched:1` line).
- Confirmation that the PNG dimensions are exactly 1600×1200.
- Confirmation that all 5 PREP-* requirements PASS observably in a single run. List the requirement IDs and the assertion result for each.
- Confirmation that the Phase 3 lifecycle invariant (context.close BEFORE browser.close in finally) is preserved.
- Confirmation that the non-smoke pass-through (JSON with `_resolvedOutput` + `_navigated`) is preserved.
- Confirmation that NO new CLI flags were added.
- Confirmation that NO new dependencies were added to package.json (record the before/after count from `verification` step 7).
- Note that Phase 4 is complete and Phase 5 (scroll-stitch output) can now build on the prepared Page that `runPreparePipeline` leaves behind.
- Any deviations from RESEARCH §Pattern 4/5 or 04-PATTERNS, with rationale (auto-fix style — mirror 03-02-SUMMARY.md's §Deviations section if any deviations occur during execution).
</output>
</content>
</invoke>