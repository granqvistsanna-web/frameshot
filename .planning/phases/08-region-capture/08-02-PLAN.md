---
phase: 08-region-capture
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/capture/region.js
  - src/output/template.js
autonomous: true
requirements: [REGION-01, REGION-02]
requirements_addressed: [REGION-01, REGION-02]

must_haves:
  truths:
    - "A new module `src/capture/region.js` exports `captureRegion(page, regionConfig, outputPath, { onProgress? })` — presentation-free, no console.*, no chalk/ora, no process.exit"
    - "`src/capture/region.js` exports `RegionError extends Error` with `.name === 'RegionError'`, constructor signature `(message, { cause } = {})` mirroring `BrowserError`"
    - "Selector-mode `captureRegion`: count-checks the selector → throws RegionError on 0 matches → scrolls .first() into view → reads boundingBox → throws RegionError on null box → applies padding → clamps to document → calls `page.screenshot({ path, clip, scale: 'device', animations: 'disabled', type: 'png' })`"
    - "Anchor-mode `captureRegion`: count-checks both `from` and `to` selectors (separate RegionError for each on mismatch) → scrolls `from` then measures → scrolls `to` then measures (order: scroll → measure → scroll → measure per RESEARCH Pitfall 3) → computes union rect → applies padding → clamps → re-scrolls to `Math.max(0, clip.y)` → rAF roundtrip → screenshot"
    - "`page.screenshot` invocation matches Phase 5 `frames.js:124-129` shape verbatim modulo `path` + `clip` values: always `scale: 'device'`, `animations: 'disabled'`, `type: 'png'`; NEVER `fullPage: true`; NEVER `omitBackground`"
    - "`mkdir(dirname(outputPath), { recursive: true })` runs BEFORE the screenshot call (not `mkdir(outputPath)` — that creates a directory named foo.png and fails with EISDIR per 08-PATTERNS §Shared Patterns)"
    - "Pure helpers `padRect(box, padding)`, `unionRect(a, b)`, and `clampToDocument(page, rect)` are colocated in the same file; `clampToDocument` reads `scrollWidth`/`scrollHeight` in ONE `page.evaluate` round-trip (geometry-once invariant from 08-PATTERNS)"
    - "`resolveTemplate(template, { date, viewport, page, region })` in `src/output/template.js` substitutes `{region}` with `slugify(region)` when `region` is truthy and leaves `{region}` literal when `region` is undefined (matches existing unknown-placeholder posture at template.js:6-7)"
    - "Existing `resolveTemplate` callers that pass `{ date, viewport, page }` without `region` continue to work — `{region}` placeholder (if present) remains literal in their output (back-compat)"
    - "The new module has ZERO imports from `src/cli/` (presentation-free library posture)"
  artifacts:
    - path: "src/capture/region.js"
      provides: "captureRegion + RegionError + padRect + unionRect + clampToDocument; new capture module sibling of src/capture/frames.js"
      contains: "captureRegion"
    - path: "src/output/template.js"
      provides: "resolveTemplate extended with {region} placeholder (literal when region arg undefined)"
      contains: "region"
  key_links:
    - from: "src/capture/region.js (page.screenshot call)"
      to: "Playwright's PageScreenshotOptions.clip (CSS pixels per types.d.ts:24276-24296)"
      via: "page.screenshot({ path: outputPath, clip, scale: 'device', animations: 'disabled', type: 'png' })"
      pattern: "scale: 'device'"
    - from: "src/capture/region.js (RegionError export)"
      to: "src/cli/format.js (Plan 04 imports this for the Guard 4 dispatcher branch)"
      via: "import { RegionError } from '../capture/region.js'"
      pattern: "class RegionError"
    - from: "src/output/template.js (resolveTemplate)"
      to: "src/capture/runCapture.js (Plan 03 calls resolveTemplate with { date, viewport, page, region })"
      via: "resolveTemplate(config.output, { date, viewport: vp.name, page, region: region.name })"
      pattern: "\\{region\\}"
---

<objective>
Create the new `src/capture/region.js` module that owns selector-mode and anchor-mode region capture, and extend `src/output/template.js` so the `{region}` placeholder resolves to the per-region slugified name (or stays literal when no region is being captured). Both files are touched by Wave 2 because they are downstream of the schema (Wave 1) but upstream of the seam (Wave 3) — and they touch DISJOINT files so they could in principle parallelize, but cohesion is higher when one plan owns both since `runCapture` (Wave 3) will call both in one breath.

Purpose: Per RESEARCH §Architectural Responsibility Map, per-region element capture lives in a NEW capture-layer module (`src/capture/region.js`) — sibling of `src/capture/frames.js` (clip+scroll+rAF analog) and `src/browser/launcher.js` (typed-error class analog) and `src/capture/index.js` (presentation-free orchestrator analog). The new module owns three pure-math helpers (padRect, unionRect, clampToDocument) and the `RegionError` typed error class. Per RESEARCH §Pattern 5, `{region}` placeholder substitution is the existing `resolveTemplate`'s extension point — pure string transform, no new files, no new dependencies.

Output: Two files touched. `src/capture/region.js` is brand-new (~150 lines per the RESEARCH §Code Examples draft); `src/output/template.js` gains one line in the `.replaceAll` chain and one argument in the destructured signature. Both are unit-testable against a Playwright page; the Wave 4 hermetic fixture is the end-to-end proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-region-capture/RESEARCH.md
@.planning/phases/08-region-capture/08-PATTERNS.md
@.planning/phases/08-region-capture/08-VALIDATION.md
@.planning/phases/08-region-capture/08-01-SUMMARY.md
@src/capture/index.js
@src/capture/frames.js
@src/browser/launcher.js
@src/output/template.js
@.planning/phases/04-prepare-pipeline/04-RESEARCH.md
@.planning/phases/05-scroll-stitch-output/05-RESEARCH.md

<interfaces>
<!-- The three composing analogs Phase 8's new module mirrors (08-PATTERNS §Pattern Assignments) -->

Analog A — `src/capture/index.js` header + mkdir-on-dirname pattern (capture/index.js:1-31, 32-33, 70-71):
- Imports: `import { mkdir, writeFile } from 'node:fs/promises'; import { dirname } from 'node:path';`
- Mandatory: `await mkdir(dirname(outputPath), { recursive: true })` BEFORE writing — NOT `mkdir(outputPath)` (creates a dir named foo.png → EISDIR)
- Library posture: NO console.*, NO process.exit, NO chalk/ora — pure library, errors bubble, caller owns presentation/lifecycle/exit codes
- Lifecycle: takes a prepared Page (Phase 4 already ran) and a resolved outputPath (caller already resolved); produces PNG; does NOT close page/context/browser

Analog B — `src/capture/frames.js:103-129` (the canonical clip + scroll + rAF + screenshot pattern):
- Scroll: `await page.evaluate((targetY) => { window.scrollTo({ top: targetY, behavior: 'instant' }); }, y);` — `behavior: 'instant'` is LOCKED invariant; NEVER 'smooth' (animates ~300ms, races rAF)
- Wait: `await page.evaluate(() => new Promise((r) => requestAnimationFrame(() => r())));` — ONE rAF roundtrip for paint to settle
- Screenshot options bag verbatim except clip value:
  ```
  page.screenshot({ clip: {x,y,width,height}, animations: 'disabled', scale: 'device', type: 'png' })
  ```
  Phase 8 adds `path: outputPath` to the bag because captureRegion writes its own file (frames.js returns buffer for stitch.js).

Analog B continued — geometry-once invariant (frames.js:74-82): when you need 2+ properties from the page, fetch in ONE page.evaluate round-trip (avoids stale-read races; reduces protocol overhead). Phase 8's `clampToDocument` reads scrollWidth + scrollHeight in one call.

Analog C — `src/browser/launcher.js:14-27` (BrowserError typed-error class verbatim shape):
```
export class BrowserError extends Error {
  constructor(message, { cause } = {}) {
    super(message);
    this.name = 'BrowserError';
    if (cause) this.cause = cause;
  }
}
```
Phase 8's `RegionError` MUST match exactly — same constructor signature, same `.cause` preservation, same `.name` setter pattern.

Playwright APIs verified by RESEARCH (against `node_modules/playwright-core/types/types.d.ts`):
- `page.locator(selector)` → Locator (types.d.ts:3597; handles shadow DOM by default)
- `locator.count()` → Promise<number> (types.d.ts:13390)
- `locator.first()` → Locator (deterministic single-match resolution)
- `locator.scrollIntoViewIfNeeded()` → Promise<void> (types.d.ts:14776-14791; native scroll, actionability built-in)
- `locator.boundingBox()` → Promise<{ x, y, width, height } | null> (types.d.ts:13105-13133; returns NULL for display:none — does NOT throw; coordinates are DOCUMENT-relative in CSS pixels)
- `page.screenshot({ clip, ... })` — clip is in CSS pixels (types.d.ts:24276-24296); requires clip rect to intersect current viewport at screenshot time (Playwright throws "Clipped area is either empty or outside the resulting image" otherwise — see Phase 5 RESEARCH §618)
- NOT usable: `locator.screenshot()` — has NO `clip` option per `LocatorScreenshotOptions` (types.d.ts:23613-23692). Padding-extended capture requires `clip`, so Phase 8 uses `page.screenshot({ clip })` for BOTH selector and anchor modes.

Existing `src/output/template.js:27-35` chain:
```
export function resolveTemplate(template, { date, viewport, page }) {
  return template
    .replaceAll('{date}', date)
    .replaceAll('{viewport}', slugify(viewport))
    .replaceAll('{page}', slugify(page));
}
```
Phase 8 destructures `region` from args and adds `.replaceAll('{region}', region ? slugify(region) : '{region}')` — when region is undefined, leave literal (matches unknown-placeholder posture in template.js:6-7).

Existing `slugify` (template.js:14-25) — UNCHANGED. Same SLUG_RE strips `/`, `{`, `}`, etc.; Unicode-aware; handles region names without modification.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/capture/region.js with RegionError, padRect/unionRect/clampToDocument pure helpers, and captureRegion(page, regionConfig, outputPath, { onProgress }) supporting selector AND anchor modes</name>
  <files>src/capture/region.js</files>
  <read_first>
    - .planning/phases/08-region-capture/RESEARCH.md §"Pattern 1" (selector flow, RESEARCH.md:246-290), §"Pattern 2" (anchor flow + union math, RESEARCH.md:292-352), §"Pattern 3" (padding + clampToDocument, RESEARCH.md:354-388), §"Pattern 4" (RegionError, RESEARCH.md:390-418), §"Pitfall 1" (re-scroll for clip intersection), §"Pitfall 2" (null boundingBox), §"Pitfall 3" (anchor scroll-then-measure order), §"Pitfall 4" (padding clamp), §"Code Examples" (RESEARCH.md:600-695 — fully drafted reference implementation)
    - .planning/phases/08-region-capture/08-PATTERNS.md §"src/capture/region.js (NEW — capture module)" §Analog A/B/C + §"Shared Patterns" §"Typed Error Class Shape" §"Presentation-free Library Posture" §"Playwright Screenshot Options Bag" §"Scroll-then-rAF-then-Screenshot" §"Geometry-once Invariant" §"mkdir(dirname(outputPath)) — Not mkdir(outputPath)" §"Anchor Mode Order"
    - src/capture/index.js (Analog A — header comment block, import shape, library posture)
    - src/capture/frames.js (Analog B — exact screenshot options bag, scroll+rAF sequence, geometry-once invariant)
    - src/browser/launcher.js (Analog C — typed-error class shape; copy BrowserError verbatim modulo name)
  </read_first>
  <behavior>
    - `import('./src/capture/region.js')` exposes named exports `captureRegion` AND `RegionError`
    - `new RegionError('msg').name === 'RegionError'`; `new RegionError('msg').message === 'msg'`; `new RegionError('msg', { cause: new Error('inner') }).cause.message === 'inner'` (constructor signature verbatim mirrors `BrowserError`)
    - `captureRegion(page, { name: 'hero', selector: '[data-test="x"]', padding: 0 }, outputPath)` on a page where `[data-test="x"]` matches one element produces a PNG at outputPath whose dimensions reflect the element's CSS-px box × deviceScaleFactor
    - `captureRegion(page, { name: 'hero', selector: '[data-test="x"]', padding: 20 }, outputPath)` produces a PNG with 40px of additional captured area (20 on each side, in CSS px) — clamped to document edges
    - `captureRegion(page, { name: 'hero', selector: '.does-not-exist' }, outputPath)` throws a `RegionError` whose `.message` contains the region name `'hero'` AND the selector text `.does-not-exist` AND the phrase `matched no elements`
    - `captureRegion` on a selector that matches but has `display:none` throws `RegionError` whose `.message` mentions `'hero'` AND `bounding box` (the null-boundingBox path per Pitfall 2)
    - `captureRegion(page, { name: 'cards', from: '[data-test="a"]', to: '[data-test="b"]', padding: 10 }, outputPath)` with both selectors matching produces a PNG spanning the union of both element boxes plus 10px of padding on every side, clamped
    - `captureRegion` anchor mode with a missing `from` throws RegionError mentioning `'from' selector` and the region name; same for missing `to` with `'to' selector`
    - The screenshot call ALWAYS uses `scale: 'device'`, `animations: 'disabled'`, `type: 'png'` — verified by source-level grep of the screenshot options bag
    - Pure helpers exist and are deterministic:
      - `padRect({ x: 100, y: 100, width: 200, height: 50 }, 10)` returns `{ x: 90, y: 90, width: 220, height: 70 }`
      - `padRect({ x: 100, y: 100, width: 200, height: 50 }, 0)` returns `{ x: 100, y: 100, width: 200, height: 50 }` (no-op)
      - `unionRect({ x: 0, y: 0, width: 100, height: 100 }, { x: 50, y: 200, width: 200, height: 50 })` returns `{ x: 0, y: 0, width: 250, height: 250 }`
      - `unionRect` is commutative: `unionRect(a, b)` deep-equals `unionRect(b, a)`
    - `mkdir(dirname(outputPath), { recursive: true })` runs BEFORE the screenshot call — verified by static grep AND by capturing to a deeply-nested non-existent path (e.g. `/tmp/fs08-test/nested/path/out.png`) and finding both the directory tree AND the PNG file afterwards
    - `captureRegion` emits ONE `{ type: 'step', label: "Capturing region '<name>'" }` event at start when an `onProgress` callback is provided
  </behavior>
  <action>
    Create `src/capture/region.js` from scratch. Use RESEARCH.md:600-695 as the reference implementation; deviations should be cosmetic only (variable naming, comment density, JSDoc fullness).

    1. Module header comment block — mirror `src/capture/index.js:1-31` structure: state the module's name, the wave/plan it ships in (Phase 8 Wave 2), the exports (`captureRegion`, `RegionError`), the contract (NO console output, NO process.exit, NO chalk/ora — pure library; errors bubble; caller owns presentation/lifecycle/exit codes), and the lifecycle expectation (takes a prepared Page, takes a resolved outputPath, produces a PNG; does NOT close page/context/browser). Include the `// Phase 8 Wave 2: ...` line prefix matching Phase 5's comment posture.

    2. Imports — copy from `src/capture/index.js:32-33`:
       - `import { mkdir } from 'node:fs/promises';` (no writeFile needed — `page.screenshot({ path })` writes the file itself)
       - `import { dirname } from 'node:path';`

    3. `RegionError` class — copy `BrowserError` from `src/browser/launcher.js:21-27` VERBATIM modulo name change. Same JSDoc preamble shape ("Named Error subclass for region-capture failures..."), same constructor signature `(message, { cause } = {})`, same `this.name = 'RegionError'`, same `if (cause) this.cause = cause`. Export as `export class RegionError extends Error { ... }`.

    4. Pure helpers — three module-private functions (per 08-PATTERNS §"Phase 8 specifics — pure-math helpers"; per RESEARCH.md:613-640 for exact implementations):
       - `function padRect(box, padding)` — returns `{ x: box.x - padding, y: box.y - padding, width: box.width + padding * 2, height: box.height + padding * 2 }`. Handles `padding === 0` correctly (no-op). Does NOT mutate `box`.
       - `function unionRect(a, b)` — returns `{ x: Math.min(a.x, b.x), y: Math.min(a.y, b.y), width: Math.max(a.x + a.width, b.x + b.width) - Math.min(a.x, b.x), height: Math.max(a.y + a.height, b.y + b.height) - Math.min(a.y, b.y) }`. Pure geometry; commutative.
       - `async function clampToDocument(page, rect)` — ONE `page.evaluate` round-trip to read `{ docWidth: document.documentElement.scrollWidth, docHeight: document.documentElement.scrollHeight }` (geometry-once invariant). Returns `{ x: Math.max(0, rect.x), y: Math.max(0, rect.y), width: Math.min(rect.width + Math.min(0, rect.x), docWidth - max(0, rect.x)), height: Math.min(rect.height + Math.min(0, rect.y), docHeight - max(0, rect.y)) }` per RESEARCH.md:631-639. The width/height arithmetic accounts for negative x/y by subtracting the overflow.

    5. Main export `async function captureRegion(page, regionConfig, outputPath, { onProgress = () => {} } = {})`:
       - Emit `onProgress({ type: 'step', label: \`Capturing region '\${regionConfig.name}'\` })` as the very first action so the CLI/server spinner reflects which region is in flight.
       - Compute `clip` based on mode:
         - SELECTOR MODE (`regionConfig.selector !== undefined`):
           - `const loc = page.locator(regionConfig.selector).first();`
           - `const count = await page.locator(regionConfig.selector).count();` (use a fresh `page.locator(sel)` for count — does NOT mutate the `.first()`-tagged locator; count of total matches, not "1 if .first() exists")
           - `if (count === 0) throw new RegionError(\`Region '\${regionConfig.name}': selector '\${regionConfig.selector}' matched no elements.\`)` — message MUST contain the region name AND the selector AND the phrase "matched no elements" (the behavior table asserts this)
           - `await loc.scrollIntoViewIfNeeded();`
           - `const box = await loc.boundingBox();`
           - `if (box === null) throw new RegionError(\`Region '\${regionConfig.name}': element '\${regionConfig.selector}' has no bounding box (likely display:none).\`)` — message MUST contain region name AND "bounding box"
           - `clip = await clampToDocument(page, padRect(box, regionConfig.padding ?? 0))` — `?? 0` is defensive (the schema already defaults to 0 in Wave 1, but the capture module makes no assumption about callers)
         - ANCHOR MODE (selector undefined, both from + to defined — schema guarantees this; defensive check optional):
           - `const fromLoc = page.locator(regionConfig.from).first();`
           - `const toLoc = page.locator(regionConfig.to).first();`
           - `if (await page.locator(regionConfig.from).count() === 0) throw new RegionError(\`Region '\${regionConfig.name}': 'from' selector '\${regionConfig.from}' matched no elements.\`)`
           - `if (await page.locator(regionConfig.to).count() === 0) throw new RegionError(\`Region '\${regionConfig.name}': 'to' selector '\${regionConfig.to}' matched no elements.\`)`
           - `await fromLoc.scrollIntoViewIfNeeded(); const boxFrom = await fromLoc.boundingBox();` (scroll → measure ATOMICALLY before touching `to` — Pitfall 3 / Anchor Mode Order invariant)
           - `await toLoc.scrollIntoViewIfNeeded(); const boxTo = await toLoc.boundingBox();`
           - `if (boxFrom === null || boxTo === null) throw new RegionError(\`Region '\${regionConfig.name}': anchor element has no bounding box.\`)`
           - `clip = await clampToDocument(page, padRect(unionRect(boxFrom, boxTo), regionConfig.padding ?? 0))`
       - Re-scroll for clip intersection (REQUIRED for both modes per Pitfall 1 — `scrollIntoViewIfNeeded` puts the FOCUS element in view, but the padded/unioned clip's TOP may sit above or below the focus element's top): `await page.evaluate((y) => window.scrollTo({ top: y, behavior: 'instant' }), Math.max(0, clip.y));` (use `behavior: 'instant'` per the locked invariant; NEVER 'smooth').
       - rAF roundtrip: `await page.evaluate(() => new Promise((r) => requestAnimationFrame(() => r())));`
       - Ensure output directory exists: `await mkdir(dirname(outputPath), { recursive: true });` (BEFORE the screenshot — never after).
       - Screenshot via `await page.screenshot({ path: outputPath, clip, scale: 'device', animations: 'disabled', type: 'png' })` — mirrors `src/capture/frames.js:124-129` verbatim modulo the `path` addition and the dynamic `clip` value. The full reference invocation is documented in `08-PATTERNS.md` §"Playwright Screenshot Options Bag" and RESEARCH.md §"Code Examples" §"Selector region capture" (RESEARCH.md:687-694). Required option keys: `path`, `clip`, `scale`, `animations`, `type`. Required values: `scale: 'device'`, `animations: 'disabled'`, `type: 'png'`. Do NOT add `fullPage: true` (defeats Phase 5's existence). Do NOT add `omitBackground` (page background is wanted).

    6. Add a JSDoc above `captureRegion` documenting the parameters, the return shape (`Promise<void>` — produces a side effect, the PNG file; the caller's runCapture is responsible for pushing the result into the accumulator with `{ outputPath, regionName }`), and the thrown error type (`RegionError` for all in-module failures; Playwright errors with `.name === 'TimeoutError'` may bubble from `boundingBox`/`scrollIntoViewIfNeeded` if a 30s default-timeout fires — those are intentionally not wrapped, matching Phase 3's posture).

    7. Verify the module is structurally sound: `node -e "import('./src/capture/region.js').then(m => console.log(Object.keys(m)))"` should print `[ 'RegionError', 'captureRegion' ]` (order may vary). Pure-helper functions (padRect, unionRect, clampToDocument) are module-private — NOT exported. If a future need surfaces (e.g. tests want to assert them directly), `export` them later as a non-breaking change.
  </action>
  <verify>
    <automated>
# Static checks
grep -c "export class RegionError extends Error" src/capture/region.js | grep -q "^1$" &&
grep -c "export async function captureRegion" src/capture/region.js | grep -q "^1$" &&
grep -c "this.name = 'RegionError'" src/capture/region.js | grep -q "^1$" &&
grep -c "scale: 'device'" src/capture/region.js | grep -q "^1$" &&
grep -c "animations: 'disabled'" src/capture/region.js | grep -q "^1$" &&
grep -c "type: 'png'" src/capture/region.js | grep -q "^1$" &&
grep -c "mkdir(dirname(outputPath)" src/capture/region.js | grep -q "^1$" &&
grep -c "behavior: 'instant'" src/capture/region.js | grep -q "^1$" &&
grep -c "requestAnimationFrame" src/capture/region.js | grep -q "^1$" &&
grep -c "fullPage: true" src/capture/region.js | grep -q "^0$" &&
grep -c "omitBackground" src/capture/region.js | grep -q "^0$" &&
# Pure helpers exist
grep -c "function padRect" src/capture/region.js | grep -q "^1$" &&
grep -c "function unionRect" src/capture/region.js | grep -q "^1$" &&
grep -c "function clampToDocument" src/capture/region.js | grep -q "^1$" &&
# Module imports as expected
node -e "
import('./src/capture/region.js').then((m) => {
  if (typeof m.captureRegion !== 'function') { console.error('FAIL captureRegion not a function'); process.exit(1); }
  if (typeof m.RegionError !== 'function') { console.error('FAIL RegionError not a class'); process.exit(1); }
  const e = new m.RegionError('msg-x', { cause: new Error('inner-y') });
  if (e.name !== 'RegionError') { console.error('FAIL .name', e.name); process.exit(1); }
  if (e.message !== 'msg-x') { console.error('FAIL .message', e.message); process.exit(1); }
  if (!(e instanceof Error)) { console.error('FAIL instanceof Error'); process.exit(1); }
  if (!e.cause || e.cause.message !== 'inner-y') { console.error('FAIL .cause', e.cause); process.exit(1); }
  // Cannot exercise captureRegion without a Playwright page in Wave 2 — end-to-end behavior verified in Plan 04.
  // But we CAN check it's an async function with expected arity
  if (m.captureRegion.length < 3) { console.error('FAIL captureRegion.length', m.captureRegion.length); process.exit(1); }
  console.log('REGION OK');
}).catch(e => { console.error('FAIL import', e); process.exit(1); });
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/capture/region.js` exists and is valid ESM (parses via dynamic import without error)
    - Named exports `captureRegion` (async function, arity ≥ 3) AND `RegionError` (class extending Error) are present
    - `RegionError` constructor signature: `(message, { cause } = {})` — verified by `new RegionError('x', { cause: new Error('y') })` preserving `.cause.message === 'y'`
    - `RegionError` instances have `.name === 'RegionError'`
    - Module imports: `node:fs/promises` (for `mkdir`) and `node:path` (for `dirname`). No imports from `src/cli/` (verified by `grep -c "from '../cli" src/capture/region.js == 0`)
    - Module contains NO `console.` calls (`grep -c "console\\." src/capture/region.js == 0`), NO `process.exit` calls, NO `chalk` or `ora` imports (presentation-free library posture)
    - Screenshot options bag matches Phase 5 contract: `scale: 'device'`, `animations: 'disabled'`, `type: 'png'` all present
    - FORBIDDEN options absent: `fullPage: true` count is 0; `omitBackground` count is 0
    - `mkdir(dirname(outputPath), { recursive: true })` appears in source (count = 1) — NOT `mkdir(outputPath)`
    - Scroll uses `behavior: 'instant'` (count ≥ 1) and rAF roundtrip pattern is present (count ≥ 1)
    - Pure helpers `padRect`, `unionRect`, `clampToDocument` are all present in source (function declarations)
    - The verify script above prints `REGION OK` with exit 0
  </acceptance_criteria>
  <done>
    `src/capture/region.js` is a complete, presentation-free capture module exporting `captureRegion` and `RegionError`. The selector and anchor branches follow the count-check → scroll → measure → null-check → pad → clamp → re-scroll → rAF → screenshot pipeline per RESEARCH §Patterns 1+2. The pure geometry helpers are colocated. End-to-end behavior is verified in Plan 04's hermetic fixture; this plan establishes the module shape and contract.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend src/output/template.js so resolveTemplate accepts an optional region argument and substitutes {region} (literal when omitted)</name>
  <files>src/output/template.js</files>
  <read_first>
    - src/output/template.js (the whole file — preserves slugify + the unknown-placeholder-stays-literal posture documented at lines 6-7)
    - .planning/phases/08-region-capture/RESEARCH.md §"Pattern 5" (RESEARCH.md:420-438 — exact code change drafted)
    - .planning/phases/08-region-capture/08-PATTERNS.md §"src/output/template.js" (analog mapping + comment-preservation rules)
    - .planning/phases/02-cli-config/ (any prior SUMMARY referencing the unknown-placeholder-stays-literal decision) — not load-bearing; the comment block at template.js:6-7 IS the canonical source for this posture
  </read_first>
  <behavior>
    - `resolveTemplate('/tmp/{region}-{page}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home', region: 'hero' })` returns `/tmp/hero-home.png`
    - `resolveTemplate('/tmp/{region}-{page}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home' })` (region omitted) returns `/tmp/{region}-home.png` — literal placeholder preserved
    - `resolveTemplate('/tmp/{region}-{page}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home', region: undefined })` returns `/tmp/{region}-home.png` — explicit `undefined` matches omitted
    - `resolveTemplate('/tmp/{viewport}/{page}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home' })` (NO `{region}` in template, no region arg) returns `/tmp/desktop/home.png` — existing behavior unchanged (back-compat)
    - `resolveTemplate('/tmp/{region}-{page}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home', region: 'Hero Card!!!' })` slugifies region name to `hero-card` (uses the existing `slugify` SLUG_RE — strips structural chars including `!`)
    - `resolveTemplate('/tmp/{region}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home', region: '../../etc/passwd' })` slugifies the traversal sequence to `--etc-passwd` (security: SLUG_RE strips `/`; per Phase 8 RESEARCH §Security threat T-08-02 / planning_context locked threat model)
    - Existing callers of `resolveTemplate(template, { date, viewport, page })` continue to work unchanged
  </behavior>
  <action>
    Edit `src/output/template.js`. Two surgical changes to `resolveTemplate` (lines 27-35), nothing else:

    1. Add `region` to the destructured argument: signature becomes `export function resolveTemplate(template, { date, viewport, page, region })` — `region` is `undefined` when omitted by the caller, which is the back-compat path.

    2. Append one line to the `.replaceAll` chain (immediately after the existing `.replaceAll('{page}', slugify(page))` line):
       `.replaceAll('{region}', region ? slugify(region) : '{region}');`
       The truthy guard means "if a region name was provided, substitute its slugified form; otherwise leave the literal `{region}` intact." Literal-when-undefined matches the existing unknown-placeholder posture documented at template.js:6-7 ("Unknown placeholders (e.g. {foo}) are left literal — typos surface visibly in the output path rather than silently being dropped"). The same defensive reasoning applies: full-page captures that don't pass `region` keep `{region}` literal so the user sees the unsubstituted token (and knows to remove `{region}` from their template if they want full-page-only).

    3. Update the leading function comment block (lines 28-30) to add one line documenting the `{region}` placeholder behavior:
       `// {region} is slugified when present; left literal when region arg is undefined (full-page run).`
       Place this line alongside the existing `{viewport}` / `{page}` documentation lines.

    4. Do NOT touch the `SLUG_RE` constant or the `slugify` function. The same Unicode-aware sanitizer handles region names — `slugify('Hero Card!!!')` returns `'hero-card'`, `slugify('../../etc/passwd')` returns `'--etc-passwd'` (the structural-char stripping is what neutralizes path traversal per planning_context security threat model).

    5. Do NOT touch the leading file header comment (lines 1-7) — the "Unknown placeholders are left literal" line at lines 6-7 IS the canonical doc for the literal-when-undefined behavior, and the new `.replaceAll('{region}', region ? slugify(region) : '{region}')` line is the literal application of that posture.
  </action>
  <verify>
    <automated>
# Source-level grep
grep -c "{ date, viewport, page, region }" src/output/template.js | grep -q "^1$" &&
grep -c "{region}" src/output/template.js | grep -q "^3$" &&
# 3 occurrences: (a) the replaceAll target, (b) the literal fallback when region is undefined, (c) the comment line
grep -c "slugify(region)" src/output/template.js | grep -q "^1$" &&
# Behavior tests
node -e "
import('./src/output/template.js').then(({ resolveTemplate }) => {
  const cases = [
    {
      input: ['/tmp/{region}-{page}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home', region: 'hero' }],
      expect: '/tmp/hero-home.png',
      label: 'region-substituted'
    },
    {
      input: ['/tmp/{region}-{page}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home' }],
      expect: '/tmp/{region}-home.png',
      label: 'region-omitted-literal'
    },
    {
      input: ['/tmp/{region}-{page}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home', region: undefined }],
      expect: '/tmp/{region}-home.png',
      label: 'region-undefined-literal'
    },
    {
      input: ['/tmp/{viewport}/{page}.png', { date: '2026-05-22', viewport: 'desktop', page: 'home' }],
      expect: '/tmp/desktop/home.png',
      label: 'back-compat-no-region-key'
    },
    {
      input: ['/tmp/{region}.png', { date: '2026-05-22', viewport: 'd', page: 'h', region: 'Hero Card!!!' }],
      expect: '/tmp/hero-card.png',
      label: 'region-slugified'
    },
    {
      input: ['/tmp/{region}.png', { date: '2026-05-22', viewport: 'd', page: 'h', region: '../../etc/passwd' }],
      expect: '/tmp/--etc-passwd.png',
      label: 'region-traversal-slugified'
    },
  ];
  let fails = 0;
  for (const { input, expect, label } of cases) {
    const got = resolveTemplate(...input);
    if (got !== expect) { console.error('FAIL', label, 'expected:', expect, 'got:', got); fails++; }
  }
  if (fails > 0) process.exit(1);
  console.log('TEMPLATE OK');
});
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/output/template.js` `resolveTemplate` signature destructures `region` (matches `{ date, viewport, page, region }`)
    - Source contains `.replaceAll('{region}', region ? slugify(region) : '{region}')` exactly once
    - Existing `SLUG_RE`, `slugify`, leading header comment block (lines 1-7) all UNCHANGED — diff against HEAD shows changes only inside `resolveTemplate` and the immediately-adjacent comment block (lines ~27-35)
    - All 6 behavior cases in the verify script pass; script prints `TEMPLATE OK` with exit 0
    - Existing callers (`src/capture/runCapture.js`, `src/cli.js`) that don't yet pass `region` continue to work — destructuring an undefined `region` is non-throwing in JS, and the truthy guard in the new `.replaceAll` produces the literal-fallback path
    - `node -e "import('./src/output/template.js').then(m => console.log(Object.keys(m)))"` does not throw and exports include `resolveTemplate`
  </acceptance_criteria>
  <done>
    `resolveTemplate` accepts an optional `region` argument and substitutes the `{region}` placeholder with the slugified value when present, leaving it literal when omitted. All existing callers continue to work unchanged. The literal-when-undefined behavior matches the documented unknown-placeholder posture at template.js:6-7.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated regionConfig → Playwright API | `regionConfig` has already passed zod validation in Plan 01 (selector/from/to are non-empty strings, name is unique). The new `src/capture/region.js` consumes the validated shape and passes it to `page.locator(selector)`, which parses as a CSS selector — NOT JS — per the planning_context locked security threat model. |
| region name → output path | Plan 02 itself does not write to the filesystem using region names — `src/output/template.js` slugifies via the existing SLUG_RE before any path interpolation reaches Plan 03/04. Path traversal via region name is neutralized at the template layer (verified by the `'--etc-passwd'` behavior case in Task 2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | selector string passed to `page.locator` | accept | Per planning_context: "Malicious selector / arbitrary JS via selector → not a Phase 8 concern (Playwright locator parses CSS, not JS)". No JS execution surface added. |
| T-08-05 | Tampering | path traversal via region `name` in output path slugification | mitigate | Existing `slugify` in `src/output/template.js:14` (SLUG_RE strips `/`, `{`, `}`, etc.). Verified by behavior case in Task 2 (`'../../etc/passwd'` → `'--etc-passwd'`). Inherited mitigation — no new code needed. |
| T-08-06 | Denial of Service | Playwright locator timeout exhaustion on selector matching nothing | accept | `locator.count()` returns immediately without retrying (does not respect actionability checks) — used as a fast pre-check before throwing RegionError. `boundingBox()` and `scrollIntoViewIfNeeded()` use Playwright's default 30s timeout; a never-resolving selector waits up to 30s then bubbles a Playwright TimeoutError. Acceptable for v0.2 personal-tool scope. |

Local-only, no exploitable surface added by this phase. The capture module is presentation-free; all errors are typed (RegionError) and bubble through the existing single-error-sink contract in `index.js`.
</threat_model>

<verification>
Both per-task verify scripts must pass with exit 0 (`REGION OK` and `TEMPLATE OK`). End-to-end region-capture behavior against a live Playwright page is verified in Plan 04's hermetic smoke fixture — this wave only proves the module shape, contract, and pure-function correctness.

Optional cross-check (informational): confirm no existing module already imports from `src/capture/region.js` yet — the dispatcher branch in `src/cli/format.js` is added in Plan 04, and the seam in `src/capture/runCapture.js` is wired in Plan 03:

```bash
grep -r "from.*capture/region" src/ --exclude-dir=node_modules
# Expected output AT WAVE 2: nothing (the file exists but no consumer imports it yet)
```
</verification>

<success_criteria>
- `src/capture/region.js` is a new ESM module exporting `RegionError` (class) and `captureRegion` (async function)
- Module is presentation-free (no console.*, no chalk/ora, no process.exit, no imports from src/cli/)
- Module follows all locked invariants: screenshot options bag matches Phase 5; mkdir uses dirname; scroll uses behavior:'instant'; rAF roundtrip present; no fullPage:true; no omitBackground
- Pure helpers padRect/unionRect/clampToDocument are colocated (module-private)
- `src/output/template.js` `resolveTemplate` accepts optional `region` and substitutes `{region}` placeholder when truthy; literal fallback when undefined
- All existing template callers (`runCapture`, `cli.js --smoke`) continue to work unchanged
- Both task verify scripts pass with exit 0
</success_criteria>

<output>
Create `.planning/phases/08-region-capture/08-02-SUMMARY.md` when done — record:
- Final line count and export list of `src/capture/region.js`
- Whether the pure helpers (padRect, unionRect, clampToDocument) stayed module-private or were exported (the planner specified module-private; deviation should be noted with rationale)
- Exact one-line change to `resolveTemplate` (signature + chain extension) for downstream consumers (Plan 03) to reference
</output>