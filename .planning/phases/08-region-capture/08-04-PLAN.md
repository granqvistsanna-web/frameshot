---
phase: 08-region-capture
plan: 04
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - src/cli.js
  - src/cli/format.js
  - samples/serve-smoke.js
  - samples/smoke-regions.yaml
autonomous: false
requirements: [REGION-01, REGION-02, REGION-03]
requirements_addressed: [REGION-01, REGION-02, REGION-03]

must_haves:
  truths:
    - "`framershot capture <config> --only=<region-name>` is a valid CLI invocation; commander parses `--only` as a required-value option (angle brackets) on the existing `capture` subcommand"
    - "`framershot capture <config> --smoke --only=<name>` exits non-zero with a clean RegionError message — `--smoke` and `--only` are mutually exclusive (RESEARCH §Pitfall 8 lock)"
    - "The CLI passes `only: opts.only` (string | undefined) through to `runCapture`'s opts bag; no other wiring change in the non-smoke branch"
    - "The CLI emits one `screenshot written (<label>): <path>` stdout line per result, where `<label>` is `region '<name>'` for region entries and `full page` for full-page entries (matches RESEARCH.md:731-734 surface)"
    - "`src/cli/format.js` `formatError` dispatcher recognizes `RegionError` via a new `instanceof` branch (Guard 4 between BrowserError and bare TimeoutError); the branch returns `${chalk.red('Error:')} ${err.message}` — same shape as ConfigError/BrowserError, body not chalked, no stack trace (08-PATTERNS §\"Error Dispatcher Branch\")"
    - "`samples/serve-smoke.js` HTML gains `[data-test=\"region-hero\"]`, `[data-test=\"region-from\"]`, and `[data-test=\"region-to\"]` sections positioned below the existing PREP-04 spacer so `scrollIntoViewIfNeeded` actually scrolls to reveal them; existing fixture sections (anim-target, io-target, hidden-by-test, lazy-img) are UNCHANGED"
    - "`samples/smoke-regions.yaml` is a new fixture file with: one selector region (`hero`), one anchor region (`cards`), padding on at least one region, `{region}` placeholder in the output template, singular `viewport:` form (back-compat exerciser per 08-PATTERNS §\"samples/smoke-regions.yaml (NEW fixture)\")"
    - "A hermetic capture of `smoke-regions.yaml` WITHOUT `--only` produces 3 PNG files per viewport: hero region, cards region, AND the full-page (Open Q#1 lock A — capture everything declared)"
    - "A hermetic capture of `smoke-regions.yaml` WITH `--only=hero` produces exactly 1 PNG file per viewport: the hero region only (full-page skipped, cards region skipped)"
    - "v0.1 back-compat: `samples/smoke.yaml --smoke` continues to exit 0 and produce exactly one PNG (no Phase 8 regression to the v0.1 path)"
    - "`samples/smoke-regions.yaml` validates against the Plan 01 schema and `loadConfig` returns `result.regions` populated"
  artifacts:
    - path: "src/cli.js"
      provides: "--only option on capture command; --smoke/--only mutual exclusion guard; non-smoke branch consumes the new array entries with optional regionName; per-result stdout labeling"
      contains: "--only"
    - path: "src/cli/format.js"
      provides: "Guard 4 — RegionError instanceof branch in formatError dispatcher"
      contains: "RegionError"
    - path: "samples/serve-smoke.js"
      provides: "Extended fixture HTML with data-test region-hero / region-from / region-to sections"
      contains: "region-hero"
    - path: "samples/smoke-regions.yaml"
      provides: "Phase 8 hermetic smoke fixture covering selector + anchor + --only across all three REGION requirements"
      contains: "regions:"
  key_links:
    - from: "src/cli.js (--only option)"
      to: "src/capture/runCapture.js (only: opts.only pass-through)"
      via: "runCapture(config, { onProgress, only: opts.only })"
      pattern: "only: opts\\.only"
    - from: "src/cli/format.js (Guard 4)"
      to: "src/capture/region.js (RegionError export)"
      via: "import { RegionError } from '../capture/region.js'"
      pattern: "RegionError"
    - from: "samples/smoke-regions.yaml"
      to: "samples/serve-smoke.js (extended HTML)"
      via: "baseUrl: http://127.0.0.1:7357 + region selectors targeting [data-test=\"region-*\"]"
      pattern: "data-test=\"region-"
---

<objective>
Wire the `--only=<region-name>` CLI flag into commander, plug `RegionError` into `formatError`'s dispatcher, extend the hermetic fixture server with region-shaped DOM sections, and create `samples/smoke-regions.yaml` as the end-to-end proof of all three REGION requirements. This plan is the user-visible surface of Phase 8: after this lands, the CLI accepts `--only`, error messages are well-formatted, and the smoke fixture demonstrates selector + anchor + filter behavior in one verifiable run.

Purpose: Per 08-PATTERNS §"Wave-friendly File Modification Boundaries", Wave 4 plans touch four DISJOINT files — `src/cli.js`, `src/cli/format.js`, `samples/serve-smoke.js`, and the new `samples/smoke-regions.yaml`. The CLI changes depend on `RegionError` being importable (Wave 2 produces it) and the runCapture `only` plumbing being live (Wave 3 produces it), so this plan sits at Wave 4. All four file edits are bundled into one plan because (a) they collectively constitute "the user-facing region surface" and (b) the hermetic smoke fixture's end-to-end check is the only proof that all four pieces wire together correctly.

Output: Four files touched. The CLI gains a `--only` option, the formatError dispatcher gains a `RegionError` branch, the fixture HTML gains three new `data-test`-tagged sections, and `samples/smoke-regions.yaml` is born — exercising selector mode (REGION-01), anchor mode (REGION-02), and the `--only` filter (REGION-03) against the hermetic localhost server.

This plan also acts as the Wave 0 fixture scaffold (per 08-VALIDATION.md "Wave 0 Requirements") — the serve-smoke.js HTML extension and the smoke-regions.yaml file are the prerequisites for any region-capture verification, and they land alongside the wiring that consumes them so a single commit produces a fully-verifiable surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-region-capture/RESEARCH.md
@.planning/phases/08-region-capture/08-PATTERNS.md
@.planning/phases/08-region-capture/08-VALIDATION.md
@.planning/phases/08-region-capture/08-01-SUMMARY.md
@.planning/phases/08-region-capture/08-02-SUMMARY.md
@.planning/phases/08-region-capture/08-03-SUMMARY.md
@.planning/phases/07-multi-viewport-capture/07-03-PLAN.md
@.planning/phases/07-multi-viewport-capture/07-04-PLAN.md
@src/cli.js
@src/cli/format.js
@samples/smoke.yaml
@samples/serve-smoke.js
@.planning/phases/06-terminal-ux/06-RESEARCH.md

<interfaces>
<!-- Current CLI shape (post-Phase 7 expected — the --only option layers on top of Phase 7 Plan-03's array-consumer shape) -->

CLI `capture` subcommand options (current src/cli.js:28-31, will be extended by Phase 7 Plan 03):
- `.option('--smoke', '...')` — Phase 3 hermetic-verifiable seam; takes config.viewports[0] post-Phase 7

Post-Phase-7 non-smoke branch (07-03-PLAN §Task 1 expected shape):
- `const results = await runCapture(config, { onProgress: (event) => { ... } });`
- `spinner.succeed(\`\${results.length} screenshot(s) written\`);`
- `for (const { outputPath } of results) console.log(\`screenshot written: \${outputPath}\`);`
- Per-viewport spinner prefix `[<viewport>] <label>` already added by Phase 7

Phase 8 layered changes (RESEARCH.md:702-735 has the full proposed diff):
- Add `.option('--only <region-name>', 'Capture only the named region (skips full-page stitch)')` (angle brackets = required value per commander v12)
- Mutual exclusion: `if (opts.smoke && opts.only) throw new RegionError('--smoke and --only are mutually exclusive');` BEFORE the smoke branch (RESEARCH §Pitfall 8; planner uses RegionError per RESEARCH.md:720 "the more polished call")
- Non-smoke branch: pass `only: opts.only` through to runCapture
- Result iteration: label each line per result shape: `const label = r.regionName ? \`region '\${r.regionName}'\` : 'full page';` and emit `console.log(\`screenshot written (\${label}): \${r.outputPath}\`);`

Phase 6's formatError dispatcher (src/cli/format.js:96-130):
- Guard 1: non-Error throws → `chalk.red('Unexpected error:') + String(err)`
- Guard 2: ConfigError → `${chalk.red('Error:')} ${err.message}`
- Guard 3: BrowserError → `${chalk.red('Error:')} ${err.message}` + optional dim "(timed out)" hint
- Guard 4 (existing): bare TimeoutError → `${chalk.red('Error:')} Operation timed out — ${err.message}`
- Default: unexpected error → red headline + dim stack body

Phase 8 inserts a new branch between Guard 3 and the existing Guard 4 (bare TimeoutError) — mirror Guard 2's shape exactly (no dim hint, no stack — RegionError messages are already actionable).

Imports at src/cli/format.js:7-9:
- `import { ConfigError } from '../config/load.js';`
- `import { BrowserError } from '../browser/launcher.js';`
Phase 8 adds:
- `import { RegionError } from '../capture/region.js';`

`samples/serve-smoke.js` current HTML structure (lines 42-86): inline HTML in a template literal hosted at 127.0.0.1:7357. Existing fixture sections use `data-test="<id>"` tagging convention (anim-target, io-target, hidden-by-test, lazy-img). Phase 8 adds three new sections AFTER the PREP-04 lazy `<img>` (around line 84) but BEFORE the closing `<footer>` (per 08-PATTERNS §"samples/serve-smoke.js (fixture HTML — add named selector sections)"):
- `<section data-test="region-hero">` — single named element for selector-region capture
- `<div data-test="region-from">` — top anchor element
- (spacer 400px between from and to to give the union rect visible height)
- `<div data-test="region-to">` — bottom anchor element

These sections sit BELOW the existing 2000px spacer so `scrollIntoViewIfNeeded` actually has to scroll to reveal them — exercising the post-Wave-2 scroll-then-measure flow.

`samples/smoke-regions.yaml` shape (RESEARCH.md:741-765 + 08-PATTERNS §"samples/smoke-regions.yaml"):
- `baseUrl: http://127.0.0.1:7357` (same hermetic fixture server, no new server)
- `output: /tmp/framershot-smoke-regions/{region}-{page}.png` (or with `{viewport}` prefix; planner uses the simpler one since the fixture uses singular viewport)
- `viewport: { width: 800, height: 600, name: smoke }` (singular form — exercises Phase 7 D-01 back-compat alias)
- `regions:` array of two entries: selector region `hero` + anchor region `cards` with from/to + at least one with padding
- `prepare:` matching `samples/smoke.yaml` (animations true, scrollPrime true, extraDelay 500-1000)
- Output path under `/tmp/` (no .gitignore work; matches existing fixture convention)

Wave 0 dependency (08-VALIDATION.md): Plan 04 IS the Wave 0 scaffold work (HTML extension + new YAML fixture). The earlier waves (1-3) verified themselves via inline scripts that constructed in-memory configs; the new YAML fixture and DOM sections are only needed for THIS plan's end-to-end proof.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the hermetic fixture HTML in samples/serve-smoke.js with region-hero / region-from / region-to sections, then create samples/smoke-regions.yaml targeting them</name>
  <files>samples/serve-smoke.js, samples/smoke-regions.yaml</files>
  <read_first>
    - samples/serve-smoke.js (the whole file — preserves PORT, the HTML template literal pattern, the existing data-test conventions, the SIGTERM/SIGINT/SIGHUP cleanup)
    - samples/smoke.yaml (the canonical fixture shape — copy structure)
    - .planning/phases/08-region-capture/08-PATTERNS.md §"samples/serve-smoke.js (fixture HTML — add named selector sections)" (exact section HTML drafted) + §"samples/smoke-regions.yaml (NEW fixture)" (exact YAML drafted)
    - .planning/phases/08-region-capture/RESEARCH.md §"Code Examples" §"Region config — YAML" (RESEARCH.md:741-765) + §"Validation Architecture" §"Wave 0 Gaps" (RESEARCH.md:935-938)
    - .planning/phases/08-region-capture/08-VALIDATION.md (Wave 0 Requirements bullet list — defines the data-test selector contract)
    - .planning/phases/07-multi-viewport-capture/07-04-PLAN.md (sibling NEW-fixture plan — same structure)
  </read_first>
  <action>
    Two file changes; both are additive (existing fixture sections AND samples/smoke.yaml stay byte-identical).

    PART A — `samples/serve-smoke.js` HTML extension:

    Locate the existing `<!-- PREP-04 fixture:` block ending with the `<img data-test="lazy-img" ...>` closing tag (around lines 79-84) and the closing `<footer>` line. Insert THREE new sections between them, preserving the existing `data-test="<id>"` tagging convention:

    1. `<!-- REGION-01 fixture: a single named element for selector-region capture. samples/smoke-regions.yaml's 'hero' region targets [data-test="region-hero"]. Positioned below the PREP-04 spacer so scrollIntoViewIfNeeded must scroll to reveal it. -->`
       `<section data-test="region-hero" style="margin: 2rem; padding: 2rem; background: #036; color: #fff;">`
       `  <h2>Region hero (selector capture target)</h2>`
       `  <p>This is the entire selector region. Padding should produce visible margin around it.</p>`
       `</section>`

    2. `<!-- REGION-02 fixture: 'from' anchor for anchor-region capture. samples/smoke-regions.yaml's 'cards' region uses from=[data-test="region-from"] to=[data-test="region-to"]. -->`
       `<div data-test="region-from" style="margin: 2rem; padding: 1rem; background: #063; color: #fff;">`
       `  Anchor: from (top of region)`
       `</div>`

    3. (400px spacer to give the union rect visible height):
       `<div style="height: 400px; background: linear-gradient(#063, #630);"></div>`
       Then the `to` anchor:
       `<!-- REGION-02 fixture: 'to' anchor — the bottom of the anchor-region union. -->`
       `<div data-test="region-to" style="margin: 2rem; padding: 1rem; background: #630; color: #fff;">`
       `  Anchor: to (bottom of region)`
       `</div>`

    The CSS color choices (#036 hero, #063→#630 anchors) are deliberately distinct from the existing fixture colors (#c00 red header, #ff0 anim, #0ff io, #888 hidden) so a visual check of any region PNG immediately reveals which section it captured.

    DO NOT touch:
    - The PORT constant (line 19) — stays at 7357
    - The existing HTML sections (lines 42-84) — anim-target, io-target, hidden-by-test, lazy-img, the 2000px spacer
    - The server creation block (lines 90-93)
    - The `server.listen(PORT, '127.0.0.1', ...)` localhost bind (line 95)
    - The SIGTERM/SIGINT/SIGHUP cleanup block (lines 100-104)

    PART B — `samples/smoke-regions.yaml` creation:

    Create the new YAML file with the following structure (modeled on `samples/smoke.yaml` lines 32-50, with the additions called out in 08-PATTERNS §"samples/smoke-regions.yaml"):

    - Leading header comment block (8-12 lines): identify this as the Phase 8 hermetic smoke fixture for REGION-01/02/03; note it targets the same `samples/serve-smoke.js` server as `samples/smoke.yaml` (which must be extended first per Part A); document the three demonstrations (selector region 'hero', anchor region 'cards', `--only` isolation via separate invocation); include local-run instructions:
      The full reference comment block is in 08-PATTERNS.md §"samples/smoke-regions.yaml (NEW fixture)" and RESEARCH.md §"Code Examples" §"Region config — YAML" (RESEARCH.md:741-765). Required YAML comments inside this block: `# To run this smoke locally:`, `#   node samples/serve-smoke.js &`, `#   node index.js capture samples/smoke-regions.yaml                # captures hero + cards + full-page (Open Q#1 lock A: capture everything declared)`, `#   node index.js capture samples/smoke-regions.yaml --only=hero    # captures hero ONLY (skips full-page and cards)`, `#   kill %1`.
    - `name: phase-08-smoke-regions`
    - `baseUrl: http://127.0.0.1:7357` (same fixture server)
    - `output: /tmp/framershot-smoke-regions/{region}-{page}.png` — `{region}` is the success-criterion-#2 evidence; `/tmp/` keeps repo clean
    - `deviceScaleFactor: 2`
    - `viewport:` (singular form — exercises Phase 7 D-01 back-compat alias per 08-PATTERNS §"samples/smoke-regions.yaml" choice):
      - `width: 800`
      - `height: 600`
      - `name: smoke`
    - `page: { path: "/", name: home }`
    - `prepare:` matching smoke.yaml for parity:
      - `animations: true`
      - `scrollPrime: true`
      - `extraDelay: 500`
      (intentionally no `hide:` list — `samples/smoke.yaml` already covers the hide-warning path; this fixture's focus is region capture)
    - `regions:` array of two entries:
      - Entry 1 — selector mode (REGION-01):
        - `name: hero`
        - `selector: '[data-test="region-hero"]'`
        - `padding: 20`
      - Entry 2 — anchor mode (REGION-02):
        - `name: cards`
        - `from: '[data-test="region-from"]'`
        - `to: '[data-test="region-to"]'`
        - `padding: 10`

    Do NOT introduce per-region prepare overrides (deferred per RESEARCH §Deferred Ideas). Do NOT modify `samples/smoke.yaml` (it stays as the v0.1 back-compat fixture).
  </action>
  <verify>
    <automated>
# PART A — serve-smoke.js HTML extension
grep -c 'data-test="region-hero"' samples/serve-smoke.js | grep -q "^1$" &&
grep -c 'data-test="region-from"' samples/serve-smoke.js | grep -q "^1$" &&
grep -c 'data-test="region-to"' samples/serve-smoke.js | grep -q "^1$" &&
# Existing fixture sections untouched
grep -c 'data-test="anim-target"' samples/serve-smoke.js | grep -q "^1$" &&
grep -c 'data-test="io-target"' samples/serve-smoke.js | grep -q "^1$" &&
grep -c 'data-test="hidden-by-test"' samples/serve-smoke.js | grep -q "^1$" &&
grep -c 'data-test="lazy-img"' samples/serve-smoke.js | grep -q "^1$" &&
# Server still binds to 127.0.0.1:7357
grep -c "server.listen(PORT, '127.0.0.1'" samples/serve-smoke.js | grep -q "^1$" &&
grep -c "const PORT = 7357" samples/serve-smoke.js | grep -q "^1$" &&
# samples/smoke.yaml UNCHANGED
git diff --quiet -- samples/smoke.yaml || { echo "FAIL samples/smoke.yaml was modified"; exit 1; }
# PART B — smoke-regions.yaml created and validates
test -f samples/smoke-regions.yaml || { echo "FAIL fixture file missing"; exit 1; }
grep -q "^regions:" samples/smoke-regions.yaml || { echo "FAIL no regions: key"; exit 1; }
grep -q "name: hero" samples/smoke-regions.yaml || { echo "FAIL hero region missing"; exit 1; }
grep -q "name: cards" samples/smoke-regions.yaml || { echo "FAIL cards region missing"; exit 1; }
grep -q "{region}" samples/smoke-regions.yaml || { echo "FAIL {region} placeholder missing"; exit 1; }
grep -q "127.0.0.1:7357" samples/smoke-regions.yaml || { echo "FAIL wrong baseUrl"; exit 1; }
grep -q 'data-test="region-hero"' samples/smoke-regions.yaml || { echo "FAIL hero selector mismatch"; exit 1; }
grep -q 'data-test="region-from"' samples/smoke-regions.yaml || { echo "FAIL from selector mismatch"; exit 1; }
grep -q 'data-test="region-to"' samples/smoke-regions.yaml || { echo "FAIL to selector mismatch"; exit 1; }
# Schema validation
node -e "
import('./src/config/load.js').then(async ({ loadConfig }) => {
  const cfg = await loadConfig('samples/smoke-regions.yaml');
  if (!Array.isArray(cfg.regions) || cfg.regions.length !== 2) { console.error('FAIL regions array', cfg); process.exit(1); }
  if (cfg.regions[0].name !== 'hero' || cfg.regions[1].name !== 'cards') { console.error('FAIL region names', cfg.regions); process.exit(1); }
  if (cfg.regions[0].padding !== 20) { console.error('FAIL hero padding', cfg.regions[0]); process.exit(1); }
  if (cfg.regions[1].padding !== 10) { console.error('FAIL cards padding', cfg.regions[1]); process.exit(1); }
  console.log('FIXTURE VALIDATES OK');
});
" &&
# Hermetic HTTP check — the data-test sections are actually served
node samples/serve-smoke.js &
SERVER_PID=$!
sleep 1
HTML=$(curl -s http://127.0.0.1:7357/)
kill $SERVER_PID 2>/dev/null
echo "$HTML" | grep -q 'data-test="region-hero"' || { echo "FAIL HTML missing region-hero"; exit 1; }
echo "$HTML" | grep -q 'data-test="region-from"' || { echo "FAIL HTML missing region-from"; exit 1; }
echo "$HTML" | grep -q 'data-test="region-to"' || { echo "FAIL HTML missing region-to"; exit 1; }
echo "FIXTURE SCAFFOLD OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `samples/serve-smoke.js` contains all three new `data-test` attributes: `region-hero`, `region-from`, `region-to` (one match each)
    - All four pre-existing `data-test` attributes (anim-target, io-target, hidden-by-test, lazy-img) remain (counts unchanged)
    - `samples/serve-smoke.js` PORT constant and 127.0.0.1 binding are unchanged
    - `samples/smoke.yaml` is bit-identical to HEAD (`git diff --quiet -- samples/smoke.yaml` returns 0)
    - `samples/smoke-regions.yaml` exists and is a valid YAML file
    - The new fixture validates against the Plan-01 schema: `loadConfig('samples/smoke-regions.yaml')` returns an object with `regions.length === 2`, names `'hero'` and `'cards'`, paddings `20` and `10`
    - The hermetic HTTP check confirms the running fixture server serves the new sections
    - The verify script prints `FIXTURE SCAFFOLD OK` with exit 0
  </acceptance_criteria>
  <done>
    The hermetic fixture is fully scaffolded: serve-smoke.js HTML extension and the new smoke-regions.yaml fixture file are in place and validate end-to-end. The fixture is ready to be consumed by Task 3's CLI end-to-end check. smoke.yaml is untouched (v0.1 back-compat fixture preserved).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `--only <region-name>` option to commander, mutual-exclusion guard with --smoke, plumb `only: opts.only` through runCapture call, label per-result stdout lines; add RegionError Guard 4 to formatError dispatcher</name>
  <files>src/cli.js, src/cli/format.js</files>
  <read_first>
    - src/cli.js (the whole file — preserves currentSpinner/getCurrentSpinner contract, both branches, the post-Phase-7 array consumer shape)
    - src/cli/format.js (the whole file — preserves Guards 1-4 ordering, body-not-chalked rule, no-stack-trace rule)
    - .planning/phases/08-region-capture/RESEARCH.md §"Pattern 7" (RESEARCH.md:468-497 — commander option wiring) + §"Pitfall 8" (RESEARCH.md:580-583 — --smoke/--only mutex) + §"Pattern 4" (RESEARCH.md:390-418 — formatError branch)
    - .planning/phases/08-region-capture/08-PATTERNS.md §"src/cli.js" (a/b/c subsections — option declaration, mutex guard, non-smoke branch update) + §"src/cli/format.js" (Guard 4 shape) + §"Shared Patterns" §"Error Dispatcher Branch"
    - .planning/phases/08-region-capture/08-03-SUMMARY.md (confirms runCapture's `only` opt signature and the result array's optional `regionName` field)
    - .planning/phases/06-terminal-ux/06-RESEARCH.md §Pitfall 6 (stdout duplication for pipe-capturability) + §Pitfall 1 (currentSpinner sink)
    - .planning/phases/07-multi-viewport-capture/07-03-PLAN.md §Task 1 (the Phase 7 non-smoke branch shape this plan layers on top of)
  </read_first>
  <behavior>
    - `framershot capture --help` lists `--only <region-name>` as an option (commander auto-generates from the .option call)
    - `framershot capture <config> --only` (no value) prints commander's "option '--only <region-name>' argument missing" error and exits non-zero (angle-bracket = required value)
    - `framershot capture samples/smoke-regions.yaml --only=hero` exits 0 and produces exactly 1 PNG at `/tmp/framershot-smoke-regions/hero-home.png` (cards skipped; full-page skipped per Open Q#1 lock A inverse)
    - `framershot capture samples/smoke-regions.yaml --only=cards` exits 0 and produces exactly 1 PNG at `/tmp/framershot-smoke-regions/cards-home.png` (hero skipped)
    - `framershot capture samples/smoke-regions.yaml --only=nonexistent` exits non-zero and prints an error line starting with red `Error:` whose message contains `Unknown --only region 'nonexistent'` AND lists the declared region names (RegionError surfaces via the new Guard 4)
    - `framershot capture samples/smoke-regions.yaml --smoke --only=hero` exits non-zero and prints an error line with red `Error:` whose message contains `--smoke and --only are mutually exclusive` (the RegionError thrown before either branch runs)
    - `framershot capture samples/smoke.yaml` (v0.1 back-compat path, no `regions:` block, no `--only`) exits 0 and produces exactly 1 PNG with exactly 1 stdout line — unchanged from Phase 7 behavior
    - `framershot capture samples/smoke.yaml --smoke` exits 0 and produces 1 PNG (v0.1 smoke back-compat)
    - `framershot capture samples/smoke-regions.yaml` (no `--only`, regions: declared) exits 0 and produces 3 PNG files: hero region, cards region, AND the full-page (Open Q#1 lock A). Stdout emits 3 lines each labeled appropriately: `screenshot written (region 'hero'): /tmp/.../hero-home.png`, `screenshot written (region 'cards'): /tmp/.../cards-home.png`, `screenshot written (full page): /tmp/.../{region}-home.png` (full-page entry's path contains literal `{region}` because no region arg was passed for full-page — matches Plan 02 template behavior)
    - The stdout line shape `screenshot written (<label>): <path>` is regular and per-result; pipe-capturability preserved (each line on its own; no JSON, no multi-result on one line)
    - `formatError(new RegionError('msg-x'))` returns `${chalk.red('Error:')} msg-x` (no stack trace, no dim body — same shape as Guard 2 ConfigError)
    - `formatError(new BrowserError('y'))` still returns the existing shape (Guard 3 unchanged — Phase 8 only ADDS Guard 4)
    - `formatError(new ConfigError('z'))` still returns the existing shape (Guard 2 unchanged)
  </behavior>
  <action>
    Two files. The changes are surgical and lockstep — they must land together because the CLI throws RegionError from the mutex guard AND from the runCapture call site, and formatError must recognize both throws.

    FILE A — `src/cli/format.js`:

    1. Add the import (alongside the existing ConfigError + BrowserError imports at lines 7-9):
       `import { RegionError } from '../capture/region.js';`

    2. Add Guard 4 BETWEEN the existing Guard 3 (BrowserError, lines 110-116) and the existing bare-TimeoutError guard (lines 118-121). The new guard mirrors Guard 2's shape exactly (no `.cause` hint, no stack trace — RegionError messages are already actionable):
       The full reference branch is in 08-PATTERNS.md §"src/cli/format.js" §"Phase 8 Guard 4 (RegionError)" and RESEARCH.md §"Pattern 4" (RESEARCH.md:406-411). Required shape: a single `if (err instanceof RegionError)` predicate that returns a `${chalk.red('Error:')} ${err.message}` template string. One statement, NO stack-trace concat, NO `.cause` dim hint. Place this branch immediately after the existing BrowserError block and immediately before the existing bare-TimeoutError block.

    3. The existing bare-TimeoutError guard at lines 118-121 (the comment refers to it as "Guard 4" — RENUMBER its inline comment to "Guard 5" so the comment numbering stays aligned with the actual dispatch order). The implementation of that block is unchanged.

    4. Do NOT touch Guards 1-3 (lines 96-116). Do NOT touch the default branch (lines 122-129). Do NOT touch the import-block ordering beyond inserting the new RegionError import alphabetically OR alongside the existing two — pick whichever produces the smaller diff.

    FILE B — `src/cli.js`:

    1. Inside the `capture <config>` command's `.option(...)` chain, add the new option after the existing `--smoke` option (around line 31):
       `.option('--only <region-name>', 'Capture only the named region (skips full-page and other regions)')`
       Angle brackets are mandatory — required value semantics per commander v12 (RESEARCH §Pattern 7 and the in-repo `-p, --port <port>` precedent at src/cli.js:113).

    2. Inside the action handler, BEFORE the existing `if (opts.smoke) { ... }` branch (around line 40), add the mutual-exclusion guard:
       Required shape: a single `if (opts.smoke && opts.only)` predicate that throws `new RegionError('--smoke and --only are mutually exclusive')`. Place this guard immediately BEFORE the existing `if (opts.smoke)` branch.
       Use `RegionError` (NOT a bare `Error`) because the new Guard 4 in formatError produces the clean `Error:` prefix vs the bare-error default (unexpected error: ... + stack body). Per RESEARCH.md:720 the planner chose this for polish. Import RegionError at the top of cli.js:
       `import { RegionError } from './capture/region.js';`

    3. In the NON-SMOKE branch (post-Phase-7 shape; will be around lines 90-107):
       - Change `const results = await runCapture(config, { onProgress: (event) => { ... } });` to `const results = await runCapture(config, { onProgress: (event) => { ... }, only: opts.only });` — add the `only: opts.only` field to the opts bag (it's `undefined` when the flag isn't passed, matching runCapture's default)
       - Change the per-result stdout iteration. Phase 7 emits `for (const { outputPath } of results) console.log(\`screenshot written: \${outputPath}\`);`. Phase 8 changes the loop body to label each result based on `regionName`:
         Required loop body shape (full reference in 08-PATTERNS.md §"src/cli.js" §"(c) Non-smoke branch" and RESEARCH.md §"Code Examples" §"`--only` flag in CLI" RESEARCH.md:731-734): iterate `for (const r of results)`; compute `const label = r.regionName ? \`region '${r.regionName}'\` : 'full page';`; emit `console.log(\`screenshot written (${label}): ${r.outputPath}\`);`. Required behavior: ONE stdout line per result, distinct `(region 'X')` vs `(full page)` label tokens, per-line pipe-capturability preserved (no JSON, no multi-result lines).
       - The `spinner.succeed(\`\${results.length} screenshot(s) written\`)` line stays unchanged (Phase 7 contract).

    4. Do NOT modify the `--smoke` branch (lines 40-83 after Phase 7's `config.viewports[0]` rename). The mutex guard at step 2 above already protects against `--smoke --only` collisions.

    5. Do NOT modify the `serve` or `start` subcommands. Do NOT modify `buildProgram` structure or the module-level `currentSpinner` reference.

    6. Do NOT introduce a new typed error class. RegionError covers both the mutex guard throw AND every region-capture failure (selector/anchor mismatch, unknown --only) from runCapture.
  </action>
  <verify>
    <automated>
# FILE A — src/cli/format.js
grep -c "import { RegionError } from '../capture/region.js'" src/cli/format.js | grep -q "^1$" &&
grep -c "if (err instanceof RegionError)" src/cli/format.js | grep -q "^1$" &&
# Existing Guards 1-3 untouched
grep -c "if (!(err instanceof Error))" src/cli/format.js | grep -q "^1$" &&
grep -c "if (err instanceof ConfigError)" src/cli/format.js | grep -q "^1$" &&
grep -c "if (err instanceof BrowserError)" src/cli/format.js | grep -q "^1$" &&
# formatError unit test for RegionError
node -e "
import('./src/cli/format.js').then(async ({ formatError }) => {
  import('./src/capture/region.js').then(({ RegionError }) => {
    const out = formatError(new RegionError('test message X'));
    if (!out.includes('Error:')) { console.error('FAIL no Error: prefix:', out); process.exit(1); }
    if (!out.includes('test message X')) { console.error('FAIL message dropped:', out); process.exit(1); }
    if (out.includes('Unexpected error:')) { console.error('FAIL fell through to default branch:', out); process.exit(1); }
    if (out.split('\n').length > 1) { console.error('FAIL multi-line output (should be no stack):', out); process.exit(1); }
    console.log('FORMAT GUARD 4 OK');
  });
});
" &&
# FILE B — src/cli.js
grep -c "import { RegionError } from './capture/region.js'" src/cli.js | grep -q "^1$" &&
grep -c "--only <region-name>" src/cli.js | grep -q "^1$" &&
grep -c "opts.smoke && opts.only" src/cli.js | grep -q "^1$" &&
grep -c "only: opts.only" src/cli.js | grep -q "^1$" &&
grep -c "r.regionName ?" src/cli.js | grep -q "^1$" &&
# --help lists --only
HELP=$(node index.js capture --help 2>&1)
echo "$HELP" | grep -q -- "--only" || { echo "FAIL --only not in help: $HELP"; exit 1; }
# --smoke --only mutex
node samples/serve-smoke.js &
SERVER_PID=$!
sleep 1
MUTEX_OUT=$(node index.js capture samples/smoke-regions.yaml --smoke --only=hero 2>&1)
MUTEX_EXIT=$?
test $MUTEX_EXIT -ne 0 || { echo "FAIL --smoke --only should exit non-zero, got 0: $MUTEX_OUT"; kill $SERVER_PID; exit 1; }
echo "$MUTEX_OUT" | grep -q "mutually exclusive" || { echo "FAIL mutex message: $MUTEX_OUT"; kill $SERVER_PID; exit 1; }
echo "$MUTEX_OUT" | grep -q "Error:" || { echo "FAIL no clean Error: prefix: $MUTEX_OUT"; kill $SERVER_PID; exit 1; }
# Unknown --only
UNKNOWN_OUT=$(node index.js capture samples/smoke-regions.yaml --only=nonexistent 2>&1)
UNKNOWN_EXIT=$?
test $UNKNOWN_EXIT -ne 0 || { echo "FAIL --only=nonexistent should exit non-zero, got 0: $UNKNOWN_OUT"; kill $SERVER_PID; exit 1; }
echo "$UNKNOWN_OUT" | grep -q "Unknown --only region 'nonexistent'" || { echo "FAIL unknown message: $UNKNOWN_OUT"; kill $SERVER_PID; exit 1; }
kill $SERVER_PID 2>/dev/null
echo "CLI WIRING OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/format.js` imports `RegionError` from `'../capture/region.js'` (count = 1)
    - `src/cli/format.js` contains `if (err instanceof RegionError)` branch (count = 1)
    - Existing Guards 1-3 (`!(err instanceof Error)`, `err instanceof ConfigError`, `err instanceof BrowserError`) all still present (count = 1 each)
    - `formatError(new RegionError('msg'))` returns a single-line string containing both `Error:` and `msg`, with NO stack trace, NO `Unexpected error:` fallthrough
    - `src/cli.js` imports `RegionError` from `'./capture/region.js'` (count = 1)
    - `src/cli.js` contains the option declaration `--only <region-name>` (count = 1; angle-bracket form)
    - `src/cli.js` contains the mutex guard `opts.smoke && opts.only` (count = 1)
    - `src/cli.js` contains `only: opts.only` in the runCapture call (count = 1)
    - `src/cli.js` contains `r.regionName ?` in the result-iteration loop (count = 1)
    - `framershot capture --help` lists `--only` as an option
    - End-to-end: `--smoke --only=hero` exits non-zero with a `mutually exclusive` Error: message
    - End-to-end: `--only=nonexistent` exits non-zero with an `Unknown --only region 'nonexistent'` Error: message
    - The verify script prints `FORMAT GUARD 4 OK` AND `CLI WIRING OK` with exit 0
  </acceptance_criteria>
  <done>
    The CLI accepts `--only=<name>` on the capture subcommand, rejects the `--smoke + --only` combination cleanly, plumbs the option through to runCapture, and labels each stdout result line based on whether it's a region or full-page capture. The formatError dispatcher recognizes RegionError and produces actionable error output without stack noise.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Eyeball the region PNGs — confirm REGION-01/02/03 end-to-end behavior visually and behaviorally</name>
  <what-built>
    Wave 4 wired the user-facing surface:
    - `framershot capture samples/smoke-regions.yaml` produces 3 PNGs per declared viewport (hero region, cards region, full-page) per Open Q#1 lock A
    - `framershot capture samples/smoke-regions.yaml --only=hero` produces 1 PNG (hero region only)
    - `framershot capture samples/smoke-regions.yaml --only=nonexistent` exits non-zero with a clean RegionError message via formatError Guard 4
    - `framershot capture samples/smoke-regions.yaml --smoke --only=hero` exits non-zero with a `mutually exclusive` error
    - `framershot capture samples/smoke.yaml` (v0.1 back-compat) still produces exactly 1 PNG, exactly 1 stdout line
  </what-built>
  <how-to-verify>
    1. Clean output directory:
       `rm -rf /tmp/framershot-smoke-regions /tmp/framershot-smoke`

    2. Start the fixture server:
       `node samples/serve-smoke.js &`

    3. Run the full smoke-regions capture (no --only):
       `node index.js capture samples/smoke-regions.yaml`
       Confirm:
       - Exit 0
       - Exactly 3 `screenshot written` stdout lines:
         - `screenshot written (region 'hero'): /tmp/framershot-smoke-regions/hero-home.png`
         - `screenshot written (region 'cards'): /tmp/framershot-smoke-regions/cards-home.png`
         - `screenshot written (full page): /tmp/framershot-smoke-regions/{region}-home.png` (full-page path keeps `{region}` literal since the full-page call passes no region arg — matches Plan 02's documented unknown-placeholder posture)
       - All three PNG files exist on disk
       - Spinner showed per-region step labels (e.g., `[smoke] Capturing region 'hero'`)

    4. Inspect the PNGs visually:
       `open /tmp/framershot-smoke-regions/hero-home.png /tmp/framershot-smoke-regions/cards-home.png /tmp/framershot-smoke-regions/\{region\}-home.png`
       Confirm:
       - `hero-home.png` shows ONLY the dark-blue (#036) hero section with ~20 CSS-px of padding around it (40px in raster at DSR 2)
       - `cards-home.png` shows the green→orange gradient region SPANNING the `region-from` green box (top), the 400px gradient spacer (middle), and the `region-to` orange box (bottom), with ~10 CSS-px of padding around the union
       - The full-page PNG (with literal `{region}` in filename) is a tall scrolled-and-stitched capture of the entire fixture page
       - NO ghosted sticky elements, NO half-played animations, NO blank lazy-loaded images in any of the three PNGs (the v0.1 invariants per-region)

    5. Run the `--only=hero` filter:
       `rm -rf /tmp/framershot-smoke-regions`
       `node index.js capture samples/smoke-regions.yaml --only=hero`
       Confirm:
       - Exit 0
       - Exactly 1 stdout line: `screenshot written (region 'hero'): /tmp/framershot-smoke-regions/hero-home.png`
       - ONLY `hero-home.png` exists; no `cards-home.png`, no full-page PNG

    6. Confirm the unknown-region error path:
       `node index.js capture samples/smoke-regions.yaml --only=nonexistent`
       Confirm:
       - Exit non-zero
       - Error message starts with red `Error:` (chalk applied; visible on terminal)
       - Message contains `Unknown --only region 'nonexistent'. Declared regions: 'hero', 'cards'.`
       - No stack trace (single-line error per formatError Guard 4 shape)

    7. Confirm the mutex error path:
       `node index.js capture samples/smoke-regions.yaml --smoke --only=hero`
       Confirm:
       - Exit non-zero
       - Error message starts with red `Error:`
       - Message contains `--smoke and --only are mutually exclusive`

    8. Confirm v0.1 back-compat:
       `rm -rf /tmp/framershot-smoke`
       `node index.js capture samples/smoke.yaml --smoke`
       Confirm:
       - Exit 0
       - `/tmp/framershot-smoke/home.png` exists
       - Exactly 1 `smoke screenshot written` stdout line
       Then:
       `rm -rf /tmp/framershot-smoke`
       `node index.js capture samples/smoke.yaml`
       Confirm:
       - Exit 0
       - Exactly 1 `screenshot written` stdout line (still labeled `(full page)` since no regions declared in smoke.yaml — Phase 8 stdout shape is uniform)

    9. Kill the fixture server: `kill %1`

    If everything above passes, type `approved`. If anything is off (region PNG cropped wrong, padding wrong, --only doesn't filter correctly, unknown error fell through to "Unexpected error:" + stack trace, mutex didn't trigger, v0.1 regressed, etc.), describe what's wrong and I'll diagnose before declaring Phase 8 done.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv (--only string) → runCapture | The `--only` value arrives as a string from commander's parse result. `runCapture` validates it against `config.regions` via `resolveRegions` (Plan 03), throwing `RegionError` on unknown name. The CLI itself does no validation beyond commander's required-value enforcement (angle brackets). |
| RegionError message → terminal | formatError's new Guard 4 renders the message body verbatim through `${chalk.red('Error:')} ${err.message}`. The message is constructed entirely from validated inputs (region names from the validated config, the `--only` value as the user typed it). No injection surface beyond the existing presentation layer. |
| smoke-regions.yaml → schema | Authored YAML; passes through `loadConfig` → `configSchema.safeParse` like any other config. No new untrusted surface; the new fixture is committed source code. |
| samples/serve-smoke.js HTML extensions → fixture page | Static HTML rendered to localhost on 127.0.0.1:7357. No user input reaches the new sections; they're pure DOM-target markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Tampering | --only value injected via shell argv | accept | The `only` string flows to `resolveRegions` for `.find((r) => r.name === only)` comparison and to RegionError messages. No SQL, no shell exec, no eval. Worst case: a crafted long string in the error message is rendered to the user's own terminal. Acceptable for local-only personal CLI tool. |
| T-08-11 | Information Disclosure | RegionError message reveals declared region names | accept | The `Declared regions: 'hero', 'cards'.` portion of the error message reveals config contents to the terminal. Since the same user authored the config AND is running the CLI, this is a non-issue. Local-only personal tool; same user, same machine. |
| T-08-12 | Tampering | path traversal via region name surfacing in output path | mitigate | Plan 02's `slugify` strips structural chars including `/`, `{`, `}`. Verified by the Plan-02 behavior case `'../../etc/passwd' → '--etc-passwd'`. The CLI itself does no path construction; runCapture calls `resolveTemplate` which slugifies. Inherited mitigation. |
| T-08-13 | Denial of Service | crafted region name causing slug explosion | accept | `slugify` is O(N) on input length; commander imposes no per-arg length cap by default. A user passing a 10MB region name would consume memory but harm only themselves. YAGNI for v0.2. |

Local-only, no exploitable surface added by this phase. The fixture server stays bound to 127.0.0.1; all error paths flow through the existing single-error-sink in `index.js`.
</threat_model>

<verification>
The Task 1 automated verify script must print `FIXTURE SCAFFOLD OK` (exit 0). The Task 2 automated verify script must print `FORMAT GUARD 4 OK` AND `CLI WIRING OK` (exit 0). The Task 3 human checkpoint provides the visual + behavioral confirmation that all three REGION requirements work end-to-end and that v0.1 back-compat is unbroken.

Phase 8's overall acceptance evidence (ROADMAP success criteria 1-5) is satisfied by:
- SC #1 (selector region produces PNG, scrolled, prepared, padding) — Task 3 step 4 (`hero-home.png` visual check + padding visible)
- SC #2 (anchor region produces PNG of bounding-box union, clipped) — Task 3 step 4 (`cards-home.png` visual check spanning from→spacer→to)
- SC #3 (`--only=<name>` captures only that region; without flag, v0.1 stitch unchanged) — Task 3 steps 5 + 8 (`--only=hero` produces 1 PNG; `samples/smoke.yaml` v0.1 path still works)
- SC #4 (missing selector / anchor / unknown --only surfaces actionable error) — Task 3 step 6 (`Unknown --only region 'nonexistent'`)
- SC #5 (hermetic smoke fixture demonstrates selector + anchor + --only) — Task 3 steps 3 + 5 (smoke-regions.yaml end-to-end without and with --only)
</verification>

<success_criteria>
- `samples/serve-smoke.js` extended with three new `data-test` sections (region-hero, region-from, region-to)
- `samples/smoke-regions.yaml` created and validates against the Plan 01 schema
- `samples/smoke.yaml` unchanged (v0.1 back-compat fixture preserved)
- `src/cli.js` adds `--only <region-name>` option, mutex guard with `--smoke`, plumbs `only: opts.only` to runCapture, labels per-result stdout lines
- `src/cli/format.js` adds Guard 4 for RegionError; Guards 1-3 unchanged
- All three end-to-end paths produce the expected PNG counts: 3 (no --only), 1 (--only=hero), 0 (--only=nonexistent error path)
- v0.1 back-compat: `samples/smoke.yaml --smoke` AND `samples/smoke.yaml` both still produce exactly 1 PNG
- Human checkpoint confirms PNGs look right visually (hero region cropped + padded, cards region spans union, no ghosting/half-animations/blank lazy-loads)
</success_criteria>

<output>
Create `.planning/phases/08-region-capture/08-04-SUMMARY.md` when done — record:
- Exact stdout line shapes produced by the three test runs (no-only / --only=hero / --only=nonexistent)
- The PNG raster dimensions reported by `file /tmp/framershot-smoke-regions/*.png` (proves DSR 2 was honored end-to-end)
- The human checkpoint's `approved` / diagnosis verdict
- Whether the full-page PNG's filename literally contains `{region}` (the Plan 02 unknown-placeholder posture — expected behavior, not a bug)
</output>