---
phase: 08-region-capture
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/capture/runCapture.js
autonomous: true
requirements: [REGION-01, REGION-02, REGION-03]
requirements_addressed: [REGION-01, REGION-02, REGION-03]

must_haves:
  truths:
    - "`runCapture(config, { onProgress, only })` accepts a new `only` option (string | undefined) plumbed from the CLI's `--only=<region-name>` flag (Plan 04 wires it)"
    - "A helper `resolveRegions(regions, only)` returns the array of regions to capture: all of `regions` when `only === undefined`; a single-element array `[match]` when `only` matches a declared region name; throws `RegionError` when `only` is set but matches nothing (or when `regions` is undefined/empty AND `only` is set)"
    - "Inside the existing Phase 7 viewport loop, after the prepare pipeline runs, a branch chooses between region capture and full-page capture: `if (targets.length === 0) { captureFullPage(...) }` else `for (region of targets) captureRegion(...)`"
    - "When `regions:` is declared in config AND `only === undefined`, BOTH per-region captures AND the full-page capture run for that viewport (per planning_context Open Q#1 lock A — 'capture everything declared'). When `only` is set, ONLY the matched region runs (full-page is skipped)"
    - "Per-region output paths are resolved via `resolveTemplate(config.output, { date, viewport: vp.name, page, region: region.name })` — using the Plan 02 extension"
    - "Per-region `onProgress` events from `captureRegion`'s inner callback are wrapped to inject `viewport: vp.name` scoping, mirroring Phase 7's per-viewport scoping pattern"
    - "Result accumulator entries gain an optional `regionName` field on region-capture entries; full-page entries omit `regionName` (or set it to undefined). Per Phase 8 contract per RESEARCH.md:729-733"
    - "Fail-fast preserved: first region (or full-page) throw aborts the run for the current viewport; the per-viewport try/finally still closes context+browser before the throw propagates; subsequent viewports never run (Phase 7 D-05 locked behavior carry-over)"
    - "Per-viewport `try/finally` lifecycle (context.close() BEFORE browser.close()) is unchanged from Phase 7"
    - "ZERO presentation code added — runCapture stays `console.*`-free, `chalk/ora`-free, `process.exit`-free"
  artifacts:
    - path: "src/capture/runCapture.js"
      provides: "region loop nested inside viewport loop; resolveRegions helper; only-filter validation throwing RegionError; per-region onProgress scoping; array-with-regionName return shape"
      contains: "captureRegion"
  key_links:
    - from: "src/capture/runCapture.js (region branch)"
      to: "src/capture/region.js (captureRegion + RegionError)"
      via: "import { captureRegion, RegionError } from './region.js';"
      pattern: "captureRegion"
    - from: "src/capture/runCapture.js (per-region outputPath)"
      to: "src/output/template.js (resolveTemplate with region arg)"
      via: "resolveTemplate(config.output, { date, viewport: vp.name, page, region: region.name })"
      pattern: "region: region\\.name"
    - from: "src/capture/runCapture.js (only-filter)"
      to: "src/capture/region.js (RegionError thrown for unknown --only)"
      via: "resolveRegions(config.regions, only) — throws RegionError when only !== undefined and no match"
      pattern: "RegionError"
---

<objective>
Wire region capture into the existing seam — `src/capture/runCapture.js`. Add an `only` option to the runCapture signature, add a `resolveRegions(regions, only)` helper that validates the CLI `--only` flag against declared regions (throwing RegionError on unknown name), and nest a region loop INSIDE Phase 7's per-viewport loop. The branch shape: when `targets.length === 0` (no regions declared AND no --only filter), full-page capture runs unchanged; when targets are present, each target runs as a region capture; when regions ARE declared but `--only` is absent, BOTH per-region captures AND a full-page capture run for the viewport (per the locked Open Q#1 disposition A — "capture everything declared").

Purpose: Per RESEARCH §Architectural Responsibility Map, the region loop orchestration belongs in `runCapture` — alongside the viewport loop Phase 7 already placed there. Pushing the loop into the seam keeps the CLI/server adapters dumb (they consume the same array-of-results contract Phase 7 established, with the addition of an optional `regionName` field on entries). The `resolveRegions` helper centralizes the `--only` validation at the earliest legal point: schema time can't see the CLI flag (RESEARCH §"Architectural Responsibility Map" row 8), so runCapture's startup is the right boundary.

Output: One file touched (`src/capture/runCapture.js`). The region loop nests inside the viewport loop; per-region `onProgress` events carry `viewport: vp.name` scoping; results array gains optional `regionName` on region entries. CLI/server wiring and fixture creation deferred to Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-region-capture/RESEARCH.md
@.planning/phases/08-region-capture/08-PATTERNS.md
@.planning/phases/08-region-capture/08-VALIDATION.md
@.planning/phases/08-region-capture/08-01-SUMMARY.md
@.planning/phases/08-region-capture/08-02-SUMMARY.md
@.planning/phases/07-multi-viewport-capture/07-CONTEXT.md
@.planning/phases/07-multi-viewport-capture/07-02-PLAN.md
@src/capture/runCapture.js
@src/capture/index.js

<interfaces>
<!-- Current state and post-Phase-7 expected shape -->

Current `runCapture` signature (src/capture/runCapture.js:34):
- `runCapture(config, { onProgress = () => {} } = {})`
- v0.1 returns: `Promise<{ outputPath, hideSummary }>`
- Post-Phase-7 returns: `Promise<Array<{ outputPath, hideSummary, viewportName }>>`
- Post-Phase-8 (this plan) returns: `Promise<Array<{ outputPath, hideSummary, viewportName, regionName? }>>`
  - `regionName` is set on region-capture entries; undefined/omitted on full-page entries
  - Phase 7 PATTERNS line 145 documents the extension-via-optional-field strategy

Phase 7 viewport loop body (post Phase 7 Plan 02, see 07-02-PLAN §Task 2):
```
const results = [];
const date = new Date().toISOString().slice(0, 10);
const page = config.page.name;
for (const vp of config.viewports) {
  const outputPath = resolveTemplate(config.output, { date, viewport: vp.name, page });
  onProgress({ type: 'step', viewport: vp.name, label: 'Launching Chromium' });
  const { browser, context } = await launchBrowser(config, vp);
  try {
    // install animation guards
    // navigate
    // prepare pipeline
    // emit warnings if any
    // capture full page
    await captureFullPage(navigatedPage, outputPath, { onProgress: (current, total) => onProgress({ type: 'frame', viewport: vp.name, current, total }) });
    results.push({ outputPath, hideSummary, viewportName: vp.name });
  } finally {
    await context.close();
    await browser.close();
  }
}
return results;
```

Phase 8 region branch (REPLACES the single `captureFullPage` call site inside the viewport loop; see 08-PATTERNS §"src/capture/runCapture.js" + RESEARCH §"Pattern 6"):
```
const targets = resolveRegions(config.regions, only);  // [] when no regions and no --only
if (targets.length === 0) {
  // Full-page path — unchanged from Phase 7. Either: no regions: block at all, OR Phase 8 OQ #1 lock A path when --only matches nothing (but that case throws RegionError in resolveRegions, so reaches here only when regions undefined AND only undefined).
  await captureFullPage(navigatedPage, outputPath, { onProgress: (current, total) => onProgress({ type: 'frame', viewport: vp.name, current, total }) });
  results.push({ outputPath, hideSummary, viewportName: vp.name });
} else {
  for (const region of targets) {
    const regionPath = resolveTemplate(config.output, { date, viewport: vp.name, page, region: region.name });
    await captureRegion(navigatedPage, region, regionPath, { onProgress: (event) => onProgress({ ...event, viewport: vp.name }) });
    results.push({ outputPath: regionPath, hideSummary, viewportName: vp.name, regionName: region.name });
  }
  // Open Q#1 lock A: when regions[] declared AND --only is undefined, ALSO capture full page.
  if (only === undefined && config.regions !== undefined) {
    await captureFullPage(navigatedPage, outputPath, { onProgress: (current, total) => onProgress({ type: 'frame', viewport: vp.name, current, total }) });
    results.push({ outputPath, hideSummary, viewportName: vp.name });
  }
}
```

`resolveRegions` helper (lives inside runCapture.js OR colocated in region.js — planner's call; this plan keeps it in runCapture.js for cohesion with the only-filter validation which is purely orchestration-layer concern):
```
function resolveRegions(regions, only) {
  if (only === undefined) {
    return regions ?? [];  // no filter; capture all declared regions (or empty if none declared)
  }
  const declared = regions ?? [];
  const match = declared.find((r) => r.name === only);
  if (!match) {
    const names = declared.map((r) => `'${r.name}'`).join(', ') || '<no regions declared>';
    throw new RegionError(`Unknown --only region '${only}'. Declared regions: ${names}.`);
  }
  return [match];
}
```

Wave 1 (Plan 01) post-condition: `config.regions` is either `undefined` (no regions: block in config) OR an array of validated region entries (each with `name`, `padding` defaulted to 0, AND either `selector` OR (`from` + `to`)). The schema's `{region}`-in-`output` cross-field refinement guarantees that when this plan reaches the region path, the template ALREADY contains `{region}` — so `resolveTemplate(...{ region: region.name })` produces distinct paths per region.

Wave 2 (Plan 02) post-condition: `src/capture/region.js` exports `captureRegion` and `RegionError`. `src/output/template.js` `resolveTemplate` accepts the `region` arg.

Phase 7 D-05 locked behavior (fail-fast sequential) carries forward unchanged — the throw from `captureRegion` propagates through the inner for-loop, out to the viewport-loop's per-iteration `try/finally` (which closes that iteration's context+browser), and bubbles to the caller. The accumulated `results` array is NOT returned on failure (matches v0.1 single-screenshot behavior shape).

Pattern 6 / Open Q#4 lock: regions × viewports = full cartesian product. `--only` filters regions only, not viewports. A 2-viewport config with `--only=hero` produces 2 PNGs (hero × desktop, hero × mobile), NOT 1. The viewport loop and region loop interact correctly by virtue of the nested structure: outer iterates viewports, inner iterates resolved targets.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend runCapture's signature with the `only` option, add resolveRegions helper, and nest region loop + Open-Q#1-A full-page branch inside the existing per-viewport loop</name>
  <files>src/capture/runCapture.js</files>
  <read_first>
    - src/capture/runCapture.js (current state — Phase 7's viewport loop body is the foundation; Phase 8 inserts inside the try block)
    - .planning/phases/08-region-capture/RESEARCH.md §"System Architecture Diagram" (RESEARCH.md:132-216), §"Pattern 6" (RESEARCH.md:440-466 — cartesian product loop shape), §"Pattern 7" (RESEARCH.md:468-497 — resolveRegions validation), §"Open Question #1" lock A (planning_context: regions + no --only = capture everything declared), §"Open Question #4" lock (per-viewport, not per-only-viewport), §"Anti-Patterns to Avoid" (especially "Mutating the config object to filter regions when `--only` is used")
    - .planning/phases/08-region-capture/08-PATTERNS.md §"src/capture/runCapture.js" + §"Shared Patterns" §"Phase 7 Array-Return Contract (inherited unchanged)" + §"Single Error Sink in `index.js` (fail-fast)"
    - .planning/phases/08-region-capture/08-02-SUMMARY.md (confirms `captureRegion` + `RegionError` exports from `src/capture/region.js`)
    - .planning/phases/08-region-capture/08-01-SUMMARY.md (confirms `config.regions` shape post-validation)
    - .planning/phases/07-multi-viewport-capture/07-02-PLAN.md §Task 2 (the existing per-viewport loop structure this plan extends)
    - .planning/phases/06-terminal-ux/06-RESEARCH.md §Pitfall 5 (context-close-before-browser-close — preserved per viewport)
  </read_first>
  <behavior>
    - `runCapture(config, { onProgress, only })` accepts a third destructured option `only: string | undefined`; default is `undefined`
    - `runCapture` signature declared arity (`runCapture.length`) is still 1 (rest in destructured opts) — no breaking change to the caller surface
    - When `config.regions === undefined` AND `only === undefined`: behavior is identical to Phase 7 (one full-page capture per viewport; results array length = `config.viewports.length`; each entry has `{ outputPath, hideSummary, viewportName }` with no `regionName`)
    - When `config.regions === [{ name: 'hero', selector: '...', padding: 0 }]` AND `only === undefined`: PER VIEWPORT, captures one region PNG (`regionName: 'hero'`) AND one full-page PNG (no `regionName`). Results length = `viewports.length × (regions.length + 1)` (Open Q#1 lock A)
    - When `config.regions === [{ name: 'hero', ... }, { name: 'cards', ... }]` AND `only === 'hero'`: PER VIEWPORT, captures ONLY the `hero` region PNG (skips the `cards` region AND skips full-page). Results length = `viewports.length × 1`. Each result has `regionName: 'hero'`
    - When `only === 'nonexistent'` (no matching region in `config.regions`): `runCapture` throws `RegionError` before launching any browser. Message: `Unknown --only region 'nonexistent'. Declared regions: 'hero', 'cards'.` (when regions are declared) OR `Unknown --only region 'nonexistent'. Declared regions: <no regions declared>.` (when `config.regions` is undefined or empty)
    - The `RegionError` from the `only`-validation path is the SAME class exported from `src/capture/region.js` (`err instanceof RegionError` is true; `err.name === 'RegionError'`)
    - Region capture call site: `await captureRegion(navigatedPage, region, regionPath, { onProgress: (event) => onProgress({ ...event, viewport: vp.name }) })` — events from inside `captureRegion` are wrapped to inject `viewport` scoping (matches Phase 7's per-viewport wrapping for captureFullPage frame events)
    - Per-region `onProgress` step events emitted by `captureRegion` (e.g., `{ type: 'step', label: "Capturing region 'hero'" }`) reach the caller's `onProgress` as `{ type: 'step', label: "Capturing region 'hero'", viewport: <vp.name> }`
    - Per-viewport `try/finally` lifecycle unchanged: `await context.close(); await browser.close();` per iteration, in that order (Pitfall 5 invariant)
    - Fail-fast: if `captureRegion` throws in iteration N, the throw propagates out, `finally` closes N's context+browser, the outer for-loop terminates, and iterations N+1...M never run; the caller never sees a partial array
    - The configuration object is NOT mutated when `only` is set — `resolveRegions` returns a NEW array, leaving `config.regions` intact for subsequent viewport iterations (Anti-Pattern from RESEARCH "Mutating the config object to filter regions" avoided)
    - ZERO new `console.*` calls, ZERO `chalk`/`ora` imports, ZERO `process.exit` calls in `runCapture.js` (presentation-free contract from the file's own header comment block preserved)
  </behavior>
  <action>
    Edit `src/capture/runCapture.js`. Build on the Phase 7 Plan-02 viewport loop structure; the changes are additive: new import, new option, new helper function, new branch inside the existing try block, one extra accumulator push.

    1. Imports — add to the existing import block:
       - `import { captureRegion, RegionError } from './region.js';`
       Keep all existing imports (launchBrowser, navigateToPage, installAnimationGuards, runPreparePipeline, captureFullPage, resolveTemplate) unchanged.

    2. Add `resolveRegions(regions, only)` as a module-level helper (NOT exported — internal to runCapture orchestration). Place it ABOVE the `runCapture` export (between the imports and the JSDoc preamble). The full reference implementation is documented in `08-PATTERNS.md` §"src/capture/runCapture.js" §"`resolveRegions` helper" and RESEARCH.md §"Pattern 7" (RESEARCH.md:488-497). Required behavior: `only === undefined` returns `regions ?? []` (nullish-coalesce to empty); `only` non-undefined searches `regions ?? []` via `.find((r) => r.name === only)`; on miss, throws `new RegionError(...)` whose message starts with `Unknown --only region '<only>'. Declared regions: <names>.` where `<names>` is either a comma-joined list of declared names quoted with single quotes OR the sentinel `<no regions declared>` when the declared list is empty. Add a brief JSDoc documenting return shape and the RegionError throw conditions.

    3. Extend `runCapture` signature — change the opts destructure from `{ onProgress = () => {} } = {}` to `{ onProgress = () => {}, only } = {}`. The default for `only` is `undefined` (not a string), matching the CLI's "flag absent" state.

    4. Validate `--only` upfront — INSIDE the runCapture body, BEFORE the viewport loop. Call `resolveRegions(config.regions, only)` once at the top to fail-fast on an unknown `--only` name before launching any browser. Discard the return value at this position — the per-viewport iteration will recompute it (cheap, and avoids closing over a variable that's only needed inside the loop). The call site is a single statement `resolveRegions(config.regions, only);` placed immediately after the function-prologue `const` declarations (`date`, `page`, `results`) and before `for (const vp of config.viewports) {`.

    5. Inside the existing per-viewport loop body — AFTER the prepare pipeline runs (after `runPreparePipeline`, after the optional `hide-missed` warning emit, but BEFORE the existing `captureFullPage` call) — REPLACE the existing single `captureFullPage(navigatedPage, outputPath, { onProgress })` + `results.push({ outputPath, hideSummary, viewportName: vp.name })` block with the Phase 8 branch. The full reference implementation is in `08-PATTERNS.md` §"src/capture/runCapture.js" (the "Phase 8 region branch" code block, ~30 lines) and RESEARCH.md §"Pattern 6" (RESEARCH.md:447-463).

       Required branch shape:
       - Compute `const targets = resolveRegions(config.regions, only);` at the start of the replaced block.
       - Branch on `targets.length === 0`:
         - TRUE branch (back-compat / v0.1+Phase 7 path): emit one `{ type: 'step', viewport: vp.name, label: 'Capturing frame 0/?' }` event, then `await captureFullPage(navigatedPage, outputPath, { onProgress })` where the inner onProgress wraps `{ type: 'frame', viewport: vp.name, current, total }` (identical to Phase 7 Plan 02's call shape), then `results.push({ outputPath, hideSummary, viewportName: vp.name })`.
         - FALSE branch (region path): `for (const region of targets)` loop. Each iteration: resolve per-region path via `resolveTemplate(config.output, { date, viewport: vp.name, page, region: region.name })` (note the `region:` field — uses Plan 02's extension); call `await captureRegion(navigatedPage, region, regionPath, { onProgress: (event) => onProgress({ ...event, viewport: vp.name }) })` (event spread + viewport tag injection); push `{ outputPath: regionPath, hideSummary, viewportName: vp.name, regionName: region.name }` to results. After the for-loop, the Open Q#1 lock A tail: `if (only === undefined && config.regions !== undefined)` then emit step event + run `captureFullPage` + push the full-page result (same call shape as the TRUE branch above).

       The `outputPath` referenced in the full-page branches is the SAME variable Phase 7's viewport loop already resolves at the top of the iteration via `resolveTemplate(config.output, { date, viewport: vp.name, page })` (no `region` arg passed — `{region}` placeholder, if present in the template, stays literal per Plan 02's resolveTemplate extension). This is the desired behavior: full-page PNGs in regions+full-page mode write to a path that visibly contains `{region}` literal — distinguishing them from per-region PNGs and matching the documented unknown-placeholder posture. (If the user wants a cleaner full-page path while regions are also declared, they remove `{region}` from the template — but the schema's `{region}` requirement when regions are declared means this is a deliberate placement decision.)

    6. Update the JSDoc on `runCapture`:
       - Add `@param {string} [opts.only] — when set, only the named region runs (skips full-page and other regions per Plan 04's --only CLI flag wiring). When unset, all declared regions PLUS full-page run per viewport.`
       - Update `@returns` to reflect the new optional `regionName` field: `Promise<Array<{ outputPath: string, hideSummary: { matched: number, missed: string[] }, viewportName: string, regionName?: string }>>`
       - Update the `@param opts.onProgress` JSDoc to mention the new `step` event shape from captureRegion: `{ type: 'step', viewport, label: "Capturing region '<name>'" }`

    7. Do NOT add presentation code — runCapture stays `console.*`-free, `chalk`/`ora`-free, `process.exit`-free per the file's existing header comment block (lines 1-16). Do NOT introduce any new error class — `RegionError` (already exported from `src/capture/region.js`) is the only typed error this plan touches.

    8. Do NOT change the per-viewport `try/finally` block ordering. The `await context.close(); await browser.close();` invariant (Pitfall 5) is preserved verbatim per iteration.

    9. Do NOT mutate `config.regions` when filtering for `--only` — `resolveRegions` returns a new array. Mutation would pollute subsequent viewport iterations (RESEARCH §"Anti-Patterns to Avoid": "Mutating the config object to filter regions when `--only` is used").

    10. Phase 7 dependency: this plan assumes `src/capture/runCapture.js` already has the per-viewport loop from Phase 7 Plan 02 in place (the `for (const vp of config.viewports)` structure, the array `results` accumulator, the per-viewport `launchBrowser(config, vp)` call, per-viewport scoped events). If Phase 7 has NOT yet landed at the implementation moment, that's a sequencing problem the executor should surface — Phase 8 cannot proceed before Phase 7 Plan 02 because there is no viewport-loop to nest inside. The plan's `depends_on: ["08-01", "08-02"]` is correct WITHIN Phase 8, but cross-phase dependency on Phase 7 is implicit per ROADMAP "Depends on Phase 7" and RESEARCH §"Assumptions Log" A1.
  </action>
  <verify>
    <automated>
# Static checks — module shape
grep -c "import { captureRegion, RegionError } from './region.js'" src/capture/runCapture.js | grep -q "^1$" &&
grep -c "function resolveRegions" src/capture/runCapture.js | grep -q "^1$" &&
grep -c "^export async function runCapture" src/capture/runCapture.js | grep -q "^1$" &&
grep -c "only } = {}" src/capture/runCapture.js | grep -q "^1$" &&
grep -c "captureRegion(navigatedPage" src/capture/runCapture.js | grep -q "^1$" &&
grep -c "region: region.name" src/capture/runCapture.js | grep -q "^1$" &&
grep -c "regionName: region.name" src/capture/runCapture.js | grep -q "^1$" &&
# fail-fast --only validation appears BEFORE the for-loop (upfront resolveRegions call)
grep -c "resolveRegions(config.regions, only)" src/capture/runCapture.js | grep -q "^2$" &&
# presentation-free contract preserved
grep -v "^\s*\(//\|\*\)" src/capture/runCapture.js | grep -c "console\." | grep -q "^0$" &&
grep -v "^\s*\(//\|\*\)" src/capture/runCapture.js | grep -c "process\.exit" | grep -q "^0$" &&
grep -c "from 'chalk'" src/capture/runCapture.js | grep -q "^0$" &&
grep -c "from 'ora'" src/capture/runCapture.js | grep -q "^0$" &&
# config mutation anti-pattern check — no config.regions = ... assignment
grep -v "^\s*\(//\|\*\)" src/capture/runCapture.js | grep -c "config\.regions\s*=" | grep -q "^0$" &&
# Module imports cleanly
node -e "
import('./src/capture/runCapture.js').then(({ runCapture }) => {
  if (typeof runCapture !== 'function') { console.error('FAIL runCapture not a function'); process.exit(1); }
  console.log('RUNCAPTURE IMPORT OK');
}).catch(e => { console.error('FAIL import', e.message); process.exit(1); });
" &&
# Unit test: resolveRegions throws RegionError on unknown --only without launching anything
node -e "
import('./src/capture/runCapture.js').then(async ({ runCapture }) => {
  import('./src/capture/region.js').then(async ({ RegionError }) => {
    // Construct minimal config; --only validation runs BEFORE browser launch so we don't need a fixture server
    const minimalConfig = {
      name: 'p8-w3-only-validation',
      baseUrl: 'http://127.0.0.1:7357',
      output: '/tmp/discard-{region}-{page}.png',
      deviceScaleFactor: 2,
      viewports: [{ name: 'd', width: 800, height: 600 }],
      page: { path: '/', name: 'home' },
      prepare: { animations: true, hide: [], scrollPrime: false, extraDelay: 0 },
      regions: [{ name: 'hero', selector: '.x', padding: 0 }],
    };
    try {
      await runCapture(minimalConfig, { onProgress: () => {}, only: 'nonexistent' });
      console.error('FAIL --only=nonexistent should have thrown RegionError'); process.exit(1);
    } catch (err) {
      if (!(err instanceof RegionError)) { console.error('FAIL not RegionError:', err.name, err.message); process.exit(1); }
      if (!err.message.includes(\"Unknown --only region 'nonexistent'\")) { console.error('FAIL message:', err.message); process.exit(1); }
      if (!err.message.includes(\"'hero'\")) { console.error('FAIL message missing declared region name:', err.message); process.exit(1); }
    }
    // Validate the empty-declared case
    const noRegionsConfig = { ...minimalConfig, output: '/tmp/discard-{page}.png', regions: undefined };
    try {
      await runCapture(noRegionsConfig, { onProgress: () => {}, only: 'anything' });
      console.error('FAIL --only with no regions: declared should have thrown'); process.exit(1);
    } catch (err) {
      if (!(err instanceof RegionError)) { console.error('FAIL not RegionError (no-regions):', err.name, err.message); process.exit(1); }
      if (!err.message.includes('<no regions declared>')) { console.error('FAIL no-regions message:', err.message); process.exit(1); }
    }
    console.log('RUNCAPTURE ONLY-VALIDATION OK');
  });
});
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/capture/runCapture.js` imports `captureRegion` AND `RegionError` from `'./region.js'` (one import statement, both names)
    - `src/capture/runCapture.js` contains a `function resolveRegions` declaration (count = 1)
    - The runCapture opts destructure includes `only` (matches `only } = {}` in the signature)
    - `resolveRegions(config.regions, only)` appears exactly TWICE: once at the top of runCapture for fail-fast validation, once inside the viewport loop for per-iteration target resolution
    - `captureRegion(navigatedPage, ...` appears exactly once (inside the region for-loop)
    - `region: region.name` appears in the resolveTemplate call once (for the per-region path)
    - `regionName: region.name` appears in the results.push call once (region-mode accumulator entry)
    - Non-comment lines contain zero `console.*` calls, zero `process.exit` calls, zero `chalk`/`ora` imports (presentation-free contract preserved)
    - No `config.regions = ...` assignment (config-mutation anti-pattern avoided)
    - Module imports cleanly: `node -e "import('./src/capture/runCapture.js')"` does not throw
    - `--only=nonexistent` test: `runCapture` throws `RegionError` whose `.message` contains `Unknown --only region 'nonexistent'` AND lists the declared region name `'hero'` (validates the names-list formatting in resolveRegions)
    - `--only=anything` with no `regions:` declared: `runCapture` throws `RegionError` whose message contains `<no regions declared>` (validates the empty-declared edge case)
    - Both validation throws happen BEFORE any browser launch (the test reaches them without a fixture server running; if a browser launch were attempted, the test would hang or fail with a connection error to 127.0.0.1:7357)
    - The verify script prints `RUNCAPTURE IMPORT OK` AND `RUNCAPTURE ONLY-VALIDATION OK` with exit 0
  </acceptance_criteria>
  <done>
    `runCapture` accepts an `only` option, validates it against `config.regions` upfront (throwing RegionError before any browser launch), and inside the per-viewport loop branches between region capture and full-page capture per the Open Q#1 lock A disposition. Per-region events are scoped with `viewport: vp.name`. Results entries gain optional `regionName`. The full end-to-end region capture against a real Playwright page is verified in Plan 04's hermetic fixture.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/server `only` flag → runCapture | The `only` string arrives unvalidated from the CLI's `--only` flag (Plan 04). `resolveRegions` validates it at runCapture's startup — throwing `RegionError` for unknown names. No untrusted-input surface added beyond the existing CLI flag layer; the schema layer already validates `config.regions` itself in Wave 1. |
| validated config.regions → per-iteration loop | Each region entry was validated by Plan 01's zod schema. `runCapture` does not re-validate; it passes entries directly to `captureRegion`. The contract between schema and runCapture is the same as v0.1's contract between schema and the original single-flow body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | shared `config.regions` array across viewport iterations | mitigate | `resolveRegions` returns a NEW array (never mutates `config.regions`). Verified by the absence of `config.regions = ...` assignments in source (`grep -c "config\\.regions\\s*=" == 0`). Multi-viewport runs see the SAME declared region list per iteration. |
| T-08-08 | Denial of Service | unknown --only causes wasted work | mitigate | Upfront `resolveRegions(config.regions, only)` call at runCapture startup throws `RegionError` BEFORE launching any Chromium process. Verified by the test cases in the verify script (both throws complete without contacting 127.0.0.1:7357). |
| T-08-09 | Tampering | per-region path overwrite collision | mitigate | Schema (Plan 01) enforces `{region}` placeholder in `output` when `regions:` is declared. `resolveTemplate` with the `region` arg per Plan 02 substitutes distinct names per iteration. The cartesian-product loop in this plan respects that — N viewports × M regions = N×M distinct paths. |

Local-only, no exploitable surface added by this phase. Fail-fast preserves v0.1's behavior shape; presentation-free contract preserved.
</threat_model>

<verification>
The Task 1 verify script must print BOTH `RUNCAPTURE IMPORT OK` AND `RUNCAPTURE ONLY-VALIDATION OK` with exit 0. End-to-end region capture against a live Playwright page (proving the selector/anchor branches actually produce PNGs at template-resolved paths, that the cartesian product works correctly, and that the regions+full-page Open Q#1 lock A behavior produces N+1 PNGs per viewport) is verified in Plan 04's hermetic fixture — this wave establishes the orchestration shape and the upfront-validation contract.

Optional cross-check: confirm Phase 7's v0.1/v0.2 back-compat is unbroken (no `regions:` block in config → full-page path runs exactly as Phase 7 shipped it):

```bash
node samples/serve-smoke.js &
SERVER_PID=$!
sleep 1
rm -rf /tmp/framershot-smoke
node index.js capture samples/smoke.yaml --smoke
SMOKE_EXIT=$?
kill $SERVER_PID 2>/dev/null
test $SMOKE_EXIT -eq 0 && echo "PHASE 8 WAVE 3 BACK-COMPAT: ok" || echo "WARN: smoke regression"
```

This is informational only — if `--smoke` regresses, the cause is likely Phase 7's wiring (Plan 07-03 reads `config.viewports[0]` in the smoke branch), not Phase 8's region work.
</verification>

<success_criteria>
- `runCapture` accepts a new `only` option, defaulting to `undefined`
- `resolveRegions` helper exists in `src/capture/runCapture.js` (not exported; orchestration-internal)
- The region branch nests INSIDE the existing per-viewport loop; full-page branch unchanged when no regions declared AND no --only filter
- When regions are declared AND `--only` is unset: per viewport, captures all regions PLUS a full-page (Open Q#1 lock A)
- When `--only=<name>` matches a declared region: per viewport, captures ONLY that region; full-page skipped
- Unknown `--only` name throws `RegionError` BEFORE any Chromium launch (upfront validation)
- Per-region `onProgress` events are wrapped to include `viewport: vp.name`
- Results array entries gain optional `regionName` on region entries; full-page entries omit it
- ZERO presentation code added (no console.*, no chalk/ora, no process.exit)
- `config.regions` is never mutated
- The Task 1 verify script passes with exit 0
</success_criteria>

<output>
Create `.planning/phases/08-region-capture/08-03-SUMMARY.md` when done — record:
- The exact shape of the modified runCapture body (where the region branch sits relative to the prepare pipeline + the original captureFullPage call)
- Confirmation of the Open Q#1 lock A behavior: regions declared + no --only = N+1 captures per viewport (N regions + 1 full-page)
- Whether `resolveRegions` lives in runCapture.js (planner's spec) or was hoisted to region.js (alternative; note rationale if so)
</output>