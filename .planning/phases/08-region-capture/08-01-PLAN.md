---
phase: 08-region-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/schema.js
autonomous: true
requirements: [REGION-01, REGION-02, REGION-03]
requirements_addressed: [REGION-01, REGION-02, REGION-03]

must_haves:
  truths:
    - "A YAML config with `regions: [{ name, selector, padding? }]` (selector mode) validates and exposes `config.regions` as an array of `{ name, selector, padding }` entries"
    - "A YAML config with `regions: [{ name, from, to, padding? }]` (anchor mode) validates and exposes `config.regions` as an array of `{ name, from, to, padding }` entries"
    - "Mixed regions array (some selector entries + some anchor entries) validates entry-by-entry — each entry independently passes the selector-XOR-(from+to) gate"
    - "A region entry containing BOTH `selector:` AND `from:`/`to:` fails validation with a ConfigError naming the region (e.g. \"region 'hero': use 'selector' OR 'from'+'to', not both\")"
    - "A region entry containing NEITHER `selector:` NOR `from:`/`to:` fails validation with a ConfigError naming the region"
    - "A region entry with `from:` but no `to:` (or vice versa) fails validation with a ConfigError indicating both anchor halves are required"
    - "A `regions:` array with duplicate `name` values fails validation with a ConfigError that names the duplicate (e.g. \"regions: duplicate name 'hero'\")"
    - "A config with `regions:` present whose `output:` template does NOT contain `{region}` fails validation with a ConfigError pointing at `output` (overwrite-prevention per Pattern 5)"
    - "A v0.1/v0.2-shaped config WITHOUT a `regions:` block validates unchanged — `config.regions` is `undefined` (back-compat, REQUIREMENTS coverage carry-forward)"
    - "Padding defaults to `0` when omitted on a region entry — the zod `.default(0)` is the source of truth"
  artifacts:
    - path: "src/config/schema.js"
      provides: "regionSchema (single shape with .superRefine selector-XOR-anchor gate + padding default), root-level regions field (optional array), root .superRefine for {region}-in-output cross-field check + duplicate-name uniqueness"
      contains: "regionSchema"
  key_links:
    - from: "src/config/schema.js (regionSchema)"
      to: "src/config/schema.js (configSchema root .superRefine)"
      via: "root cross-field refinement that requires {region} placeholder in output when regions[] is present"
      pattern: "\\{region\\}"
    - from: "src/config/schema.js (regionSchema .superRefine)"
      to: "src/config/schema.js (formatZodError catch-all at lines 67-86)"
      via: ".superRefine emits issue.code === 'custom' which formatZodError's catch-all renders as `${field}: ${issue.message}` — no formatZodError changes required"
      pattern: "z\\.ZodIssueCode\\.custom"
---

<objective>
Lock the v0.2 region-capture config shape at the schema boundary so every downstream consumer (runCapture, captureRegion, output/template) reads ONE normalized region shape: `config.regions` as an optional array of entries where each entry has either `selector` OR (`from` + `to`), a unique `name`, and a `padding` defaulting to `0`. Enforce the `{region}` placeholder requirement on `output` at validation time so the cartesian product of regions × viewports cannot overwrite itself.

Purpose: Per RESEARCH §Architectural Responsibility Map, region schema validation belongs in `src/config/schema.js` — the existing zod-refinement landing spot. Phase 7 D-02 established the precedent for schema-enforced uniqueness as the only mechanism preventing PNG overwrites. Pushing all region invariants into the schema keeps `runCapture` and `captureRegion` branch-free for region-shape concerns (they only have to branch on selector-vs-anchor at capture time, with full confidence the shape is already validated).

Output: Extended `src/config/schema.js` with `regionSchema` (selector-XOR-(from+to) `.superRefine`, padding default), root-level `regions: z.array(regionSchema).optional()`, root-level `.superRefine` for `{region}`-in-`output` cross-field check + duplicate-name uniqueness check. `formatZodError` unchanged. v0.1/v0.2 configs without `regions:` block continue to validate unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-region-capture/RESEARCH.md
@.planning/phases/08-region-capture/08-PATTERNS.md
@.planning/phases/08-region-capture/08-VALIDATION.md
@.planning/phases/07-multi-viewport-capture/07-CONTEXT.md
@.planning/phases/07-multi-viewport-capture/07-01-PLAN.md
@src/config/schema.js
@samples/smoke.yaml

<interfaces>
<!-- Current shape of src/config/schema.js — extract from the file before modifying -->

Existing `viewportSchema` (lines 9-13) — base shape precedent for new region schemas. Same `z.object` + `z.number().int().positive()` posture:
- `viewportSchema = z.object({ width: positive int, height: positive int, name: z.string().min(1).optional() })`

Existing `prepareSchema` with `.default({})` (lines 22-34) — the precedent for "default-when-omitted" YAML ergonomics. Phase 8's `padding` uses the same `.default()` pattern at the scalar level (`padding: z.number().int().min(0).default(0)`).

Existing `.refine` style precedent (lines 42-47, only `.refine` in the codebase — message-shape sets the precedent for new refinements):
- `baseUrl: z.string().url().refine((u) => u.startsWith('http://') || u.startsWith('https://'), { message: 'baseUrl must use http or https' })`
- Pattern: predicate returning boolean + `{ message: '<field>: <human reason>' }`

Existing root `configSchema` (lines 36-54) — Phase 7's plan (07-01) ALSO extends this with a root-level refinement+transform (viewport/viewports mutual exclusivity + singular→plural normalize). Phase 8 chains an ADDITIONAL `.superRefine` on top of Phase 7's transform output for the `{region}`-in-`output` check. Order matters: Phase 7's transform runs FIRST (produces `config.viewports`), then Phase 8's region cross-field refinement runs on the transformed shape. In zod 3, `.transform(...).superRefine(...)` chains correctly — refinements after a transform see the transformed value.

Existing `formatZodError` catch-all (lines 67-86):
- Refinement issues use `issue.code === 'custom'` and surface via `return ${field}: ${issue.message}` — no formatZodError changes needed.
- `.superRefine` issues with explicit `path: [...]` set the `<field>` part; issues with no path get `<root>`.

Existing `ResolvedConfig` typedef (line 56):
- `@typedef {z.infer<typeof configSchema>} ResolvedConfig`
- After this plan, `z.infer` reflects the new optional `regions` field; the typedef name stays.

Pattern 5 mandate (RESEARCH.md:825-834 + 08-PATTERNS.md): when `regions:` is present AND `output` lacks `{region}`, every region writes to the same path. Must be a `ConfigError` at load time, not a runtime overwrite.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regionSchema with selector-XOR-(from+to) superRefine, padding default, and unique-name + {region}-in-output root refinements</name>
  <files>src/config/schema.js</files>
  <read_first>
    - src/config/schema.js (the whole file — preserves existing patterns, especially the formatZodError catch-all)
    - .planning/phases/08-region-capture/RESEARCH.md §"Pattern 5" (RESEARCH.md:420-438) + §"Updated zod schema sketch" (RESEARCH.md:769-834) + §"Architectural Responsibility Map" rows for schema validation/normalization
    - .planning/phases/08-region-capture/08-PATTERNS.md §"src/config/schema.js" (analog mapping: viewportSchema/prepareSchema/.refine precedent + formatZodError catch-all behavior)
    - .planning/phases/07-multi-viewport-capture/07-01-PLAN.md (sibling refinement+transform pattern; Phase 8 chains AFTER Phase 7's transform)
    - samples/smoke.yaml + samples/sample.yaml (v0.1-shaped configs that MUST still validate — neither has a regions: block)
  </read_first>
  <behavior>
    - Selector region entry `{ name: 'hero', selector: '.hero', padding: 20 }` parses and `padding` is `20` (explicit value preserved)
    - Selector region entry `{ name: 'hero', selector: '.hero' }` parses and `padding` is `0` (default applied)
    - Anchor region entry `{ name: 'cards', from: '#a', to: '#b' }` parses; `padding` defaults to `0`
    - Mixed array `[{ name: 'a', selector: '.x' }, { name: 'b', from: '#p', to: '#q', padding: 5 }]` parses; both entries valid
    - Entry with BOTH `selector` and `from`/`to` set REJECTS with message `region 'X': use 'selector' OR 'from'+'to', not both`
    - Entry with NEITHER `selector` nor `from`/`to` REJECTS with message `region 'X': must declare 'selector' OR both 'from' and 'to'`
    - Entry with `from` set but no `to` REJECTS with message `region 'X': 'from' and 'to' must both be set` (half-anchor case)
    - Entry with `to` set but no `from` REJECTS with same half-anchor message
    - `regions: [{ name: 'h', selector: '.x' }, { name: 'h', selector: '.y' }]` REJECTS with `regions: duplicate name 'h'`
    - `regions: [...]` with non-empty length AND `output: '/tmp/{page}.png'` (no `{region}`) REJECTS with message naming `output` and explaining `{region}` requirement
    - `regions: [...]` AND `output: '/tmp/{region}-{page}.png'` validates (placeholder present)
    - Config WITHOUT a `regions:` field validates and `result.data.regions` is `undefined` (back-compat — no `.default([])`, must stay undefined so downstream can distinguish "no regions block" from "empty regions block")
    - `formatZodError` source UNCHANGED — every new failure case surfaces through the existing catch-all
  </behavior>
  <action>
    Edit src/config/schema.js. Add the new schemas ABOVE `configSchema` (alongside `viewportSchema` / `prepareSchema` — same locality). Then extend `configSchema` with the optional `regions` field and chain a `.superRefine` on the root for the `{region}`-in-`output` check.

    1. Define `regionSchema` as a `z.object` with these fields (per RESEARCH.md:786-792, adapted to Phase 8's superRefine recommendation at RESEARCH.md:109):
       - `name: z.string().min(1)` — REQUIRED, non-empty
       - `selector: z.string().min(1).optional()`
       - `from: z.string().min(1).optional()`
       - `to: z.string().min(1).optional()`
       - `padding: z.number().int().min(0).default(0)` — scalar padding, default 0 (per RESEARCH §Pattern 3; asymmetric padding deferred per planning_context Open Q#2 lock)

       Chain a `.superRefine((data, ctx) => {...})` on `regionSchema` that enforces selector-XOR-(from+to) per RESEARCH.md:792-812:
       - Compute `hasSelector = data.selector !== undefined`, `hasAnchor = data.from !== undefined && data.to !== undefined`, `halfAnchor = (data.from !== undefined) !== (data.to !== undefined)`
       - If `hasSelector && (hasAnchor || halfAnchor)`: push `{ code: z.ZodIssueCode.custom, path: ['<name>'], message: \`region '\${data.name}': use 'selector' OR 'from'+'to', not both\` }` — use the region's `name` literal in `path` so `formatZodError` surfaces it as the field token (the planner's chose path-uses-name approach mirrors how Phase 7's duplicate-name issue uses `path: ['viewports']`). Acceptable alternative: empty `path: []` — then `<root>: region '<name>': ...`. Choose the cleaner one; either passes the behavior list.
       - Else if `!hasSelector && !hasAnchor`: push `{ code: 'custom', path: ['<name>'], message: \`region '\${data.name}': must declare 'selector' OR both 'from' and 'to'\` }`
       - Else if `halfAnchor`: push `{ code: 'custom', path: ['<name>'], message: \`region '\${data.name}': 'from' and 'to' must both be set\` }`

    2. Add `regions: z.array(regionSchema).optional()` to the existing `configSchema` (insert AFTER `viewport` per the existing field-order convention; no `.default([])` — back-compat requires `undefined` for omitted-block to distinguish from explicit-empty-block).

    3. Chain a root-level `.superRefine((data, ctx) => {...})` on `configSchema` that runs AFTER Phase 7's transform (Phase 7's transform produces normalized `data.viewports` + `data.regions`; Phase 8's refinement reads both):
       - Add duplicate-name check: if `data.regions !== undefined`, build `names = data.regions.map(r => r.name)`; if `new Set(names).size !== names.length`, find the first duplicate and push `{ code: z.ZodIssueCode.custom, path: ['regions'], message: \`duplicate name '\${dupName}'\` }` — matches Phase 7's `duplicate name 'desktop'` message shape so the existing `formatZodError` catch-all renders it as `regions: duplicate name 'hero'`.
       - Add `{region}`-in-`output` check: if `data.regions !== undefined && data.regions.length > 0 && !data.output.includes('{region}')`, push `{ code: z.ZodIssueCode.custom, path: ['output'], message: 'template must contain {region} when regions are declared (to avoid overwrites)' }`.

    4. If Phase 7 has NOT landed yet (Phase 7 is in flight per ROADMAP), the existing `configSchema` is the v0.1 shape (no `.transform()`). Phase 8's `.superRefine` still chains correctly — it runs after whatever sits in front of it. When Phase 7 ships, the Phase 7 `.transform()` lands BEFORE this Phase 8 `.superRefine`, and the chain order remains valid in zod 3. No coordination problem: this plan only modifies the schema body and tails a superRefine on the END.

    5. Update the JSDoc above `configSchema` (or add a brief inline comment near the new `regions` field) noting that `regions` is optional, that each entry must be selector-XOR-(from+to), and that the output template must contain `{region}` when any regions are declared.

    6. Do NOT introduce any new error type (RESEARCH §Pattern 4 confirms `RegionError` is a runtime error class for capture-time failures, NOT for schema-time failures — ConfigError covers all schema failures).

    7. Do NOT touch `formatZodError` — its catch-all (lines 83-84) already handles `z.ZodIssueCode.custom` issues correctly.

    8. Do NOT touch any other field of `configSchema`. The Phase 8 schema change is purely additive: one new `regions` field + one new root `.superRefine`.

    Run `node -e "import('./src/config/schema.js').then(m => console.log(Object.keys(m)))"` to confirm the module still parses; full proof lives in the verify script below.
  </action>
  <verify>
    <automated>
node -e "
import('./src/config/schema.js').then(async ({ configSchema, formatZodError }) => {
  const baseValid = {
    name: 'p', baseUrl: 'http://x.test',
    viewport: { width: 800, height: 600, name: 'smoke' },
    page: { path: '/', name: 'home' },
    prepare: { animations: true, hide: [], scrollPrime: true, extraDelay: 0 },
  };
  // 1. v0.1 back-compat: no regions block validates unchanged
  const v01 = configSchema.safeParse({ ...baseValid, output: '/tmp/{page}.png' });
  if (!v01.success) { console.error('FAIL v0.1-back-compat', v01.error.issues); process.exit(1); }
  if (v01.data.regions !== undefined) { console.error('FAIL regions-should-be-undefined-when-omitted', v01.data.regions); process.exit(1); }
  // 2. selector region validates + padding defaults to 0
  const sel = configSchema.safeParse({ ...baseValid, output: '/tmp/{region}-{page}.png', regions: [{ name: 'hero', selector: '.hero' }] });
  if (!sel.success) { console.error('FAIL selector-region', sel.error.issues); process.exit(1); }
  if (sel.data.regions[0].padding !== 0) { console.error('FAIL padding-default', sel.data.regions[0]); process.exit(1); }
  // 3. padding explicit value preserved
  const padded = configSchema.safeParse({ ...baseValid, output: '/tmp/{region}-{page}.png', regions: [{ name: 'hero', selector: '.hero', padding: 20 }] });
  if (!padded.success || padded.data.regions[0].padding !== 20) { console.error('FAIL padding-explicit', padded.error?.issues ?? padded.data); process.exit(1); }
  // 4. anchor region validates
  const anc = configSchema.safeParse({ ...baseValid, output: '/tmp/{region}-{page}.png', regions: [{ name: 'cards', from: '#a', to: '#b' }] });
  if (!anc.success) { console.error('FAIL anchor-region', anc.error.issues); process.exit(1); }
  // 5. mixed array validates
  const mix = configSchema.safeParse({ ...baseValid, output: '/tmp/{region}-{page}.png', regions: [{ name: 'a', selector: '.x' }, { name: 'b', from: '#p', to: '#q', padding: 5 }] });
  if (!mix.success || mix.data.regions.length !== 2) { console.error('FAIL mixed', mix.error?.issues ?? mix.data); process.exit(1); }
  // 6. selector + anchor → reject with both-not-allowed message
  const both = configSchema.safeParse({ ...baseValid, output: '/tmp/{region}-{page}.png', regions: [{ name: 'hero', selector: '.x', from: '#p', to: '#q' }] });
  if (both.success) { console.error('FAIL both-rejected'); process.exit(1); }
  const bothLines = formatZodError(both.error);
  if (!bothLines.some(l => l.includes(\"not both\") && l.includes(\"'hero'\"))) { console.error('FAIL both-message', bothLines); process.exit(1); }
  // 7. neither → reject
  const neither = configSchema.safeParse({ ...baseValid, output: '/tmp/{region}-{page}.png', regions: [{ name: 'oops' }] });
  if (neither.success) { console.error('FAIL neither-rejected'); process.exit(1); }
  const neitherLines = formatZodError(neither.error);
  if (!neitherLines.some(l => l.includes(\"must declare 'selector' OR\") && l.includes(\"'oops'\"))) { console.error('FAIL neither-message', neitherLines); process.exit(1); }
  // 8. half-anchor (from without to) → reject
  const half = configSchema.safeParse({ ...baseValid, output: '/tmp/{region}-{page}.png', regions: [{ name: 'half', from: '#only-from' }] });
  if (half.success) { console.error('FAIL half-rejected'); process.exit(1); }
  const halfLines = formatZodError(half.error);
  if (!halfLines.some(l => l.includes(\"'from' and 'to'\") && l.includes(\"'half'\"))) { console.error('FAIL half-message', halfLines); process.exit(1); }
  // 9. duplicate names → reject with field-named message
  const dup = configSchema.safeParse({ ...baseValid, output: '/tmp/{region}-{page}.png', regions: [{ name: 'h', selector: '.x' }, { name: 'h', selector: '.y' }] });
  if (dup.success) { console.error('FAIL dup-rejected'); process.exit(1); }
  const dupLines = formatZodError(dup.error);
  if (!dupLines.some(l => l.includes(\"duplicate name 'h'\"))) { console.error('FAIL dup-message', dupLines); process.exit(1); }
  // 10. regions present but output lacks {region} → reject pointing at output
  const noPlaceholder = configSchema.safeParse({ ...baseValid, output: '/tmp/{page}.png', regions: [{ name: 'hero', selector: '.hero' }] });
  if (noPlaceholder.success) { console.error('FAIL no-placeholder-rejected'); process.exit(1); }
  const npLines = formatZodError(noPlaceholder.error);
  if (!npLines.some(l => l.startsWith('output:') && l.includes('{region}'))) { console.error('FAIL no-placeholder-message', npLines); process.exit(1); }
  // 11. regions present AND output has {region} → validates
  const ok = configSchema.safeParse({ ...baseValid, output: '/tmp/{region}-{page}.png', regions: [{ name: 'hero', selector: '.hero' }] });
  if (!ok.success) { console.error('FAIL final-ok', ok.error.issues); process.exit(1); }
  // 12. existing v0.1 sample fixtures still parse
  const { default: yaml } = await import('js-yaml');
  const fs = await import('node:fs');
  for (const path of ['samples/smoke.yaml', 'samples/sample.yaml']) {
    const parsed = yaml.load(fs.readFileSync(path, 'utf8'));
    const r = configSchema.safeParse(parsed);
    if (!r.success) { console.error('FAIL fixture-regression', path, r.error.issues); process.exit(1); }
    if (r.data.regions !== undefined) { console.error('FAIL fixture-regions-leaked', path, r.data.regions); process.exit(1); }
  }
  console.log('SCHEMA OK');
});
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/config/schema.js` contains the identifier `regionSchema` (grep ≥ 1)
    - `src/config/schema.js` contains the substring `duplicate name` (matches Phase 7's message shape)
    - `src/config/schema.js` contains the substring `{region}` (the output-placeholder check message)
    - `src/config/schema.js` contains `padding: z.number().int().min(0).default(0)` (or equivalent — grep for `.default(0)` near `padding`)
    - `src/config/schema.js` adds the `regions` field to `configSchema` (grep for `regions: z.array`)
    - `formatZodError` source is unchanged (the body between `export function formatZodError` and its closing `}` matches HEAD modulo whitespace — `git diff src/config/schema.js -- :^src/config/schema.js | grep -A40 "function formatZodError" | head -40` should show no semantic change in that function)
    - The verify script above prints `SCHEMA OK` with exit 0
    - `node -e "import('./src/config/schema.js')"` does not throw
    - Both `samples/smoke.yaml` and `samples/sample.yaml` continue to parse successfully and produce `result.data.regions === undefined`
  </acceptance_criteria>
  <done>
    `regionSchema` enforces selector-XOR-(from+to), defaults `padding` to 0, and rejects half-anchors with actionable messages. The root `configSchema` accepts an optional `regions[]` array, rejects duplicate region names, rejects configs that declare regions but lack `{region}` in `output`, and continues to validate every v0.1-shaped config unchanged. Every assertion in the verify script passes; `formatZodError` is byte-untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file → schema validation | User-authored config file is parsed by `js-yaml` (safe by default) and validated by zod before any downstream consumer reads it. This is the only untrusted-input surface added by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | region entries (selector/from/to strings, padding) | mitigate | zod enforces `selector` / `from` / `to` as non-empty strings; `padding` as non-negative int; selector-XOR-anchor refinement rejects malformed entries; duplicate-name refinement rejects overwriting configurations. Surface verified by the per-entry behavior table in Task 1. |
| T-08-02 | Tampering | region `name` used in output path slugification (path-traversal `../`) | mitigate | Handled downstream by `slugify` in `src/output/template.js:14` (existing SLUG_RE strips structural chars including `/`, `{`, `}`, `..` segments collapse to `--`). No schema-layer change needed; this is the existing posture from planning_context security threat model. |
| T-08-03 | Denial of Service | regions[] array length (1000-entry user config) | accept | Local-only personal CLI tool, no exploitable surface added by this phase. User authoring a large array harms only themselves; YAGNI for a `.max()` cap. Matches Phase 7's T-07-02 disposition. |

Local-only, no exploitable surface added by this phase. Schema work is pure input validation; the existing `formatZodError` + `ConfigError` dispatch (Phase 6) already ensures no raw zod internals leak to stdout/stderr.
</threat_model>

<verification>
The Task 1 verify script must print `SCHEMA OK` with exit 0. No other end-to-end check is required at this wave — `src/config/load.js` is unchanged (zod validation absorbs the new schema automatically per RESEARCH §Code Touchpoints line 1000), and runtime region-capture behavior is verified by Plan 02 and Plan 04.

Optional regression check (informational, not blocking):

```bash
node samples/serve-smoke.js &
SERVER_PID=$!
sleep 1
node index.js capture samples/smoke.yaml --smoke
SMOKE_EXIT=$?
kill $SERVER_PID 2>/dev/null
test $SMOKE_EXIT -eq 0 && echo "PHASE 8 WAVE 1 BACK-COMPAT: ok" || echo "WARN: smoke regression (likely Phase 7 wiring drift, not Phase 8 schema)"
```
</verification>

<success_criteria>
- `src/config/schema.js` exports `regionSchema` (named) and an extended `configSchema` accepting an optional `regions[]` array
- `regionSchema` uses `.superRefine` to enforce selector-XOR-(from+to) with field-named error messages
- Root-level `.superRefine` enforces region-name uniqueness AND `{region}`-in-`output` cross-field requirement
- `formatZodError` source unchanged — every Phase 8 refinement message surfaces through the existing catch-all
- v0.1 sample fixtures (`smoke.yaml`, `sample.yaml`) still validate unchanged with `result.data.regions === undefined`
- The Task 1 verify script passes with exit 0 and prints `SCHEMA OK`
</success_criteria>

<output>
Create `.planning/phases/08-region-capture/08-01-SUMMARY.md` when done — record the exact path-token choice for the per-entry superRefine (named region vs empty path) and confirmation that `formatZodError` is byte-untouched.
</output>