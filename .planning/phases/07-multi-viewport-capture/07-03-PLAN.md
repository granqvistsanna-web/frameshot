---
phase: 07-multi-viewport-capture
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/cli.js
  - src/server/index.js
autonomous: true
requirements: [MULTI-01]
requirements_addressed: [MULTI-01]

must_haves:
  truths:
    - "`framershot capture <multi-viewport-config>` (non-smoke) prints one `screenshot written: <path>` stdout line per viewport in array order"
    - "`framershot capture <singular-viewport-config>` (non-smoke, v0.1 shape) still prints exactly one `screenshot written: <path>` line (back-compat — the singular config is normalized to a one-element viewports array upstream)"
    - "`framershot capture <singular-viewport-config> --smoke` (v0.1 shape) still produces exactly one PNG (the smoke path takes `config.viewports[0]` post-normalization)"
    - "While `runCapture` is running, the ora spinner text shows a per-viewport prefix (e.g. `[desktop] Capturing frame 12/14`) so the user can tell which viewport is in flight"
    - "The selector-warning sandwich (spinner.stop → printSelectorWarnings → spinner.start) still works on per-viewport `warning` events"
    - "POST /api/capture's SSE `done` frame returns `{ type: 'done', outputs: [{ outputPath, urlPath, viewportName }, ...] }` (array shape) — UI form-builder shape is UNCHANGED in this phase per D-05"
    - "POST /api/capture's SSE `step`/`frame`/`warning` events continue to flow through `send(event)` verbatim — they already carry `viewport: <name>` per Plan 02"
  artifacts:
    - path: "src/cli.js"
      provides: "non-smoke branch consumes array return; --smoke branch reads config.viewports[0]; spinner text gains per-viewport prefix"
      contains: "results.length"
    - path: "src/server/index.js"
      provides: "handleCapture consumes array return; done-frame payload becomes outputs: [...]"
      contains: "outputs"
  key_links:
    - from: "src/cli.js (non-smoke)"
      to: "src/capture/runCapture.js"
      via: "const results = await runCapture(config, { onProgress })"
      pattern: "const results = await runCapture"
    - from: "src/cli.js (--smoke)"
      to: "src/browser/launcher.js"
      via: "launchBrowser(config, config.viewports[0])"
      pattern: "launchBrowser\\(config,\\s*config\\.viewports\\[0\\]\\)"
    - from: "src/server/index.js handleCapture"
      to: "src/capture/runCapture.js"
      via: "const results = await runCapture(parsed.data, { onProgress: send })"
      pattern: "const results = await runCapture"
---

<objective>
Adapt the two `runCapture` consumers (CLI ora adapter + server SSE adapter) to the new array-returning shape from Plan 02. Per D-05, the web UI form stays single-viewport — the server-side wiring update is bookkeeping only (the schema's singular→plural normalizer converts the form payload before runCapture sees it). The `--smoke` branch in the CLI updates to read `config.viewports[0]` because the schema transform absorbed the singular `viewport:` key.

Purpose: Both adapters were structurally identical in v0.1 (consume `{ outputPath }` from runCapture, present it). Plan 02 changed the return shape; this plan updates both adapters in lockstep. Both files have ZERO overlap, so this plan owns both — keeping them in one plan ensures a single commit captures the lockstep migration without leaving either consumer half-migrated.

Output: Multi-viewport `framershot capture` runs print one stdout line per viewport; the spinner shows per-viewport progress; the server's SSE `done` frame returns all output URLs; v0.1 singular configs (smoke + non-smoke) still work end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-multi-viewport-capture/07-CONTEXT.md
@.planning/phases/07-multi-viewport-capture/07-PATTERNS.md
@.planning/phases/07-multi-viewport-capture/07-01-SUMMARY.md
@.planning/phases/07-multi-viewport-capture/07-02-SUMMARY.md
@src/cli.js
@src/server/index.js
@src/cli/format.js
@samples/smoke.yaml
@.planning/phases/06-terminal-ux/06-RESEARCH.md

<interfaces>
<!-- Current adapter shapes — extract before modifying -->

CLI `--smoke` branch (src/cli.js:40-83):
- `const viewport = config.viewport.name ?? 'default';` (line 46) — UPDATE to `config.viewports[0].name`
- `const { browser, context } = await launchBrowser(config);` (line 51) — UPDATE to `launchBrowser(config, config.viewports[0])`
- Rest unchanged.

CLI non-smoke branch (src/cli.js:85-108):
- `const { outputPath } = await runCapture(config, { onProgress: ... })` — UPDATE to `const results = await runCapture(...)`
- `spinner.succeed(\`screenshot written: \${outputPath}\`)` — UPDATE for N results
- `console.log(\`screenshot written: \${outputPath}\`)` — UPDATE to one stdout line per result
- onProgress handler maps `event.type` to spinner.text — gains per-viewport prefix in the label

Server `handleCapture` (src/server/index.js:99-162):
- Form-builder shape (lines 115-123) — UNCHANGED per D-05 (still `viewport: body.viewport`, the schema normalizes)
- SSE pump (lines 152-156): `const { outputPath } = await runCapture(...)` + `send({ type: 'done', outputPath, urlPath: ... })` — UPDATE to consume array + emit `outputs: [{ outputPath, urlPath, viewportName }, ...]`
- `outputPathToUrl` (lines 164-170) — UNCHANGED (called per-output in the new mapping)
- `errorToMessage` (lines 172-180) — UNCHANGED (fail-fast: first per-viewport error is the only error)
- `send` helper (lines 148-150) — UNCHANGED

Single-error-sink contract (index.js top-level catch):
- Throws from inside `runCapture`'s loop bubble out of `program.parseAsync` and into `index.js`'s catch, which calls `getCurrentSpinner()?.fail()` and `formatError(err)`. This plan changes NOTHING about that path — fail-fast just means iteration N+1..M never run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update src/cli.js — non-smoke branch consumes array return with per-viewport spinner prefix; --smoke branch reads config.viewports[0]</name>
  <files>src/cli.js</files>
  <read_first>
    - src/cli.js (the whole file — preserves currentSpinner/getCurrentSpinner contract, both branches)
    - src/cli/format.js (makeProgress + printSelectorWarnings — the selector-warning sandwich pattern is unchanged)
    - .planning/phases/07-multi-viewport-capture/07-PATTERNS.md §"src/cli.js"
    - .planning/phases/07-02-SUMMARY.md (confirms the array return shape + event scoping)
    - .planning/phases/06-terminal-ux/06-RESEARCH.md §Pitfall 1 (currentSpinner sink), §Pitfall 2 (warning sandwich), §Pitfall 6 (stdout duplicate)
  </read_first>
  <action>
    Edit src/cli.js. Two surgical updates, both inside the `capture <config>` action handler.

    --smoke BRANCH (currently lines 40-83):
    - Line 46: change `const viewport = config.viewport.name ?? 'default';` to `const viewport = config.viewports[0].name;` (name is required on plural entries post-Wave-1; no fallback needed)
    - Line 51: change `const { browser, context } = await launchBrowser(config);` to `const { browser, context } = await launchBrowser(config, config.viewports[0]);` (two-arg form from Plan 02)
    - Everything else in the smoke branch stays as-is. The smoke path is deliberately documented as taking the FIRST viewport only — it's the v0.1 back-compat verification path (single PNG out).
    - Add a one-line comment near line 46 explaining the choice: `// smoke takes the first viewport only — see Phase 7 PLAN-03 / ROADMAP success criterion #4 (v0.1 back-compat)`

    NON-SMOKE BRANCH (currently lines 90-107):
    - Change `const { outputPath } = await runCapture(...)` (line 90) to `const results = await runCapture(...)`.
    - Inside the `onProgress` callback (lines 91-101): every event now carries `event.viewport`. Update spinner.text construction to include a per-viewport prefix. Concretely:
      - `if (event.type === 'step') spinner.text = \`[\${event.viewport}] \${event.label}\`;`
      - `else if (event.type === 'frame') spinner.text = \`[\${event.viewport}] Capturing frame \${event.current}/\${event.total}\`;`
      - The `warning` branch keeps the sandwich pattern unchanged (`spinner.stop()` → `printSelectorWarnings({ matched: 0, missed: event.selectors })` → `spinner.start()`). The warning event itself carries `event.viewport` but the existing `printSelectorWarnings` helper only consumes `{ matched, missed }` — no signature change is required here; the per-viewport tag is informational and surfaced via the spinner.text resumed after the sandwich.
    - After the await: change the success line emission. `spinner.succeed(\`\${results.length} screenshot(s) written\`)` is the new spinner-line; then iterate `for (const { outputPath } of results) console.log(\`screenshot written: \${outputPath}\`);` to emit one stdout line per viewport (matches §Pitfall 6 pipe-capturability — each line is individually pipe-friendly).
    - Reset `currentSpinner = null;` AFTER `spinner.succeed` and BEFORE the for-of stdout loop (matches the existing pattern at line 105).

    The top-level catch in index.js is unchanged. If iteration N inside `runCapture` throws, the rejected promise from `runCapture` propagates out, the action handler's await rejects, commander's `parseAsync` rejects, and index.js's `.catch` handles it via `getCurrentSpinner()?.fail()` + `formatError(err)` + `process.exit(1)`. Fail-fast at the seam means the for-of stdout loop after the await never runs on failure — exactly the v0.1 single-screenshot behavior shape.

    Do NOT add a new typed error (D-05 locked-behavior). Do NOT change `serve` command or `buildProgram`'s structure.
  </action>
  <verify>
    <automated>
      # Static checks
      grep -c "config.viewports\[0\]" src/cli.js | grep -q "^2$" &&
      grep -c "launchBrowser(config, config.viewports\[0\])" src/cli.js | grep -q "^1$" &&
      grep -c "const results = await runCapture" src/cli.js | grep -q "^1$" &&
      grep -c "for (const { outputPath } of results)" src/cli.js | grep -q "^1$" &&
      grep -v "^\s*\(//\|\*\)" src/cli.js | grep -c "config\.viewport\." | grep -q "^0$" &&
      # End-to-end #1: v0.1 singular smoke still works (back-compat — success criterion #4)
      node samples/serve-smoke.js &
      SERVER_PID=$!
      sleep 1
      rm -rf /tmp/framershot-smoke
      node index.js capture samples/smoke.yaml --smoke
      SMOKE_EXIT=$?
      kill $SERVER_PID 2>/dev/null
      test $SMOKE_EXIT -eq 0 || { echo "FAIL v0.1 smoke regressed"; exit 1; }
      ls /tmp/framershot-smoke/home.png || { echo "FAIL v0.1 smoke PNG missing"; exit 1; }
      # End-to-end #2: v0.1 singular non-smoke still works (back-compat)
      node samples/serve-smoke.js &
      SERVER_PID=$!
      sleep 1
      rm -rf /tmp/framershot-smoke
      OUTPUT=$(node index.js capture samples/smoke.yaml 2>&1)
      NONSMOKE_EXIT=$?
      kill $SERVER_PID 2>/dev/null
      test $NONSMOKE_EXIT -eq 0 || { echo "FAIL v0.1 non-smoke regressed: $OUTPUT"; exit 1; }
      LINES=$(echo "$OUTPUT" | grep -c "^screenshot written: ")
      test "$LINES" -eq 1 || { echo "FAIL expected 1 stdout line for v0.1 singular, got $LINES: $OUTPUT"; exit 1; }
      echo "CLI BACK-COMPAT OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/cli.js` contains `config.viewports[0]` exactly 2 times (in --smoke branch: once for the viewport-name lookup, once in the launchBrowser call)
    - `src/cli.js` contains `launchBrowser(config, config.viewports[0])` exactly once
    - `src/cli.js` contains `const results = await runCapture` exactly once
    - `src/cli.js` contains `for (const { outputPath } of results)` exactly once
    - Non-comment lines of `src/cli.js` contain ZERO occurrences of `config.viewport.` (singular) — confirmed by `grep -v '^\s*\(//\|\*\)' src/cli.js | grep -c 'config\.viewport\.' == 0`
    - End-to-end: `node index.js capture samples/smoke.yaml --smoke` exits 0 and `/tmp/framershot-smoke/home.png` exists (success criterion #4 — v0.1 smoke back-compat)
    - End-to-end: `node index.js capture samples/smoke.yaml` (non-smoke, single-viewport) exits 0 and emits exactly one `screenshot written: ` stdout line (v0.1 non-smoke back-compat)
    - The verify script prints `CLI BACK-COMPAT OK` with exit 0
  </acceptance_criteria>
  <done>
    Both CLI branches (smoke + non-smoke) work against the post-Wave-1/2 shape. v0.1 singular configs still pass end-to-end. The spinner shows per-viewport progress. The non-smoke branch emits one pipe-capturable stdout line per viewport.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update src/server/index.js — handleCapture's SSE pump consumes the array return; done-frame payload becomes outputs: [{...}]; UI form unchanged</name>
  <files>src/server/index.js</files>
  <read_first>
    - src/server/index.js (the whole file — confirms `send` helper, `outputPathToUrl`, `errorToMessage`, path-traversal guard all stay untouched)
    - .planning/phases/07-multi-viewport-capture/07-PATTERNS.md §"src/server/index.js"
    - .planning/phases/07-multi-viewport-capture/07-CONTEXT.md §Decisions D-05 (UI surface unchanged in this phase)
    - .planning/phases/07-02-SUMMARY.md (confirms onProgress events already carry `viewport: <name>` — server-side send() passes them through verbatim)
  </read_first>
  <action>
    Edit src/server/index.js. ONE surgical change inside `handleCapture` (currently lines 99-162); everything else stays untouched.

    Inside the `try { ... } catch { ... } finally { ... }` block (lines 152-161), change:
    - `const { outputPath } = await runCapture(parsed.data, { onProgress: (event) => send(event) });`
    - `send({ type: 'done', outputPath, urlPath: outputPathToUrl(outputPath) });`

    To:
    - `const results = await runCapture(parsed.data, { onProgress: (event) => send(event) });`
    - `send({ type: 'done', outputs: results.map(({ outputPath, viewportName }) => ({ outputPath, urlPath: outputPathToUrl(outputPath), viewportName })) });`

    DO NOT touch:
    - The form-builder block (lines 115-123). `body.viewport` (singular object) stays — the schema's singular→plural normalizer (Plan 01) converts it. Per D-05, the UI doesn't learn multi-viewport in this phase.
    - The `send` helper (lines 148-150) — same SSE frame format.
    - `outputPathToUrl` (lines 164-170) — same path-to-URL mapping, called once per output via the `.map(...)` above.
    - `errorToMessage` (lines 172-180) — fail-fast: the first per-viewport error becomes the SSE `error` frame, exactly matching v0.1's behavior shape.
    - The path-traversal guard in `handleStaticScreenshot` (lines 182-213) — unchanged.
    - The `127.0.0.1`-only bind (line 46) — unchanged. Local-only invariant preserved.

    The `step`/`frame`/`warning` SSE events already carry `viewport: <name>` per Plan 02 — `send(event)` passes them through unchanged. Browser clients (the existing UI) ignore unknown fields in events, so this is non-breaking for the current UI even though the UI doesn't currently render per-viewport progress. The UI rendering work is explicitly deferred per D-05.
  </action>
  <verify>
    <automated>
      # Static checks
      grep -c "const results = await runCapture(parsed.data" src/server/index.js | grep -q "^1$" &&
      grep -c "outputs: results.map" src/server/index.js | grep -q "^1$" &&
      # Form-builder block stays singular per D-05
      grep -c "viewport: body.viewport" src/server/index.js | grep -q "^1$" &&
      # 127.0.0.1 bind preserved
      grep -c "server.listen(port, '127.0.0.1'" src/server/index.js | grep -q "^1$" &&
      # End-to-end: spin up the server, POST a multi-viewport candidate via the UI's payload shape, confirm done-frame is the array form
      node samples/serve-smoke.js &
      FIXTURE_PID=$!
      sleep 1
      node -e "
      import('./src/server/index.js').then(async ({ startServer }) => {
        // Server blocks forever; launch in background via spawn instead.
      });
      " &
      # easier: use index.js with the serve command
      node index.js serve --port 5174 &
      SERVER_PID=$!
      sleep 2
      # POST a singular-viewport request (UI form shape) — should still work post-D-05 wiring
      RESP=\$(node -e \"
      const http = require('node:http');
      const body = JSON.stringify({
        name: 'ui-singular-test',
        baseUrl: 'http://127.0.0.1:7357',
        deviceScaleFactor: 2,
        viewport: { width: 800, height: 600, name: 'web' },
        page: { path: '/', name: 'home' },
        prepare: { animations: true, hide: [], scrollPrime: false, extraDelay: 100 },
      });
      const req = http.request({ host: '127.0.0.1', port: 5174, path: '/api/capture', method: 'POST', headers: { 'content-type': 'application/json', 'content-length': Buffer.byteLength(body) } }, (res) => {
        let buf = '';
        res.on('data', (c) => { buf += c.toString('utf8'); });
        res.on('end', () => { process.stdout.write(buf); });
      });
      req.write(body); req.end();
      \")
      kill $SERVER_PID 2>/dev/null
      kill $FIXTURE_PID 2>/dev/null
      # Check the SSE stream contains a done frame with the new outputs: [...] shape
      echo \"\$RESP\" | grep -q '\"type\":\"done\"' || { echo \"FAIL no done frame: \$RESP\"; exit 1; }
      echo \"\$RESP\" | grep -q '\"outputs\":\\[' || { echo \"FAIL done frame missing outputs array: \$RESP\"; exit 1; }
      echo \"\$RESP\" | grep -q '\"viewportName\":\"web\"' || { echo \"FAIL viewportName not surfaced: \$RESP\"; exit 1; }
      echo "SERVER BACK-COMPAT OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/server/index.js` contains `const results = await runCapture(parsed.data` exactly once
    - `src/server/index.js` contains `outputs: results.map` exactly once
    - `src/server/index.js` contains `viewport: body.viewport` exactly once (form-builder shape preserved per D-05)
    - `src/server/index.js` still binds to `127.0.0.1` (line still reads `server.listen(port, '127.0.0.1', ...)`)
    - `src/server/index.js` has zero changes outside `handleCapture` (verified by `git diff src/server/index.js` showing changes only between the `async function handleCapture` declaration and its closing brace)
    - End-to-end SSE: POSTing a singular-viewport candidate to `/api/capture` produces a `done` frame whose payload is `{ type: 'done', outputs: [{ outputPath, urlPath, viewportName: 'web' }] }` (array of length 1 — the singular form is normalized upstream)
    - The verify script prints `SERVER BACK-COMPAT OK` with exit 0
  </acceptance_criteria>
  <done>
    `handleCapture` consumes the array return from `runCapture` and emits an `outputs: [...]` array in the SSE `done` frame. The UI form-builder shape stays single-viewport per D-05 — the schema normalizer absorbs the form payload. Existing UI clients continue to work; multi-viewport UI rendering is deferred.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /api/capture body → schema | Form payload arrives as JSON on the localhost-only listener; passed to `configSchema.safeParse` which enforces the same invariants as the YAML loader. No new untrusted-input surface added — the form-builder shape is unchanged per D-05. |
| /screenshots/* request → filesystem | Path-traversal guard in `handleStaticScreenshot` (lines 182-213) unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Elevation of Privilege | localhost binding | mitigate | `server.listen(port, '127.0.0.1', ...)` unchanged — no LAN exposure. |
| T-07-06 | Information Disclosure | /screenshots/* path traversal | mitigate | `absPath.startsWith(SCREENSHOT_ROOT + '/')` guard in `handleStaticScreenshot` unchanged. |
| T-07-07 | Tampering | UI form payload | mitigate | `configSchema.safeParse` (post-Wave-1) enforces same invariants as YAML loader; mutually exclusive `viewport`/`viewports`; field-named error messages via `formatZodError`. |

Local-only, no exploitable surface added by this phase. The form-builder shape preservation per D-05 means the localhost-only attack surface is unchanged.
</threat_model>

<verification>
Both per-task verify scripts produce their respective OK lines with exit 0. Additionally, run the v0.1 back-compat acceptance one more time at the plan level:

```bash
# v0.1 smoke (singular config + --smoke flag) — success criterion #4
node samples/serve-smoke.js &
SERVER_PID=$!
sleep 1
rm -rf /tmp/framershot-smoke
node index.js capture samples/smoke.yaml --smoke && \
  ls /tmp/framershot-smoke/home.png && \
  echo "v0.1 SMOKE BACK-COMPAT: ok"
kill $SERVER_PID 2>/dev/null

# v0.1 non-smoke (singular config, full pipeline) — must emit 1 stdout line
node samples/serve-smoke.js &
SERVER_PID=$!
sleep 1
rm -rf /tmp/framershot-smoke
LINES=$(node index.js capture samples/smoke.yaml | grep -c '^screenshot written: ')
test "$LINES" -eq 1 && echo "v0.1 NON-SMOKE BACK-COMPAT: ok ($LINES line)"
kill $SERVER_PID 2>/dev/null
```

Both lines must print OK. Multi-viewport E2E is Plan 04's responsibility (smoke-multi fixture).
</verification>

<success_criteria>
- CLI non-smoke branch consumes the array return and emits one stdout line per viewport
- CLI --smoke branch reads `config.viewports[0]` (singular `config.viewport.` is gone from the file)
- Server `handleCapture` emits the new `outputs: [...]` done-frame shape
- v0.1 singular smoke fixture still passes end-to-end (smoke + non-smoke paths)
- Server's UI form-builder shape unchanged (D-05)
- Server still binds to `127.0.0.1` only
- Zero changes outside the two adapter touchpoints
</success_criteria>

<output>
Create `.planning/phases/07-multi-viewport-capture/07-03-SUMMARY.md` when done — record the spinner-prefix format choice (the planner picks `[<viewport>] <label>`; alternates fine if cleaner) and the exact `outputs: [...]` payload shape for the SSE `done` frame.
</output>
