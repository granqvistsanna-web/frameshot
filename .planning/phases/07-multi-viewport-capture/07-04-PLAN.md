---
phase: 07-multi-viewport-capture
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - samples/smoke-multi.yaml
autonomous: false
requirements: [MULTI-01]
requirements_addressed: [MULTI-01]

must_haves:
  truths:
    - "A hermetic 2-viewport `framershot capture samples/smoke-multi.yaml` run against `samples/serve-smoke.js` produces exactly 2 PNG files at distinct paths"
    - "Each PNG's path is template-resolved using its viewport's `name` — paths differ by viewport segment, so no overwrites occur (ROADMAP success criterion #2)"
    - "`samples/smoke.yaml` is UNCHANGED — its existence is the v0.1 back-compat evidence (ROADMAP success criterion #4)"
    - "The new fixture targets the same hermetic fixture server (`samples/serve-smoke.js` on `http://127.0.0.1:7357`) — no new server, no network dependency"
    - "Output paths land under `/tmp/framershot-smoke-multi/<viewport>/<page>.png` (matches the `/tmp/` convention from `samples/smoke.yaml` — no repo pollution, no `.gitignore` work)"
  artifacts:
    - path: "samples/smoke-multi.yaml"
      provides: "2-viewport hermetic smoke fixture demonstrating ROADMAP success criterion #5 end-to-end"
      contains: "viewports:"
    - path: "samples/smoke.yaml"
      provides: "UNCHANGED — back-compat evidence for ROADMAP success criterion #4"
      contains: "viewport:"
  key_links:
    - from: "samples/smoke-multi.yaml"
      to: "samples/serve-smoke.js"
      via: "baseUrl: http://127.0.0.1:7357 (same fixture host)"
      pattern: "127\\.0\\.0\\.1:7357"
    - from: "samples/smoke-multi.yaml output template"
      to: "src/output/template.js resolveTemplate"
      via: "{viewport} placeholder slugified per-iteration in runCapture's loop"
      pattern: "\\{viewport\\}"
---

<objective>
Produce the hermetic 2-viewport smoke fixture that demonstrates ROADMAP success criterion #5 ("The hermetic smoke fixture demonstrates a 2-viewport run producing 2 distinct PNGs end-to-end") and provides the canonical proof for ROADMAP success criterion #2 ("`{viewport}` placeholder resolves to per-viewport `name` so PNGs land at distinct paths"). Keep `samples/smoke.yaml` untouched — it's the back-compat acceptance evidence for success criterion #4.

Purpose: The hermetic smoke fixture pattern (`samples/serve-smoke.js` + `samples/*.yaml`) is the established Phase 3-6 verification standard. Two fixtures (singular + multi) is cleaner than mutating one and losing the singular-shape coverage — CONTEXT.md "Locked behavior" explicitly chose this approach.

Output: One new YAML fixture file at `samples/smoke-multi.yaml`, modeled exactly on `samples/smoke.yaml` with plural viewports + `{viewport}` placeholder in the output template, plus a human checkpoint confirming a 2-viewport `framershot capture samples/smoke-multi.yaml` run produces 2 distinct PNG files end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-multi-viewport-capture/07-CONTEXT.md
@.planning/phases/07-multi-viewport-capture/07-PATTERNS.md
@.planning/phases/07-multi-viewport-capture/07-02-SUMMARY.md
@samples/smoke.yaml
@samples/serve-smoke.js

<interfaces>
<!-- Existing smoke fixture for reference (samples/smoke.yaml) -->

Existing `samples/smoke.yaml` shape (lines 32-50):
- `name: phase-04-smoke`
- `baseUrl: http://127.0.0.1:7357`
- `output: /tmp/framershot-smoke/{page}.png`
- `deviceScaleFactor: 2`
- `viewport: { width: 800, height: 600, name: smoke }`
- `page: { path: "/", name: home }`
- `prepare: { animations: true, hide: ['.hidden-by-test', '.does-not-exist'], scrollPrime: true, extraDelay: 1000 }`

Hermetic fixture server (samples/serve-smoke.js):
- Listens on `127.0.0.1:7357` (PORT constant in the file)
- Serves a single page at `/` with:
  - `.hidden-by-test` element (exercises the PREP-03 hide path)
  - lazy `<img>` below a tall spacer (exercises the PREP-04 scroll-prime + lazy-load path)
- No state; reentrant; safe to run multiple captures against it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create samples/smoke-multi.yaml — 2 viewports, {viewport} in output template, same hermetic fixture host as samples/smoke.yaml</name>
  <files>samples/smoke-multi.yaml</files>
  <read_first>
    - samples/smoke.yaml (sibling fixture — copy the shape, swap singular for plural, add {viewport} placeholder)
    - samples/serve-smoke.js (confirm the fixture server hostname/port + that no server changes are needed)
    - .planning/phases/07-multi-viewport-capture/07-PATTERNS.md §"samples/smoke-multi.yaml (NEW fixture)" (the planner-spec Phase 7 adaptation)
    - .planning/phases/07-multi-viewport-capture/07-CONTEXT.md §"Locked behavior" (two-fixture pattern explained)
  </read_first>
  <action>
    Create `samples/smoke-multi.yaml` with the following structure (modeled on `samples/smoke.yaml` lines 32-50, with the substitutions called out in PATTERNS.md §"Phase 7 adaptation"):

    - Leading header comment block (5-10 lines) explaining: this is the Phase 7 multi-viewport hermetic smoke fixture; it targets the same `samples/serve-smoke.js` server as `samples/smoke.yaml`; it's the canonical evidence for ROADMAP success criteria #5 (2-viewport run → 2 PNGs) and #2 (`{viewport}` placeholder → distinct paths); `samples/smoke.yaml` stays as the v0.1 back-compat fixture (criterion #4); to run locally: `node samples/serve-smoke.js &` then `node index.js capture samples/smoke-multi.yaml` then `kill %1`.
    - `name: phase-07-smoke-multi`
    - `baseUrl: http://127.0.0.1:7357` (same fixture server, no server changes per CONTEXT.md)
    - `output: /tmp/framershot-smoke-multi/{viewport}/{page}.png` — the `{viewport}` placeholder is the success-criterion-#2 evidence; the `/{viewport}/` segment guarantees the two PNGs land at distinct paths even with the same page name. `/tmp/` keeps repo clean (no `.gitignore` work needed).
    - `deviceScaleFactor: 2` (top-level per D-04)
    - `viewports:` — array of two entries:
      - `{ name: desktop, width: 800, height: 600 }` (matches the existing fixture viewport so the fixture page renders cleanly)
      - `{ name: mobile, width: 375, height: 667 }` (mobile-portrait dimensions; the fixture page is responsive enough to render at this size)
    - `page: { path: "/", name: home }` (same fixture page)
    - `prepare:` — same shape as `samples/smoke.yaml` to exercise the same prepare-pipeline branches per viewport:
      - `animations: true`
      - `hide: ['.hidden-by-test', '.does-not-exist']` — the `.does-not-exist` entry is intentional, exercises the per-viewport selector-warning event path (Phase 6 pattern); leaving it in confirms the warning event fires PER VIEWPORT (2 warning events, one per viewport — verified by the human checkpoint in Task 2)
      - `scrollPrime: true`
      - `extraDelay: 1000` — matches `samples/smoke.yaml` for parity

    Do NOT introduce any new YAML keys (e.g. no per-viewport `deviceScaleFactor` per D-04 — deferred). Do NOT modify `samples/smoke.yaml`. Do NOT modify `samples/serve-smoke.js`. Do NOT add a `.gitignore` entry (the `/tmp/` output path keeps the repo clean).
  </action>
  <verify>
    <automated>
      # File exists
      test -f samples/smoke-multi.yaml || { echo "FAIL fixture file missing"; exit 1; }
      # Singular smoke fixture untouched
      git diff --quiet -- samples/smoke.yaml || { echo "FAIL samples/smoke.yaml was modified — must stay unchanged"; exit 1; }
      # Required keys present
      grep -q "^viewports:" samples/smoke-multi.yaml || { echo "FAIL no viewports: key"; exit 1; }
      grep -q "name: desktop" samples/smoke-multi.yaml || { echo "FAIL desktop viewport missing"; exit 1; }
      grep -q "name: mobile" samples/smoke-multi.yaml || { echo "FAIL mobile viewport missing"; exit 1; }
      grep -q "{viewport}" samples/smoke-multi.yaml || { echo "FAIL {viewport} placeholder missing from output template"; exit 1; }
      grep -q "127.0.0.1:7357" samples/smoke-multi.yaml || { echo "FAIL wrong baseUrl"; exit 1; }
      # Schema validation passes for the new fixture
      node -e "
      import('./src/config/load.js').then(async ({ loadConfig }) => {
        const cfg = await loadConfig('samples/smoke-multi.yaml');
        if (!Array.isArray(cfg.viewports) || cfg.viewports.length !== 2) { console.error('FAIL viewports', cfg); process.exit(1); }
        if (cfg.viewports[0].name !== 'desktop' || cfg.viewports[1].name !== 'mobile') { console.error('FAIL names', cfg.viewports); process.exit(1); }
        console.log('FIXTURE VALIDATES OK');
      });
      " || exit 1
      # End-to-end hermetic 2-viewport run produces 2 distinct PNGs
      rm -rf /tmp/framershot-smoke-multi
      node samples/serve-smoke.js &
      SERVER_PID=$!
      sleep 1
      OUTPUT=$(node index.js capture samples/smoke-multi.yaml 2>&1)
      CAPTURE_EXIT=$?
      kill $SERVER_PID 2>/dev/null
      test $CAPTURE_EXIT -eq 0 || { echo "FAIL capture exit: $CAPTURE_EXIT — $OUTPUT"; exit 1; }
      test -f /tmp/framershot-smoke-multi/desktop/home.png || { echo "FAIL desktop PNG missing"; exit 1; }
      test -f /tmp/framershot-smoke-multi/mobile/home.png || { echo "FAIL mobile PNG missing"; exit 1; }
      LINES=$(echo "$OUTPUT" | grep -c "^screenshot written: ")
      test "$LINES" -eq 2 || { echo "FAIL expected 2 stdout lines, got $LINES: $OUTPUT"; exit 1; }
      echo "SMOKE-MULTI OK — 2 PNGs at distinct paths"
    </automated>
  </verify>
  <acceptance_criteria>
    - `samples/smoke-multi.yaml` exists and is a valid YAML file
    - `samples/smoke.yaml` is bit-identical to the version on HEAD (`git diff --quiet -- samples/smoke.yaml` returns 0)
    - `loadConfig('samples/smoke-multi.yaml')` returns an object with `viewports.length === 2` and names `'desktop'`/`'mobile'`
    - End-to-end: `node index.js capture samples/smoke-multi.yaml` (with the fixture server running) exits 0
    - Both `/tmp/framershot-smoke-multi/desktop/home.png` AND `/tmp/framershot-smoke-multi/mobile/home.png` exist after the run
    - The CLI prints exactly 2 `screenshot written: ` lines on stdout (one per viewport, matching the v0.1 per-screenshot stdout convention)
    - The verify script prints `SMOKE-MULTI OK — 2 PNGs at distinct paths` with exit 0
  </acceptance_criteria>
  <done>
    The new fixture file exists and a hermetic 2-viewport capture run against `samples/serve-smoke.js` produces exactly 2 PNGs at template-resolved distinct paths. `samples/smoke.yaml` is untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Eyeball the two PNGs — confirm distinct viewport sizes + clean prepare pipeline per viewport</name>
  <what-built>
    A hermetic 2-viewport `framershot capture` end-to-end produced two PNG files at:
      /tmp/framershot-smoke-multi/desktop/home.png   (800×600 CSS pixels × DSR 2 → 1600×1200 raster)
      /tmp/framershot-smoke-multi/mobile/home.png    (375×667 CSS pixels × DSR 2 → 750×1334 raster)
    Per-viewport selector-warning events (from the intentional `.does-not-exist` selector) fired twice — once per viewport — and the v0.1 prepare pipeline (animation guards, IO shim, hide, scroll-prime, extraDelay) ran per viewport.
  </what-built>
  <how-to-verify>
    1. Confirm both PNGs exist on disk:
       `ls -la /tmp/framershot-smoke-multi/desktop/home.png /tmp/framershot-smoke-multi/mobile/home.png`

    2. Confirm the PNGs have the expected raster dimensions (sharp's metadata works, but `file` also works):
       `file /tmp/framershot-smoke-multi/desktop/home.png` — should report width 1600 (800 × DSR 2)
       `file /tmp/framershot-smoke-multi/mobile/home.png`  — should report width 750  (375 × DSR 2)

    3. Open both PNGs visually (`open /tmp/framershot-smoke-multi/desktop/home.png` and the mobile one). Confirm:
       - Desktop PNG looks proportionally wider than mobile (different aspect ratios)
       - The `.hidden-by-test` element is HIDDEN in both (success criterion #3 — per-viewport prepare pipeline ran cleanly)
       - Neither PNG shows ghosted sticky elements, half-played animations, or blank lazy-loaded images (the v0.1 invariants per-viewport)
       - Both PNGs cover the full page height (scroll-stitch fired per viewport)

    4. Re-read the capture-run terminal output. Confirm:
       - Exactly 2 `screenshot written: ` lines (one per viewport, in array order: desktop then mobile)
       - The spinner showed per-viewport prefixes (e.g. `[desktop] Capturing frame X/N` then `[mobile] Capturing frame X/N`)
       - The `.does-not-exist` selector warning surfaced TWICE (once per viewport — proves per-viewport isolation of the prepare pipeline)

    5. Confirm `samples/smoke.yaml` (v0.1 singular fixture) STILL works unchanged:
       `node samples/serve-smoke.js &`
       `node index.js capture samples/smoke.yaml --smoke`
       `kill %1`
       — should still produce `/tmp/framershot-smoke/home.png` and exit 0.

    If everything above passes, type `approved`. If anything is off (PNGs the wrong size, hide selectors not applied, only one warning event, v0.1 smoke regressed, etc.), describe what's wrong and I'll diagnose before declaring Phase 7 done.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none added) | The new YAML fixture is committed source authored by the developer — it's input to the same validated pipeline as `samples/smoke.yaml`. No new untrusted-input surface. The fixture server (`samples/serve-smoke.js`) is unchanged and stays bound to 127.0.0.1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | (n/a) | fixture file | accept | Static YAML input fixture, hermetic localhost target, /tmp/ output. Local-only, no exploitable surface added by this phase. |

Local-only, no exploitable surface added by this phase.
</threat_model>

<verification>
The Task 1 verify script provides the automated proof (`SMOKE-MULTI OK — 2 PNGs at distinct paths` with exit 0). The Task 2 human checkpoint provides the visual + behavioral confirmation that the prepare pipeline ran cleanly per viewport.

Phase 7's overall acceptance evidence (success criteria 1-5) is satisfied by:
- SC #1 (one PNG per viewport per run) — Task 1 verify (2 PNGs out)
- SC #2 ({viewport} placeholder → distinct paths) — Task 1 verify (paths under `/desktop/` and `/mobile/`)
- SC #3 (own browser context per viewport) — Plan 02 lifecycle + 2-warning-events check in Task 2
- SC #4 (v0.1 singular config still works) — Task 2 step 5 (smoke.yaml --smoke run)
- SC #5 (hermetic 2-viewport demo) — Task 1 verify end-to-end run
</verification>

<success_criteria>
- `samples/smoke-multi.yaml` exists and validates against the post-Wave-1 schema
- `samples/smoke.yaml` is bit-identical to HEAD
- Hermetic 2-viewport capture run produces 2 PNG files at template-resolved distinct paths
- 2 `screenshot written: ` stdout lines
- Per-viewport selector-warning events fire twice (one per viewport — confirms per-viewport prepare pipeline isolation)
- Human checkpoint confirms the PNGs look right visually and v0.1 smoke fixture still works
</success_criteria>

<output>
Create `.planning/phases/07-multi-viewport-capture/07-04-SUMMARY.md` when done — record the exact 2 PNG paths produced + their `file`-reported dimensions, and the human checkpoint's `approved`/diagnosis verdict.
</output>
